---
phase: 01-foundation-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .npmrc
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - eslint.config.mjs
  - .prettierrc.json
  - .prettierignore
  - .gitignore
  - .env.example
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - README.md
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - FOUND-09
  - FOUND-11
  - FOUND-12
  - FOUND-13

must_haves:
  truths:
    - "A developer can run `pnpm install` and `pnpm dev` and load http://localhost:3000 in a browser without errors."
    - "The browser tab title at http://localhost:3000 contains the string 'James Nhek'."
    - "The page renders Geist Sans for body and headings, with no FOUT/FOIT, in a near-black ink color on a near-white paper background."
    - "Typography scales smoothly via clamp() at 375px, 768px, and 1280px viewports with no horizontal scroll and no clipped headings."
    - "`pnpm lint`, `pnpm format:check`, and `pnpm typecheck` all exit 0 with TypeScript strict + noUncheckedIndexedAccess enforced."
    - "`pnpm build` succeeds with no `tailwind.config.js` present and no `output: 'export'` in next.config.ts."
  artifacts:
    - path: "package.json"
      provides: "Pinned packageManager (pnpm@<exact>), engines.node >=22.18, all scripts (dev/build/start/lint/format/format:check/typecheck), Next 16 + React 19.2 + Tailwind v4 + TS + Prettier + ESLint + zod + lucide-react dependencies"
      contains: "packageManager"
    - path: "tsconfig.json"
      provides: "TypeScript strict config (FOUND-04)"
      contains: "noUncheckedIndexedAccess"
    - path: "next.config.ts"
      provides: "Typed next config WITHOUT output: 'export' (FOUND-12)"
    - path: "postcss.config.mjs"
      provides: "@tailwindcss/postcss plugin entry (Tailwind v4)"
      contains: "@tailwindcss/postcss"
    - path: "eslint.config.mjs"
      provides: "ESLint 9 flat config with eslint-config-next + prettier (FOUND-11)"
      contains: "eslint-config-prettier/flat"
    - path: ".prettierrc.json"
      provides: "Prettier config with prettier-plugin-tailwindcss"
      contains: "prettier-plugin-tailwindcss"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS-first @theme tokens (color + type scale); no tailwind.config.js (FOUND-02, FOUND-05, FOUND-06)"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Geist Sans + Geist Mono via next/font/google, metadataBase, default title containing 'James Nhek' (FOUND-03, FOUND-09)"
      contains: "James Nhek"
    - path: "src/app/page.tsx"
      provides: "Home shell with inline hero + 5 numbered section placeholders (D-07 verbatim copy from UI-SPEC) — refactored to primitives in Plan 02"
  key_links:
    - from: "src/app/layout.tsx"
      to: "next/font/google (Geist, Geist_Mono)"
      via: "import + variable: '--font-geist-sans' / '--font-geist-mono'"
      pattern: "next/font/google"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import './globals.css'"
      pattern: "globals.css"
    - from: "src/app/globals.css"
      to: "next/font CSS variables"
      via: "@theme inline { --font-sans: var(--font-geist-sans); ... }"
      pattern: "@theme inline"
    - from: "package.json"
      to: "pnpm runtime + Node 22 LTS"
      via: "packageManager + engines.node"
      pattern: "packageManager"
---

<objective>
Scaffold a Next.js 16 + Tailwind v4 + TypeScript strict project on pnpm/Node 22, wire Geist Sans + Geist Mono via `next/font/google`, define the CSS-first `@theme` design tokens from UI-SPEC.md (mono palette + clamp() type scale) inside `src/app/globals.css`, set up ESLint flat config (no `next lint`) and Prettier with `prettier-plugin-tailwindcss`, and render an inline home shell (`src/app/page.tsx`) that displays the hero + 5 numbered section placeholders with the verbatim copy from UI-SPEC.md's Copywriting Contract. The slice ends with a developer being able to run `pnpm install && pnpm dev` and see a typographically-correct, monochrome page at http://localhost:3000 whose tab title contains "James Nhek".

Purpose: Lock the toolchain end-to-end before any reusable components or env validation are introduced. This is the smallest slice that proves Next.js 16 + Tailwind v4 + Geist + TypeScript strict + pnpm + Node 22 all work together in one buildable, lintable, type-checkable, locally-runnable repo.

Output: A buildable scaffold at `/Users/pnhek/usf msds/github/portfolio/` with the home shell rendering inline (no primitives yet — Plan 02 refactors). After this plan, `pnpm lint && pnpm typecheck && pnpm build && pnpm dev` all succeed and the local browser shows a Geist-rendered, monochrome shell that closely matches what Phase 2 will populate.

User-visible delta: From "empty repo with only `.planning/` and `CLAUDE.md`" → "I open localhost:3000 and see a typographically-correct portfolio shell rendered in Geist Sans on near-white paper with `01. About` / `02. Experience` / `03. Featured Projects` / `04. Uses` / `05. Contact` section headings and verbatim 'Coming soon — ...' placeholders, scrolling cleanly at 375 / 768 / 1280 viewports."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-slice/01-CONTEXT.md
@.planning/phases/01-foundation-slice/01-RESEARCH.md
@.planning/phases/01-foundation-slice/01-UI-SPEC.md
@.planning/phases/01-foundation-slice/SKELETON.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs in scope. All sourced from UI-SPEC.md and RESEARCH.md. -->
<!-- Executor MUST NOT re-derive these — read the source files for full context, but the wire-level contract is below. -->

### `@theme` token names (UI-SPEC.md `## Tailwind v4 @theme Token Block` — names are binding; values may be tuned ±2% only if contrast verification demands it)

- `--font-sans` → `var(--font-geist-sans)`        (declared inside `@theme inline`)
- `--font-mono` → `var(--font-geist-mono)`        (declared inside `@theme inline`)
- `--color-paper`     `oklch(0.985 0 0)`           (~#fafafa, page bg)
- `--color-ink`       `oklch(0.18 0 0)`            (~#0a0a0a, text + diagram strokes)
- `--color-ink-muted` `oklch(0.55 0 0)`            (~#737373, anchors, captions, "Coming soon" placeholders)
- `--color-rule`      `oklch(0.92 0 0)`            (~#e5e5e5, hairlines)
- `--text-caption`    `clamp(13px, 0.85rem, 14px)`
- `--text-body`       `clamp(17px, 1rem + 0.1vw, 19px)`
- `--text-subhead`    `clamp(20px, 1.125rem + 0.4vw, 24px)`
- `--text-heading`    `clamp(24px, 1.25rem + 1.1vw, 32px)`
- `--text-display`    `clamp(36px, 1.5rem + 4.5vw, 56px)`
- `--leading-tight`   `1.1`
- `--leading-snug`    `1.2`
- `--leading-display` `1.1`
- `--leading-heading` `1.2`
- `--leading-body`    `1.6`
- `--leading-caption` `1.4`

### CRITICAL idiom corrections from RESEARCH.md (override UI-SPEC.md where they differ)

- The font block uses `@theme inline { ... }` (NOT plain `@theme`) because the values are `var()` references. The color and type-scale tokens may live in a plain `@theme { ... }` block. Two blocks total, in this order.
- ESLint script in `package.json` MUST be `"lint": "eslint ."` — `next lint` is removed in Next 16.
- `eslint-config-prettier` is imported as `eslint-config-prettier/flat` and MUST be the LAST entry in the flat-config array.
- The `next/font/google` import uses `Geist` and `Geist_Mono` (variable fonts — do NOT pass a `weight` array). `subsets: ["latin"]` is required.

### Verbatim Phase 1 copy (UI-SPEC.md Copywriting Contract — do NOT paraphrase)

- Tab `<title>`: `James Nhek — AI Engineer`
- Meta description: `AI Engineer at Asurion. RAG, evaluations, and agentic workflows. Based in San Francisco. Open to roles.`
- Hero display: `James Nhek`
- Hero subhead: `AI Engineer @ Asurion`
- Hero body 1: `RAG · evaluations · agentic workflows`
- Hero body 2: `San Francisco — open to AI Engineer roles.`
- `01. About` (anchor `#about`) — placeholder: `Coming soon — the tax-analyst → AI-engineer pivot.`
- `02. Experience` (anchor `#experience`) — placeholder: `Coming soon — Asurion and prior roles.`
- `03. Featured Projects` (anchor `#projects`) — placeholder: `Coming soon — four projects on agents, RAG, evaluations, and data pipelines.`
- `04. Uses` (anchor `#uses`) — placeholder: `Coming soon — model defaults, MCP servers, eval stack, agent framework.`
- `05. Contact` (anchor `#contact`) — placeholder: `Coming soon — email, LinkedIn, GitHub.`

### Standard stack (RESEARCH.md `## Standard Stack`, versions verified 2026-05-20)

- `next` — let `create-next-app@latest` resolve to `^16.2.x`
- `react` / `react-dom` — `^19.2.x` (bundled by scaffold)
- `typescript` — whatever `create-next-app` pulls (`^5.6`+ acceptable; `^6.0` also acceptable — Next 16 supports both)
- `tailwindcss` + `@tailwindcss/postcss` — `^4.3.x` (scaffolded)
- `zod` — `^3.23` (pin to `^3.23` if scaffold pulled 4.x; Plan 02 consumes)
- `lucide-react` — `^0.460` (per CLAUDE.md; Phase 1 installs Day 1 but does NOT import — first usage is Phase 2 Contact). Pin to `^0.460` (last 0.x) to honor CLAUDE.md exactly.
- `eslint` — `^9.x` (flat config)
- `eslint-config-next` — version matching `next`
- `eslint-config-prettier` — `^10.x`
- `prettier` — `^3.x`
- `prettier-plugin-tailwindcss` — `^0.8.x`

### Forbidden / banned in this plan (RESEARCH.md "Anti-Patterns to Avoid" + CLAUDE.md "What NOT to Use")

- `tailwind.config.js` of any form — must not exist
- `"lint": "next lint"` script — banned (Next 16 removed it)
- `output: 'export'` in `next.config.ts` — banned (FOUND-12)
- `"use client"` anywhere in this plan — Phase 1 ships zero client islands
- `Geist({ weight: [...] })` — Geist is a variable font; do not pass `weight`
- Hard-coding the "01." prefix anywhere — but in this plan we render headings inline; the numbered prefix appears as part of the section title content (Plan 02 extracts it into the `NumberedHeading` primitive). Use the inline string `"01. About"` etc. as plain `<h2>` content for now, exactly mirroring UI-SPEC.md's eventual primitive output.
- `framer-motion` (legacy name) — use `motion` if ever needed (not in this plan)
- `react-icons` umbrella — use `lucide-react` (not imported in this plan)
- `next-themes`, `@vercel/analytics`, `@vercel/speed-insights`, `resend`, `sonner`, `motion`, `@next/mdx`, `shadcn` — none install in Phase 1
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm initial package install (slopcheck human gate)</name>
  <what-built>
    No code yet. This is the package-legitimacy gate per RESEARCH.md `## Package Legitimacy Audit`. slopcheck was unavailable in research; the 14 packages we will install (`next`, `react`, `react-dom`, `typescript`, `tailwindcss`, `@tailwindcss/postcss`, `postcss`, `zod`, `lucide-react`, `eslint`, `eslint-config-next`, `eslint-config-prettier`, `prettier`, `prettier-plugin-tailwindcss`) are all `[ASSUMED]` legitimate based on cross-referencing CLAUDE.md, official Next.js docs, official Tailwind docs, and the npm registry — but the planner is required to gate first install behind a blocking human checkpoint.
  </what-built>
  <how-to-verify>
    1. Read RESEARCH.md `## Package Legitimacy Audit` — confirm all 14 packages are listed as Approved (from well-known maintainers with multi-million weekly downloads).
    2. Open https://www.npmjs.com/package/next and confirm publisher is "vercel" with 9+ years of history and ~9M weekly downloads.
    3. Open https://www.npmjs.com/package/tailwindcss and confirm publisher is "tailwindlabs" with 8+ years of history.
    4. Open https://www.npmjs.com/package/zod and confirm publisher is "colinhacks" and `^3.23` is a stable existing version (Zod 4 exists but we pin to 3.x).
    5. Open https://www.npmjs.com/package/lucide-react and confirm publisher is "lucide-icons" and `^0.460` exists.
    6. Approve to proceed with `pnpm create next-app@latest portfolio ...` and the supporting `pnpm add` commands in Task 2.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with scaffold + installs, or describe any concerns (e.g., "pin different zod version") for the executor to address before installing.</resume-signal>
  <acceptance_criteria>
    - User has typed "approved" (or equivalent) in the resume signal.
    - No package was installed before this checkpoint cleared.
    - T-01-SC (supply chain) threat documented in `<threat_model>` of this plan.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold Next.js 16 + Tailwind v4 + TS, install supporting deps, lock toolchain config</name>
  <read_first>
    - /Users/pnhek/usf msds/github/portfolio/CLAUDE.md (`## Technology Stack`, `## Recommended Stack`, `## What NOT to Use`, `## Installation`)
    - /Users/pnhek/usf msds/github/portfolio/.planning/phases/01-foundation-slice/01-RESEARCH.md (`## Standard Stack`, `## Architecture Patterns` Pattern 3 + Pattern 4 + Pattern 5, `## Common Pitfalls` 1 / 4 / 7 / 9 / 10)
    - /Users/pnhek/usf msds/github/portfolio/.planning/STATE.md (Key Decisions Locked — Stack pins)
    - /Users/pnhek/usf msds/github/portfolio/.planning/phases/01-foundation-slice/SKELETON.md (Architectural Decisions table)
  </read_first>
  <files>
    package.json, pnpm-lock.yaml, .npmrc, tsconfig.json, next.config.ts, postcss.config.mjs, eslint.config.mjs, .prettierrc.json, .prettierignore, .gitignore, .env.example, README.md
  </files>
  <action>
    Working directory is `/Users/pnhek/usf msds/github/portfolio/` (the repo root). The directory currently has `CLAUDE.md` and `.planning/` only; the scaffold writes alongside them.

    Run (do NOT create a subdirectory — scaffold into the current repo root by passing `.` as the project name):
      `pnpm create next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --turbopack`
    If the scaffolder prompts because the directory is non-empty, accept overwriting the default README only; do NOT overwrite anything under `.planning/` or `CLAUDE.md`.

    After scaffold, install supporting deps:
      `pnpm add zod@^3.23 lucide-react@^0.460`
      `pnpm add -D prettier prettier-plugin-tailwindcss eslint-config-prettier`

    Pin the package manager. Run `pnpm --version` to capture the exact pnpm version (e.g., `10.30.2`), then edit `package.json` to add a top-level `"packageManager": "pnpm@<exact-version>"` field AND an `"engines": { "node": ">=22.18" }` field (FOUND-13). Create `.npmrc` with `engine-strict=true` and `package-manager-strict=true` so wrong-tool invocations fail loudly.

    Edit `package.json` `scripts` to exactly:
      - `"dev": "next dev --turbopack"`
      - `"build": "next build --turbopack"`
      - `"start": "next start"`
      - `"lint": "eslint ."`              (CRITICAL: not `next lint` — Next 16 removed it; RESEARCH.md Pattern 4, Pitfall 1)
      - `"format": "prettier --write ."`
      - `"format:check": "prettier --check ."`
      - `"typecheck": "tsc --noEmit"`
    Remove any other scripts the scaffold added (e.g., `"lint": "next lint"`).

    Edit `tsconfig.json` to add `"noUncheckedIndexedAccess": true` under `compilerOptions` (the scaffold sets `strict: true` already — verify). Confirm `paths` is `{ "@/*": ["./src/*"] }` (Pitfall 9 — the `--src-dir --import-alias "@/*"` combo should emit this; verify post-scaffold).

    Replace `next.config.ts` (or `next.config.mjs` if scaffold emitted .mjs — keep .ts if Node 22.18+ is available) with a typed config that imports `NextConfig` from `next` and exports an empty config object (no `output`, no `experimental.turbopack` since Turbopack is stable in Next 16 and lives at the top-level `turbopack` key per Next 16 docs). Do NOT include `output: 'export'` anywhere (FOUND-12, RESEARCH.md Pitfall 3).

    Replace `eslint.config.mjs` with a flat config that imports `defineConfig` and `globalIgnores` from `eslint/config`, imports `eslint-config-next/core-web-vitals` and `eslint-config-next/typescript`, imports `eslint-config-prettier/flat` (note the `/flat` subpath), and exports `defineConfig([...nextVitals, ...nextTs, prettier, globalIgnores([".next/**", "out/**", "build/**", "next-env.d.ts"])])`. The `prettier` config must be the last entry before `globalIgnores` (RESEARCH.md Pattern 4).

    Create `.prettierrc.json` with `{ "semi": true, "singleQuote": false, "trailingComma": "all", "printWidth": 80, "plugins": ["prettier-plugin-tailwindcss"] }` (RESEARCH.md Pattern 5).

    Create `.prettierignore` with: `.next/`, `out/`, `node_modules/`, `pnpm-lock.yaml`, `public/`, `.planning/` (the planning prose has its own conventions — Prettier should not reflow it), `next-env.d.ts`.

    Verify `.gitignore` (created by scaffold) covers `.env*` (FOUND-10 / V8 Data Protection). If missing, add `.env*.local` and `.env` patterns. Do NOT ignore `.env.example`.

    Create `.env.example` with a single line documenting `NEXT_PUBLIC_SITE_URL=https://pjnhek.com` (Plan 02 wires the zod parse).

    Overwrite the scaffold's default `README.md` with a one-paragraph stub: project name "pjnhek.com", one-line "Custom portfolio for James Nhek — AI Engineer at Asurion", and a "See `.planning/PROJECT.md` for the full brief" link. Include the local-run commands (`pnpm install`, `pnpm dev`, `pnpm lint`, `pnpm typecheck`, `pnpm build`).

    Per RESEARCH.md `## Don't Hand-Roll` and `## Common Pitfalls`: do NOT add Husky / lint-staged; do NOT add Vitest / Jest; do NOT initialize shadcn; do NOT add `@vercel/analytics`.

    After all edits, run `pnpm install` (re-link lockfile) and verify `pnpm lint`, `pnpm typecheck`, and `pnpm build` all exit 0 on a still-empty `src/app/page.tsx` (the scaffold default). Fix any scaffold-default lint or type issues encountered (typical: `app/page.tsx` scaffold uses `any` in a comment — clean as needed without removing files).
  </action>
  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/portfolio" &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; test ! -f tailwind.config.js &amp;&amp; test ! -f tailwind.config.ts &amp;&amp; test ! -f tailwind.config.mjs &amp;&amp; ! grep -RIn --include=next.config.\* -E "output\s*:\s*['\"]export['\"]" . &amp;&amp; ! grep -RIn --include=package.json -E "\"next lint\"" . &amp;&amp; grep -q "\"packageManager\": \"pnpm@" package.json &amp;&amp; grep -q "\"node\": \">=22\\.18\"" package.json &amp;&amp; node -e "const c=require('./tsconfig.json');process.exit(c.compilerOptions.strict===true&amp;&amp;c.compilerOptions.noUncheckedIndexedAccess===true?0:1)" &amp;&amp; grep -q "eslint-config-prettier/flat" eslint.config.mjs</automated>
  </verify>
  <done>
    `package.json` has `packageManager: pnpm@<exact>` and `engines.node: ">=22.18"`. `tsconfig.json` has `strict: true` and `noUncheckedIndexedAccess: true`. `next.config.ts` (or .mjs) contains no `output: 'export'`. `eslint.config.mjs` imports `eslint-config-prettier/flat` last. `.prettierrc.json` includes `prettier-plugin-tailwindcss`. `tailwind.config.{js,ts,mjs}` does NOT exist anywhere in the repo. The `lint` script is `eslint .` not `next lint`. `pnpm install`, `pnpm lint`, `pnpm typecheck`, and `pnpm build` all exit 0.
  </done>
  <acceptance_criteria>
    - `pnpm lint` exits 0
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
    - `pnpm --version` matches the version pinned in `package.json` `packageManager` field
    - `node -e "const c=require('./tsconfig.json');process.exit(c.compilerOptions.strict===true&&c.compilerOptions.noUncheckedIndexedAccess===true?0:1)"` exits 0
    - `node -e "const p=require('./package.json');process.exit(p.packageManager?.startsWith('pnpm@')&&/22(\.|\b)/.test(p.engines?.node||'')?0:1)"` exits 0
    - `find . -maxdepth 2 -name "tailwind.config.*" -not -path "./node_modules/*"` prints no results
    - `grep -RIn -E "output\s*:\s*['\"]export['\"]" next.config.* 2>/dev/null | grep -v '^#'` prints no results
    - `grep -q "next lint" package.json && echo FAIL || echo OK` prints "OK"
    - `grep -q "eslint-config-prettier/flat" eslint.config.mjs` succeeds
    - `grep -q "engine-strict=true" .npmrc` succeeds
    - `.env.example` exists and contains `NEXT_PUBLIC_SITE_URL`
    - `.gitignore` contains an `.env`-pattern entry (e.g. `.env*.local`)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace globals.css with `@theme` design tokens; wire Geist + metadata in layout.tsx; render inline home shell</name>
  <read_first>
    - /Users/pnhek/usf msds/github/portfolio/.planning/phases/01-foundation-slice/01-UI-SPEC.md (`## Typography`, `## Color`, `## Tailwind v4 @theme Token Block`, `## Copywriting Contract`, `## Page Composition (Home Route, Phase 1)`)
    - /Users/pnhek/usf msds/github/portfolio/.planning/phases/01-foundation-slice/01-RESEARCH.md (`## Architecture Patterns` Pattern 1 + Pattern 2, `## Common Pitfalls` 2 / 6 / 10)
    - /Users/pnhek/usf msds/github/portfolio/.planning/phases/01-foundation-slice/01-CONTEXT.md (`<decisions>` D-03 / D-04 / D-05 / D-06 / D-07)
    - /Users/pnhek/usf msds/github/portfolio/src/app/globals.css (current scaffold default — we replace it)
    - /Users/pnhek/usf msds/github/portfolio/src/app/layout.tsx (current scaffold default — we rewrite metadata + fonts)
    - /Users/pnhek/usf msds/github/portfolio/src/app/page.tsx (current scaffold default — we replace with inline home shell)
  </read_first>
  <files>
    src/app/globals.css, src/app/layout.tsx, src/app/page.tsx
  </files>
  <action>
    Overwrite `src/app/globals.css` to contain, in order:
      1. `@import "tailwindcss";`
      2. `@theme inline { ... }` block declaring `--font-sans: var(--font-geist-sans);` and `--font-mono: var(--font-geist-mono);` (the `inline` modifier is REQUIRED per RESEARCH.md Pattern 1 — Pitfall 2 explains why).
      3. `@theme { ... }` block declaring all four `--color-*` tokens (`--color-paper`, `--color-ink`, `--color-ink-muted`, `--color-rule`) with the oklch values from the `<interfaces>` block above, all five `--text-*` clamp() tokens, and all six `--leading-*` values from UI-SPEC.md `## Tailwind v4 @theme Token Block`.
      4. Plain CSS rules outside `@theme`: `html { color: var(--color-ink); background: var(--color-paper); }`, `body { font-family: var(--font-sans); font-size: var(--text-body); line-height: var(--leading-body); }`, `::selection { background: var(--color-ink); color: var(--color-paper); }`.
    Do NOT define `--space-*` tokens — Tailwind v4's default spacing scale already covers every `px-*`, `py-*`, `gap-*`, `mb-*` value used in UI-SPEC.md per RESEARCH.md Pattern 1's "Why we are NOT defining `--space-*` tokens" note. Adding them would be a documentation noise win at best.

    Replace `src/app/layout.tsx` to:
      1. Import `Geist` and `Geist_Mono` from `next/font/google` (NOT from `geist/font/sans` — the `next/font/google` route is the locked path per CONTEXT.md, RESEARCH.md Pattern 2, and CLAUDE.md).
      2. Configure each with `subsets: ["latin"]` (REQUIRED — RESEARCH.md Pitfall 10), `display: "swap"`, and `variable: "--font-geist-sans"` / `variable: "--font-geist-mono"` respectively. Do NOT pass a `weight` array (Geist is a variable font — RESEARCH.md Anti-Patterns).
      3. Export `metadata` with `metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL ?? "https://pjnhek.com")`, `title: "James Nhek — AI Engineer"`, and `description: "AI Engineer at Asurion. RAG, evaluations, and agentic workflows. Based in San Francisco. Open to roles."` (verbatim from UI-SPEC.md Copywriting Contract — RESEARCH.md Pitfall 6 verifies the tab title contains "James Nhek"). NOTE: this plan uses `process.env.NEXT_PUBLIC_SITE_URL` with a fallback — Plan 02 will swap this to `env.NEXT_PUBLIC_SITE_URL` from `src/lib/env.ts` so removing the zod `.default()` fails the build (FOUND-10).
      4. Render `<html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>` with `<body>{children}</body>`. Import `"./globals.css"` at the top.
      5. The component remains a Server Component (no `"use client"`).

    Replace `src/app/page.tsx` to render the verbatim home shell from UI-SPEC.md `## Page Composition (Home Route, Phase 1)` AS INLINE HTML (no primitives yet — Plan 02 will refactor this into the 5 primitives without changing the visible output). Use the exact strings from UI-SPEC.md Copywriting Contract verbatim. Structure:
      - `<main>` wrapper.
      - `<header>` block (the hero — outside any `<section>`). Use UI-SPEC.md `## Page Composition` padding values: `px-6 pb-12 pt-24 md:px-12 md:pb-16 md:pt-32`. Inside, `<div className="mx-auto max-w-2xl">` with the hero `<h1>` (the only `<h1>` on the page, using `text-[length:var(--text-display)] font-medium tracking-[-0.02em] text-[color:var(--color-ink)]`), followed by three `<p>` lines: subhead (`text-[length:var(--text-subhead)] font-medium`), body specialization (`text-[length:var(--text-body)]`), body availability (`text-[length:var(--text-body)]`).
      - Five `<section id="..." className="py-16 md:py-24">` blocks, in order: `#about`, `#experience`, `#projects`, `#uses`, `#contact`. Each contains `<div className="mx-auto max-w-2xl px-6 md:px-12">` and inside it:
        - An `<h2 className="mb-8 md:mb-12 flex items-baseline gap-2">` with TWO `<span>`s: a mono+muted+tabular-nums span containing the literal text `"01."` (or `"02."`, ..., `"05."`) and a sans+heading span containing the title text (`"About"`, etc.). This mirrors UI-SPEC.md `NumberedHeading` output exactly — Plan 02 extracts it into the primitive without changing the visible output. Use `text-[length:var(--text-caption)] font-mono text-[color:var(--color-ink-muted)] tabular-nums` on the number span and `text-[length:var(--text-heading)] font-medium tracking-[-0.01em] text-[color:var(--color-ink)]` on the title span.
        - A `<p>` containing the verbatim "Coming soon — ..." placeholder for that section.
    Do NOT include an `ArchitectureDiagram` placeholder, a `Tag` chip, or an `ExternalLink` in this plan — those introduce the primitives that Plan 02 owns. Plan 02 also adds the placeholder SVG and the Tag/ExternalLink exercises into Sections 01 / 02 / 03 per UI-SPEC.md `## Page Composition`. This plan's page.tsx is intentionally text-only so Plan 02's diff is purely an additive refactor.
    Do NOT introduce `"use client"`. Do NOT import lucide-react icons. Do NOT add a footer or nav.

    Run `pnpm lint && pnpm typecheck && pnpm format && pnpm build && pnpm dev` and verify the browser at http://localhost:3000 renders Geist Sans on the near-white paper with all 5 numbered section headings + placeholders, tab title shows "James Nhek — AI Engineer", and DevTools viewport simulation at 375/768/1280 px shows no horizontal scroll and smooth clamp() growth on the hero `<h1>`.
  </action>
  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/portfolio" &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm format:check &amp;&amp; pnpm build &amp;&amp; grep -q "@import \"tailwindcss\";" src/app/globals.css &amp;&amp; grep -q "@theme inline" src/app/globals.css &amp;&amp; grep -q -- "--color-paper" src/app/globals.css &amp;&amp; grep -q -- "--color-ink-muted" src/app/globals.css &amp;&amp; grep -q -- "--text-display" src/app/globals.css &amp;&amp; grep -q "James Nhek — AI Engineer" src/app/layout.tsx &amp;&amp; grep -q "AI Engineer at Asurion" src/app/layout.tsx &amp;&amp; grep -q "next/font/google" src/app/layout.tsx &amp;&amp; grep -q "Geist_Mono" src/app/layout.tsx &amp;&amp; grep -q 'subsets:\s*\[\s*"latin"\s*\]' src/app/layout.tsx &amp;&amp; ! grep -q '"use client"' src/app/page.tsx &amp;&amp; ! grep -q '"use client"' src/app/layout.tsx &amp;&amp; grep -q 'id="about"' src/app/page.tsx &amp;&amp; grep -q 'id="experience"' src/app/page.tsx &amp;&amp; grep -q 'id="projects"' src/app/page.tsx &amp;&amp; grep -q 'id="uses"' src/app/page.tsx &amp;&amp; grep -q 'id="contact"' src/app/page.tsx &amp;&amp; grep -q "Coming soon — the tax-analyst → AI-engineer pivot." src/app/page.tsx</automated>
  </verify>
  <done>
    `src/app/globals.css` contains `@import "tailwindcss";` followed by `@theme inline` (fonts) and `@theme` (colors + type scale) blocks with the exact token names from UI-SPEC.md. `src/app/layout.tsx` imports `Geist` + `Geist_Mono` from `next/font/google` (with `subsets: ["latin"]`, `display: "swap"`, no `weight`), exports `metadata.title = "James Nhek — AI Engineer"` and `metadata.metadataBase = new URL(process.env.NEXT_PUBLIC_SITE_URL ?? "https://pjnhek.com")`, and applies both font CSS-variable className strings to `<html>`. `src/app/page.tsx` renders the hero + 5 numbered sections (`#about`, `#experience`, `#projects`, `#uses`, `#contact`) with the verbatim UI-SPEC.md Copywriting Contract text. No `"use client"` directives anywhere. `pnpm lint && pnpm typecheck && pnpm format:check && pnpm build` all exit 0.
  </done>
  <acceptance_criteria>
    - `grep -q "@import \"tailwindcss\";" src/app/globals.css` succeeds
    - `grep -q "@theme inline" src/app/globals.css` succeeds (Pitfall 2 mitigation)
    - `grep -q -- "--font-sans: var(--font-geist-sans);" src/app/globals.css` succeeds
    - All 4 color tokens present: `--color-paper`, `--color-ink`, `--color-ink-muted`, `--color-rule`
    - All 5 type tokens present: `--text-caption`, `--text-body`, `--text-subhead`, `--text-heading`, `--text-display`
    - `find . -maxdepth 2 -name "tailwind.config.*" -not -path "./node_modules/*"` still prints no results (FOUND-02 integrity)
    - `grep -q "James Nhek — AI Engineer" src/app/layout.tsx` succeeds (FOUND-09 / Pitfall 6)
    - `grep -q "next/font/google" src/app/layout.tsx` succeeds (FOUND-03)
    - `grep -q "subsets" src/app/layout.tsx` succeeds (Pitfall 10)
    - `grep -q "metadataBase" src/app/layout.tsx` succeeds (FOUND-09)
    - `grep -q "use client" src/app/page.tsx` does NOT match (RESEARCH.md Architectural Responsibility Map)
    - `grep -q "use client" src/app/layout.tsx` does NOT match
    - All 5 section IDs present: `id="about"`, `id="experience"`, `id="projects"`, `id="uses"`, `id="contact"`
    - All 5 verbatim "Coming soon — ..." placeholders present in `src/app/page.tsx`
    - `pnpm lint`, `pnpm typecheck`, `pnpm format:check`, `pnpm build` all exit 0
    - When `pnpm dev` is started and `curl -s http://localhost:3000` is run, the response body contains both `James Nhek — AI Engineer` (in `<title>`) and `Coming soon — the tax-analyst → AI-engineer pivot.` (in body)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine | Supply-chain edge: packages downloaded by `pnpm install` and `pnpm create next-app`. The 14 packages are `[ASSUMED]` per RESEARCH.md `## Package Legitimacy Audit` (slopcheck unavailable in research). |
| developer machine → repo | Files committed to git: a `.env*` leak would expose secrets, but Phase 1 has no secrets (`NEXT_PUBLIC_SITE_URL` is intentionally public). |
| `process.env` → `app/layout.tsx` `metadataBase` | Build-time input. In this plan it has a string fallback; Plan 02 hardens via zod and removes the fallback later (Phase 4). |
| (no other boundaries in Phase 1) | No auth, no DB, no user input, no API, no client interactivity per RESEARCH.md `## Architectural Responsibility Map`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm/pnpm installs (`pnpm create next-app`, `pnpm add zod lucide-react`, `pnpm add -D prettier prettier-plugin-tailwindcss eslint-config-prettier`) | mitigate | Task 1 is a blocking-human checkpoint that gates the first install behind manual confirmation (RESEARCH.md `## Package Legitimacy Audit`). After install, `pnpm-lock.yaml` pins every transitive dep; subsequent commits cannot silently swap a dep without a lockfile diff visible in PR review. `.npmrc` sets `engine-strict=true` and `package-manager-strict=true` so wrong-tool invocations (npm/yarn/bun) fail loudly. All 14 direct packages are from well-known maintainers (vercel, tailwindlabs, facebook, microsoft, colinhacks, lucide-icons, prettier, eslint) with multi-million weekly downloads — independently verified against authoritative sources per RESEARCH.md. Not auto-approvable (`workflow.auto_advance` ignored). |
| T-01-CFG-01 | Tampering | `next.config.ts` `output:'export'` regression | mitigate | Acceptance criteria + the `find` grep in `<verify><automated>` block any commit that adds `output:'export'` (FOUND-12, RESEARCH.md Pitfall 3). Phase verification (`<verification>` below) re-runs the check at phase gate. |
| T-01-CFG-02 | Tampering | `tailwind.config.{js,ts,mjs}` regression | mitigate | Acceptance criterion `test ! -f tailwind.config.*` blocks any commit that introduces v3-style JS config (FOUND-02, CLAUDE.md "What NOT to Use"). |
| T-01-CFG-03 | Tampering | `package.json` `lint` script regression to `next lint` | mitigate | Acceptance criterion `grep -q "next lint" package.json && echo FAIL || echo OK` blocks regression (RESEARCH.md Pitfall 1; Next 16 removed `next lint`). |
| T-01-INFO-01 | Information Disclosure | Secret committed to public `pjnhek/portfolio` repo (Phase 1 prep for DEP-01 in Plan 03) | accept | Phase 1 has no secrets to leak. `NEXT_PUBLIC_SITE_URL` is the only env var introduced and is intentionally public-by-design (CLAUDE.md V8 Data Protection note in RESEARCH.md `## Security Domain`). `.gitignore` covers `.env*.local` and `.env` patterns; `.env.example` is the only env file committed and contains only the public URL example. CLAUDE.md is explicit about Asurion confidentiality, which is enforced at Phase 2's confidentiality gate, not here. |
| T-01-CONFIG | Tampering | Build-time env-var consumption (`metadataBase`) | mitigate (deferred to Plan 02) | This plan reads `process.env.NEXT_PUBLIC_SITE_URL` with a string fallback. Plan 02 introduces `src/lib/env.ts` with zod validation and swaps `app/layout.tsx` to consume `env.NEXT_PUBLIC_SITE_URL`, after which removing the `.default()` makes the build hard-fail per FOUND-10 (RESEARCH.md Pitfall 5). |
| T-01-DOS | Denial of Service | Vercel preview URL exposure (Plan 03) | accept | Vercel preview URLs are non-guessable hashes and have no commercial cost on Hobby tier within Phase 1's expected traffic (a few PRs). Production-tier abuse handling is Phase 4 (DEP-08 usage alerts). |
</threat_model>

<verification>
## Phase-Level Checks (applicable to this plan)

Run from `/Users/pnhek/usf msds/github/portfolio/`:

1. `pnpm lint && pnpm typecheck && pnpm format:check && pnpm build` — all exit 0 (FOUND-01, FOUND-04, FOUND-11, FOUND-12).
2. `find . -maxdepth 2 -name "tailwind.config.*" -not -path "./node_modules/*"` — no results (FOUND-02).
3. `grep -RIn -E "output\s*:\s*['\"]export['\"]" next.config.* 2>/dev/null | grep -v '^\s*#'` — no results (FOUND-12).
4. `node -e "const c=require('./tsconfig.json');process.exit(c.compilerOptions.strict===true&&c.compilerOptions.noUncheckedIndexedAccess===true?0:1)"` — exit 0 (FOUND-04).
5. `node -e "const p=require('./package.json');process.exit(p.packageManager?.startsWith('pnpm@')&&/22(\.|\b)/.test(p.engines?.node||'')?0:1)"` — exit 0 (FOUND-13).
6. With `pnpm dev` running, `curl -s http://localhost:3000 | grep -o '<title[^>]*>[^<]*</title>'` returns a string containing "James Nhek" (FOUND-09 / ROADMAP success #2).
7. Manual DevTools check at 375 / 768 / 1280 px viewports — no horizontal scroll, no clipped hero heading (FOUND-06 / ROADMAP success #1).
8. Manual contrast spot-check (axe DevTools or Lighthouse a11y on the running page) — body text ink-on-paper passes 4.5:1 (FOUND-05). The token values from UI-SPEC.md are pre-verified to ≈18.8:1 ink/paper and ≈4.61:1 ink-muted/paper, so this should be a formality.
</verification>

<success_criteria>
This plan is complete when:
- A fresh clone + `pnpm install && pnpm dev` shows the styled shell at http://localhost:3000 in a browser, rendering Geist Sans for body and headings on the near-white paper, with all 5 numbered sections in the correct vertical order.
- The browser tab title contains the string "James Nhek".
- `pnpm lint`, `pnpm typecheck`, `pnpm format:check`, and `pnpm build` all exit 0.
- No `tailwind.config.{js,ts,mjs}` exists in the repo.
- No `output: 'export'` exists in `next.config.*`.
- `package.json` pins `packageManager: pnpm@<exact>` and `engines.node >=22.18`.
- All 5 section IDs match UI-SPEC.md anchor list (`#about`, `#experience`, `#projects`, `#uses`, `#contact`).
- Zero `"use client"` directives in `src/app/`.
- All 5 verbatim "Coming soon — ..." placeholders from UI-SPEC.md Copywriting Contract appear in `src/app/page.tsx`.
- Plan 02 can proceed (deps installed, file shapes ready for primitive extraction + env wiring).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-slice/01-01-SUMMARY.md` when done — record exact installed versions of `next`, `react`, `react-dom`, `typescript`, `tailwindcss`, `@tailwindcss/postcss`, `zod`, `lucide-react`, the exact `pnpm` version pinned in `packageManager`, the exact Node version (`node -v`) used locally, and a screenshot path or DevTools viewport notes at 375 / 768 / 1280. List any deviations from the planned versions (e.g., if pnpm pulled zod 4.x and was downgraded to ^3.23, note it). Note explicitly that env validation is deferred to Plan 02 and that `process.env.NEXT_PUBLIC_SITE_URL` currently has a string fallback in `app/layout.tsx`.
</output>
