---
phase: 01-foundation-slice
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - .github/pull_request_template.md
  - README.md
autonomous: false
requirements:
  - DEP-01
  - DEP-02
user_setup:
  - service: github
    why: "Public source repo `pjnhek/portfolio` — DEP-01. The repo is part of James's portfolio; recruiters who click 'View Source' land here."
    env_vars: []
    dashboard_config:
      - task: "Create the public repository `pjnhek/portfolio`"
        location: "https://github.com/new"
      - task: "Enable branch protection on `main`: Require a pull request before merging + Require status checks (Vercel) to pass"
        location: "https://github.com/pjnhek/portfolio/settings/branches"
  - service: vercel
    why: "Preview deploys on every PR — DEP-02. Production deploy stays on a `*.vercel.app` subdomain until Phase 4 cuts pjnhek.com over."
    env_vars: []
    dashboard_config:
      - task: "Import `pjnhek/portfolio` from GitHub as a new Vercel project; accept defaults (Framework: Next.js auto-detected; Build Command: pnpm build; Install: pnpm install)"
        location: "https://vercel.com/new"
      - task: "Settings → Git: Production branch `main`, Preview Branches: All branches"
        location: "Vercel project Settings → Git"

must_haves:
  truths:
    - "The public repository `https://github.com/pjnhek/portfolio` exists and contains the Plan 01 + Plan 02 code on the `main` branch."
    - "`main` is protected: PRs are required to merge, and Vercel's status check must pass."
    - "A Vercel project is linked to `pjnhek/portfolio` with preview deploys configured for every PR."
    - "Opening any PR to `main` produces a Vercel comment containing a `*.vercel.app` preview URL."
    - "Visiting that preview URL returns 200 and the response body's `<title>` contains 'James Nhek'."
    - "The legacy `pnhek.github.io` is unchanged in this phase (Phase 4 decommissions it per DEP-07)."
  artifacts:
    - path: "README.md"
      provides: "Recruiter-facing landing in `View Source` — links to .planning/PROJECT.md, lists local-run commands, notes preview-deploy convention"
    - path: ".github/pull_request_template.md"
      provides: "Standard PR template — checklist gates lint/typecheck/build pass + visual preview-URL link before merge"
  key_links:
    - from: "github.com/pjnhek/portfolio"
      to: "vercel.com project"
      via: "Vercel GitHub App installed on the repo"
      pattern: "vercel"
    - from: "PR opened on pjnhek/portfolio"
      to: "Vercel preview deploy URL"
      via: "Vercel comment in PR conversation"
      pattern: "vercel.app"
    - from: "Vercel preview URL"
      to: "Static HTML rendered from Plan 02's home shell"
      via: "next build (Turbopack) on Vercel"
      pattern: "<title.*James Nhek"
---

<objective>
Take the locally-buildable codebase from Plan 02 and lift it onto the public infrastructure that powers Phase 1's "live URL" success criterion: create the public GitHub repository `pjnhek/portfolio` (D-01, DEP-01), push the local repo to it, link Vercel to the repo with preview deploys on every PR (D-02, DEP-02), enable branch protection on `main`, and prove the pipeline works by opening a smoke-test PR (a trivial README edit) and verifying the Vercel-emitted preview URL returns 200 with "James Nhek" in its `<title>`. This is the final slice that flips Phase 1's status from "James can see localhost" to "anyone with the preview URL can see the shell."

Purpose: Close the last gap between "buildable locally" and "deployed slice that recruiters could be shown if needed." Every subsequent phase (2 / 3 / 4) inherits this pipeline — Phase 2 ships real content into the same Vercel preview, Phase 3 adds metadata factory + Lighthouse + axe runs against the same preview URL, Phase 4 cuts the `pjnhek.com` domain over to this same Vercel project. Locking the pipeline now means later phases only write content.

Output: A `https://github.com/pjnhek/portfolio` public repo with `main` protected, a Vercel project linked to it with preview deploys enabled, a verified-working preview URL from a smoke-test PR, plus two committed files in the repo (a recruiter-facing `README.md` and a minimal `.github/pull_request_template.md`).

User-visible delta: From "localhost:3000 only" → "anyone with the Vercel preview URL can open it on a phone and see the typographically-correct Phase 1 shell at 375px with the tab title 'James Nhek — AI Engineer.'"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-slice/01-CONTEXT.md
@.planning/phases/01-foundation-slice/01-RESEARCH.md
@.planning/phases/01-foundation-slice/SKELETON.md
@.planning/phases/01-foundation-slice/01-01-PLAN.md
@.planning/phases/01-foundation-slice/01-02-PLAN.md
@.planning/phases/01-foundation-slice/01-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- No new code interfaces. This plan is repo + Vercel infrastructure plus two small files. -->

### Locked decisions

- **D-01 (CONTEXT.md):** GitHub repo is `pjnhek/portfolio`, **public**. Recruiter "View Source" lands in the repo — the source is part of the portfolio.
- **D-02 (CONTEXT.md):** Vercel project linked to `pjnhek/portfolio` with preview deploys on every PR. Production deploy stays on a Vercel `*.vercel.app` subdomain — `pjnhek.com` cutover is Phase 4 (DEP-03..06).
- **ROADMAP.md Phase 1 success #2:** "Every pull request to `main` produces a Vercel preview deployment URL whose browser tab title contains 'James Nhek'..."

### Authentication constraints (RESEARCH.md `## Environment Availability` + `## Open Questions` #5)

- GitHub repo creation MUST be performed by the user (`gh repo create` or web UI) — Claude does not assume an authenticated `gh` CLI or a stored PAT.
- Vercel project linking MUST be performed by the user via the Vercel web UI (no Vercel CLI dependency).
- Branch protection on `main` MUST be configured by the user in the GitHub repo settings UI — no automation per RESEARCH.md Open Question #5.
- These three steps are blocking-human checkpoints. Each checkpoint must surface enough specifics that the user can complete the step in a single web-UI session without re-reading the plan.

### Out-of-scope in this plan

- `pjnhek.com` custom domain configuration in Vercel (DEP-03..06 → Phase 4).
- Vercel usage email alerts (DEP-08 → Phase 4).
- UptimeRobot uptime ping (DEP-09 → Phase 4).
- GitHub profile curation, pinned-repo selection (DEP-10 → Phase 4).
- Legacy `pnhek.github.io` redirect (DEP-07 → Phase 4).
- Husky / pre-commit hooks (CONTEXT.md `<decisions>` Discretion default: skip).
- GitHub Actions CI workflow (not required — Vercel preview is the enforcement boundary in Phase 1).
- Production env var `NEXT_PUBLIC_SITE_URL=https://pjnhek.com` (DEP-03 → Phase 4; until then Plan 02's `.default()` keeps the build green).

### Forbidden

- Creating the repo under any name other than `pjnhek/portfolio` (D-01).
- Making the repo private (D-01: public).
- Skipping branch protection (DEP-01 acceptance: "main branch protected").
- Configuring `pjnhek.com` in Vercel in this phase (Phase 4 owns DEP-03..06).
- Setting `NEXT_PUBLIC_SITE_URL` in Vercel production environment (Phase 4).
- Force-pushing to `main` after the initial push (rebase pattern not relevant here; standard PR workflow only).
- Squashing the planning history (`.planning/`) — every file under `.planning/` MUST be pushed as-is.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Commit a recruiter-facing README + minimal PR template, prepare repo for first push</name>
  <read_first>
    - /Users/pnhek/usf msds/github/portfolio/README.md (Plan 01's one-paragraph stub — this task expands it)
    - /Users/pnhek/usf msds/github/portfolio/.planning/PROJECT.md (core value, audience, constraints — informs README phrasing)
    - /Users/pnhek/usf msds/github/portfolio/CLAUDE.md (`## Project` section — high-level project framing for the README)
    - /Users/pnhek/usf msds/github/portfolio/.planning/STATE.md (`## Performance Metrics` — informs README "Status" block)
  </read_first>
  <files>README.md, .github/pull_request_template.md</files>
  <action>
    Replace `README.md` with a short recruiter-facing page (target ≤ 60 lines markdown) that:
    1. Title: `# pjnhek.com — James Nhek Portfolio`.
    2. One-paragraph framing: "Custom portfolio for James Nhek, AI Engineer at Asurion. Built as part of the portfolio itself — recruiters who click 'View Source' should see real engineering, not a template." Reference the locked stack inline ("Next.js 16 App Router · React 19.2 · Tailwind v4 (CSS-first `@theme`) · TypeScript strict · Geist via `next/font/google` · pnpm on Node 22 LTS · deployed on Vercel Hobby").
    3. A `## Run locally` section with `pnpm install`, `pnpm dev`, `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm format:check`. State the local URL is http://localhost:3000.
    4. A `## Status` section that says: "Phase 1 — Foundation Slice (shell only; real content lands in Phase 2). See `.planning/ROADMAP.md` for the full plan." DO NOT include screenshots (Phase 1 has none).
    5. A `## Where things live` section with a 5-bullet path map: `src/app/` (routes), `src/components/primitives/` (5 hand-rolled UI primitives), `src/lib/env.ts` (zod-validated env), `public/diagrams/` (placeholder SVG; real diagrams in Phase 2), `.planning/` (project brief, requirements, phase docs).
    6. A `## Project brief` section with a single sentence "Full audience + constraints + out-of-scope hard contract: see `.planning/PROJECT.md`."
    7. DO NOT include a "License" section (this is a personal portfolio, not OSS — leaving the field absent is intentional).
    8. DO NOT include badges (Vercel deploy / build status). They tend to drift; the live preview URL is the load-bearing signal.
    9. DO NOT mention `pjnhek.com` as a live URL anywhere — until Phase 4 the production URL is a `*.vercel.app` subdomain. Use the literal phrase "preview URL" only.

    Create `.github/pull_request_template.md` with a minimal checklist (≤ 20 lines markdown):
    1. "## Summary" — one line: "What does this PR change?"
    2. "## Verification" with checkboxes:
       - `[ ] pnpm lint exits 0`
       - `[ ] pnpm typecheck exits 0`
       - `[ ] pnpm build exits 0`
       - `[ ] Vercel preview URL renders the change correctly (tab title still contains "James Nhek")`
       - `[ ] No new tailwind.config.* file added`
       - `[ ] No new "use client" added (Phase 1) — or explicitly justified (Phase 2+)`
       - `[ ] No new package without a lockfile diff visible above`
    3. "## Scope" — one line: "Phase this PR belongs to (1/2/3/4)? Any out-of-scope-by-roadmap items snuck in?"

    Run `pnpm format && pnpm lint && pnpm typecheck && pnpm build` after writing both files (Prettier should be fine with markdown defaults; if `pnpm format:check` flags either file, accept the Prettier rewrite).

    Initialize git if it is not already initialized in this repo root (Plan 01's scaffold should have done this — verify with `git status`; if `fatal: not a git repository`, run `git init -b main`). Stage all files including `.planning/` (planning history is part of the portfolio per D-01).

    Do NOT commit yet — the user needs to confirm the repo URL before the first push lands. The next task is the gate.
  </action>
  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/portfolio" &amp;&amp; test -f README.md &amp;&amp; test -f .github/pull_request_template.md &amp;&amp; grep -q "pjnhek.com" README.md &amp;&amp; grep -q "James Nhek" README.md &amp;&amp; grep -q "Next.js 16" README.md &amp;&amp; grep -q "Tailwind v4" README.md &amp;&amp; grep -q "pnpm" README.md &amp;&amp; grep -q "Vercel" README.md &amp;&amp; ! grep -Ei "license" README.md &amp;&amp; grep -q "pnpm lint exits 0" .github/pull_request_template.md &amp;&amp; grep -q "James Nhek" .github/pull_request_template.md &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build &amp;&amp; git status &amp;&amp; git diff --cached --quiet 2&gt;/dev/null; echo "staged-or-not OK"</automated>
  </verify>
  <done>
    `README.md` is a short recruiter-facing landing with the locked stack, local-run commands, status, and path map. `.github/pull_request_template.md` is a minimal PR checklist. Both files are staged in git but not yet committed. `pnpm lint && pnpm typecheck && pnpm build` all exit 0 after the writes.
  </done>
  <acceptance_criteria>
    - `test -f README.md && test -f .github/pull_request_template.md` succeeds.
    - `README.md` contains "Next.js 16", "Tailwind v4", "pnpm", and "Vercel".
    - `README.md` does NOT contain any "License" section (grep -Ei "license" returns nothing).
    - `README.md` does NOT mention `pjnhek.com` as a currently-live URL (only as the future domain in Phase 4 context — wording check is manual but `! grep -E "https://pjnhek\.com" README.md` should hold since Phase 4 owns that mention).
    - `.github/pull_request_template.md` contains the literal phrase `pnpm lint exits 0` and `James Nhek`.
    - `pnpm lint && pnpm typecheck && pnpm build` all exit 0.
    - `git status` reports the repo is initialized (no `fatal: not a git repository`).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Create GitHub repo `pjnhek/portfolio`, push, enable branch protection (DEP-01)</name>
  <what-built>
    The local repo is committed-ready (Task 1). This checkpoint requires the user to perform three GitHub web-UI actions that require auth Claude does not have: (a) create the public repo `pjnhek/portfolio`, (b) push the local code to it, and (c) enable branch protection on `main`. RESEARCH.md `## Environment Availability` and Open Question #5 lock these as human-only steps.
  </what-built>
  <how-to-verify>
    Run the following from `/Users/pnhek/usf msds/github/portfolio/` (Claude has done the pre-work — README, PR template, all `.planning/` content is staged).

    **Step A — Initial commit** (run before steps B/C):
    1. From the project root: `git add .` (this stages `.planning/`, `src/`, `public/`, `package.json`, `pnpm-lock.yaml`, all config files, `README.md`, `.github/pull_request_template.md`, etc.).
    2. Verify the user-email-config: `git config user.email "nhekvirakyuth@gmail.com"` and `git config user.name "James Nhek"` (or your preferred display name).
    3. `git commit -m "feat(01-foundation): scaffold + design tokens + 5 primitives + zod env + Vercel-ready shell"`.

    **Step B — Create the repo on GitHub**:
    1. Open https://github.com/new
    2. **Repository name:** `portfolio` (the org/user prefix `pjnhek` is determined by which account you're signed into — confirm the form shows the new repo will live at `pjnhek/portfolio`)
    3. **Description (optional):** "Custom portfolio for James Nhek — AI Engineer at Asurion. Next.js 16 + Tailwind v4 + TypeScript."
    4. **Visibility:** Public (locked by D-01).
    5. **Do NOT** initialize with README / .gitignore / license — the local repo already has them.
    6. Click **Create repository**.
    7. On the post-create screen, copy the exact remote URL shown (HTTPS or SSH — your preference).

    **Step C — Push the local repo**:
    1. From `/Users/pnhek/usf msds/github/portfolio/`:
       `git remote add origin <URL from Step B.7>`
       `git branch -M main`
       `git push -u origin main`
    2. Refresh https://github.com/pjnhek/portfolio — confirm the file tree matches the local repo, including `.planning/` and the SKELETON.md / 01-*-PLAN.md files.

    **Step D — Enable branch protection on `main`** (DEP-01 acceptance: "main branch protected"):
    1. Open https://github.com/pjnhek/portfolio/settings/branches
    2. **Branch name pattern:** `main`
    3. Enable: **Require a pull request before merging** (uncheck "Require approvals" since this is a solo repo — the requirement is the PR workflow itself, not human review by others).
    4. Enable: **Require status checks to pass before merging**. Initially the list of available status checks is empty; that is fine for now — Vercel adds itself after the project is linked in Task 3. You will return here briefly in Task 4 to tick the Vercel check.
    5. Save.

    **Step E — Confirm push succeeded** by running locally:
    `git remote -v` (should show `origin  <URL>  (fetch)` + `(push)`)
    `git log --oneline -1` (should show your first commit hash + message)

    Type "approved" + paste the repo URL so the executor can record it in the SUMMARY. If anything fails (wrong owner, push rejected, branch protection rule warning), describe the error verbatim so the executor can troubleshoot before Task 3.
  </how-to-verify>
  <resume-signal>Type "approved" and paste the GitHub repo URL (e.g. `https://github.com/pjnhek/portfolio`). If branch protection couldn't be enabled exactly as described (e.g. requires GitHub Pro for private repos — not applicable since this is public), describe what was actually configured.</resume-signal>
  <acceptance_criteria>
    - The repo `https://github.com/pjnhek/portfolio` exists, is **public**, and the file tree matches the local working directory (including `.planning/`, `SKELETON.md`, all `01-*-PLAN.md` files, and the Phase 1 source).
    - `main` is the default branch.
    - Branch protection on `main` is enabled with "Require a pull request before merging" turned on. (The Vercel status-check requirement is added in Task 4 once Vercel is linked.)
    - The local repo's `origin` points to the GitHub URL.
    - At least one commit has been pushed.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Link Vercel to `pjnhek/portfolio`; enable preview deploys on all branches (DEP-02)</name>
  <what-built>
    GitHub repo + branch protection from Task 2 are now live. This checkpoint links Vercel to the repo so every PR produces a preview URL. The Vercel web UI requires user auth — Claude cannot do this without a Vercel token, and RESEARCH.md `## Environment Availability` flags this as a human step.
  </what-built>
  <how-to-verify>
    **Step A — Create the Vercel project**:
    1. Open https://vercel.com/new
    2. Sign in if needed (recommended: with your GitHub account so the GitHub App permissions are seamless).
    3. Under **Import Git Repository**, find `pjnhek/portfolio`. If it isn't listed, click **Adjust GitHub App Permissions** and grant access to the `pjnhek/portfolio` repo (or to all your repos).
    4. Click **Import** on `pjnhek/portfolio`.
    5. On the **Configure Project** screen:
       - **Project Name:** `pjnhek-portfolio` (or accept the default — the project name is for your dashboard only, not the URL).
       - **Framework Preset:** Verify it auto-detected as **Next.js**. If not, manually select Next.js.
       - **Root Directory:** `.` (leave as default).
       - **Build & Output Settings:** Verify `Install Command` is `pnpm install`, `Build Command` is `pnpm build` (or `next build`), `Output Directory` is `.next` (default for App Router). If the install command shows `npm install`, change it to `pnpm install` — the `packageManager` field in `package.json` should make Vercel auto-detect pnpm, but verify.
       - **Node.js Version:** Open the "..." or expand the Node section and confirm **22.x** is selected. If 20.x is selected, change to 22.x (FOUND-13 + RESEARCH.md Pitfall 4).
       - **Environment Variables:** Do NOT add `NEXT_PUBLIC_SITE_URL` for production yet — Phase 4 owns DEP-03. Plan 02's `.default("https://pjnhek.com")` in `src/lib/env.ts` keeps Phase 1 builds green without it.
    6. Click **Deploy**. Wait for the initial production deploy of `main` to complete.
    7. After deploy, note the production URL Vercel emits (e.g. `https://pjnhek-portfolio.vercel.app` or `https://pjnhek-portfolio-<hash>.vercel.app`). Open it in a browser and confirm the Plan 02 shell renders with the Geist + monochrome styling and "James Nhek" in the tab.

    **Step B — Verify preview-on-PR is enabled**:
    1. In the Vercel project dashboard, go to **Settings → Git**.
    2. Verify **Production Branch:** `main`.
    3. Verify **Preview Branches:** set to **All branches** (default). If it shows something else, change to All branches.
    4. Save.

    **Step C — Verify the Vercel GitHub App emits PR comments**:
    1. Vercel installs a GitHub App on the repo as part of the import. This App posts a comment on each PR with the preview URL. No further config is needed unless the App was rejected.
    2. If you want to confirm now, https://github.com/pjnhek/portfolio/settings/installations should show the Vercel app with permissions granted.

    Type "approved" + paste the Vercel production URL (the `*.vercel.app` URL from Step A.7) so the executor can record it in the SUMMARY. If the production deploy failed, paste the Vercel build-log error verbatim.
  </how-to-verify>
  <resume-signal>Type "approved" + paste the Vercel production URL (e.g. `https://pjnhek-portfolio.vercel.app`). If the import failed (wrong framework detection, build error, install command mismatch), paste the build log error.</resume-signal>
  <acceptance_criteria>
    - A Vercel project is linked to the GitHub repo `pjnhek/portfolio`.
    - The initial production deploy from `main` succeeded — a `*.vercel.app` URL is live.
    - Visiting that URL returns 200, renders the Plan 02 shell (Geist Sans, monochrome, 5 numbered sections, placeholder diagram, LangGraph chip, github.com/pjnhek link), and the response `<title>` contains "James Nhek".
    - Vercel project Settings → Git shows Production Branch `main` and Preview Branches "All branches".
    - Node version is **22.x** in the project settings (FOUND-13 / RESEARCH.md Pitfall 4).
    - No `NEXT_PUBLIC_SITE_URL` production env var was added yet (Phase 4 owns DEP-03).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Smoke-test the preview pipeline — open a trivial PR, verify preview URL emits, then update branch protection to require the Vercel status check</name>
  <read_first>
    - /Users/pnhek/usf msds/github/portfolio/README.md (the file we will tweak to trigger a PR — pick a harmless edit like clarifying a sentence or fixing a typo)
    - /Users/pnhek/usf msds/github/portfolio/.planning/phases/01-foundation-slice/01-RESEARCH.md (`## Common Pitfalls` Pitfall 8 — "Vercel preview deploys not firing on PRs"; the smoke-test PR is the verification ritual described there)
  </read_first>
  <files>(none persistently — the smoke-test edit is reverted before the PR closes, OR kept as a minor README polish if desired; no production code is changed)</files>
  <action>
    Open a smoke-test PR on `pjnhek/portfolio` to verify the Vercel preview-on-PR pipeline works end-to-end (RESEARCH.md Pitfall 8 mitigation). The executor here can drive this via `gh` CLI if it is installed and authenticated; otherwise fall back to a `checkpoint:human-verify` (instructions below).

    **Path A — `gh` CLI is available and authenticated:**
    1. Check `gh auth status` (exit 0 means authenticated).
    2. `git checkout -b chore/01-smoke-test-preview`
    3. Make a trivial edit to `README.md` — e.g., append a single space at the end of an existing line, or add a one-line clarifying comment in the `## Status` block (the edit content is irrelevant; we only need a diff). Save the file.
    4. `git add README.md`
    5. `git commit -m "chore(01): smoke-test Vercel preview pipeline"`
    6. `git push -u origin chore/01-smoke-test-preview`
    7. `gh pr create --base main --head chore/01-smoke-test-preview --title "chore(01): smoke-test Vercel preview pipeline" --body "Verifies Vercel preview-on-PR (DEP-02). Will be closed once the preview URL is captured in 01-03-SUMMARY."`
    8. Wait up to ~3 minutes for Vercel to comment on the PR with the preview URL (`gh pr view --comments` or just refresh the PR in browser).
    9. Capture the preview URL from the Vercel comment.
    10. Run `curl -s -o /tmp/preview-home.html -w "%{http_code}" <preview-url>` — expect HTTP 200. Run `grep -o '<title[^>]*>[^<]*</title>' /tmp/preview-home.html` — confirm the string contains "James Nhek".
    11. Open the preview URL in a browser, DevTools at 375 / 768 / 1280 viewports — confirm no horizontal scroll and Geist Sans rendering.

    **Path B — `gh` CLI is NOT available — convert to a human checkpoint inline:**
    1. Surface the smoke-test as a single-step human-verify checkpoint with the same intent and steps as Path A but executed via the GitHub web UI: create the branch locally, push it, open a PR through https://github.com/pjnhek/portfolio/pulls, wait for the Vercel comment, paste the preview URL.

    **Step 5 — Update branch protection to require the Vercel status check** (deferred from Task 2 because the Vercel check name only appears in GitHub after the first Vercel deploy completes):
    1. Open https://github.com/pjnhek/portfolio/settings/branches
    2. Edit the existing rule on `main`.
    3. Under "Require status checks to pass before merging", search for the Vercel check (commonly named `Vercel` or `Vercel – Preview` — it appears as soon as Vercel has run at least one deploy that GitHub Apps-attaches to a PR; the smoke-test PR ensured this).
    4. Tick the Vercel check as a required status check.
    5. Save.

    **Step 6 — Close the smoke-test PR**:
    1. Decide whether to merge the trivial README change or close-without-merging. Either is acceptable — the README edit was intentionally innocuous. If closing without merging, run `gh pr close <PR#> --delete-branch` (or use the web UI's "Close pull request" + delete-branch). If merging, ensure the merge is via squash + PR description retained; then delete the branch.
    2. Verify `git checkout main && git pull` brings the local repo back in sync.
  </action>
  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/portfolio" &amp;&amp; git branch --show-current | grep -q "^main$" &amp;&amp; git remote get-url origin | grep -q "pjnhek/portfolio" &amp;&amp; (gh pr list --state all --limit 5 2&gt;/dev/null | grep -q "smoke-test" || echo "PR was closed/merged — record the preview URL in 01-03-SUMMARY manually") &amp;&amp; (gh api repos/pjnhek/portfolio/branches/main/protection 2&gt;/dev/null | grep -q "required_status_checks" || echo "branch-protection-API-query-skipped: requires gh auth; verified via web UI")</automated>
  </verify>
  <done>
    A smoke-test PR was opened on `pjnhek/portfolio`. Vercel emitted a preview URL via a PR comment. The preview URL returned HTTP 200 and `<title>` contained "James Nhek" (FOUND-09 / ROADMAP success #2 verified on Vercel infrastructure). The Vercel status check is now ticked as a required check on `main`'s branch-protection rule. The smoke-test branch is closed or merged; local `main` is in sync with `origin/main`.
  </done>
  <acceptance_criteria>
    - The PR was opened, Vercel commented with a preview URL within ~3 minutes, and the preview URL returned HTTP 200 with "James Nhek" in `<title>`.
    - The Vercel status check is configured as a required check on `main` in GitHub branch protection.
    - The smoke-test branch is closed/merged and `git checkout main && git pull` succeeds with no merge conflict.
    - The preview URL is recorded in `01-03-SUMMARY.md` along with a timestamped curl output showing 200 + title.
    - Local repo `git status` is clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → `github.com/pjnhek/portfolio` (public) | Code becomes publicly visible. Any committed secret would leak (V8 Data Protection). Phase 1 has none. |
| `github.com` → Vercel (GitHub App webhook) | Vercel reads code + builds on Vercel infrastructure. Trust boundary mitigated by Vercel's mainstream usage + SOC 2 posture (acceptable for a public marketing site). |
| Vercel preview deploy → public internet | Anyone with the URL can read the preview. Vercel preview URLs are non-guessable hashes (RESEARCH.md `## Security Domain` Information Disclosure pattern). |
| GitHub web UI → branch protection on `main` | If misconfigured (e.g., disabled, or "Allow force pushes" enabled), main-branch tampering becomes possible. Mitigated by acceptance criterion + checkpoint procedure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SEC-LEAK | Information Disclosure | First push to public repo includes a committed secret (e.g. `.env.local`, a stale API key) | mitigate | `.gitignore` from Plan 01 covers `.env*.local` and `.env` patterns. Before Task 2 commit, the executor MUST run `git status` and visually confirm no `.env*`-pattern files are staged. RESEARCH.md `## Security Domain` notes Phase 1 has no secrets to begin with — only `NEXT_PUBLIC_SITE_URL` (intentionally public). Acceptance criterion in Task 1 implicitly enforces clean staging. |
| T-01-VERCEL-PROD-ENV | Tampering | A future PR (or Phase 4) sets `NEXT_PUBLIC_SITE_URL=https://pjnhek.com` in **preview** instead of **production** in Vercel, leaking the production domain into preview metadata `<meta>` tags | mitigate (defense-in-depth) | Phase 4 explicitly owns DEP-03. Task 3's acceptance criterion forbids setting the var in this plan. Phase 4 plans will set production env only; if preview-env is touched it should be a separate value. |
| T-01-BR-PROT | Tampering / Repudiation | `main` could be force-pushed if branch protection is misconfigured (e.g., "Allow force pushes" enabled) | mitigate | Task 2 + Task 4 acceptance criteria enforce that branch protection is enabled and requires PR + status check. Force-push is disabled by default when "Require a pull request before merging" is enabled. |
| T-01-VERCEL-USAGE | Denial of Service | Phase 1 has no rate-limiting on the public preview URL — a malicious crawler could blow through Vercel Hobby bandwidth | accept | RESEARCH.md `## Security Domain` notes: "Vercel preview URLs are non-guessable hashes." For a labeled-skeleton page, the bandwidth-per-request is tiny (~1 MB at most) and Hobby's 100 GB/month tolerates many millions of hits. Phase 4 adds usage alerts at 50/80/100% (DEP-08). |
| T-01-SC | Tampering | Vercel GitHub App permissions — granting access to all repos vs just this one | mitigate | Task 3 Step A.3 recommends granting access to `pjnhek/portfolio` specifically (not "all repositories"). Reduces blast radius if the Vercel App is ever compromised. |
| T-01-SC-VERCEL | Tampering | The Vercel platform itself is the supply-chain edge for the running production site | accept | Vercel is the locked hosting choice per CLAUDE.md / D-02. Mitigation in case of platform-wide compromise (extremely rare) is outside Phase 1's scope. |
</threat_model>

<verification>
## Phase-Level Checks (applicable to this plan)

1. `https://github.com/pjnhek/portfolio` exists, is public, and contains the Phase 1 source + `.planning/`.
2. `main` is protected: PRs required + Vercel status check required (verified in web UI; `gh api repos/pjnhek/portfolio/branches/main/protection` is the CLI verification if available).
3. A Vercel project is linked to the repo with Production Branch = `main`, Preview Branches = All branches, Node 22.x, `pnpm install` / `pnpm build` (or `next build`).
4. A smoke-test PR triggered a Vercel preview comment within ~3 minutes.
5. The preview URL responds with HTTP 200 and `<title>` contains "James Nhek".
6. No `NEXT_PUBLIC_SITE_URL` env var has been added to Vercel production yet (Phase 4 owns DEP-03 — verify the Vercel project's Environment Variables page is empty or contains only non-Phase-4 vars).
7. Local `main` and `origin/main` are in sync (`git status` clean after `git pull`).
</verification>

<success_criteria>
This plan is complete when:
- The public repository `https://github.com/pjnhek/portfolio` exists with `main` as the default branch.
- Branch protection on `main` is enabled with "Require pull request before merging" + the Vercel status check as a required check.
- A Vercel project is linked to the repo with preview deploys on all branches and Node 22.x.
- A smoke-test PR produced a Vercel preview URL that returned HTTP 200 with "James Nhek" in `<title>`.
- The preview URL is recorded in `01-03-SUMMARY.md` so subsequent phases (and Phase 4's domain cutover) can reference it.
- Local repo's `git status` is clean and `main` matches `origin/main`.
- `pnpm lint && pnpm typecheck && pnpm build` continue to exit 0 locally (no regression introduced).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-slice/01-03-SUMMARY.md` when done — record:
1. Exact GitHub repo URL.
2. Exact Vercel production URL (`*.vercel.app`).
3. Exact smoke-test preview URL captured from the Vercel PR comment.
4. Curl + grep output proving `<title>` contains "James Nhek" on the preview URL.
5. Node version Vercel is running (22.x).
6. The branch protection settings actually configured on `main` (PR required: yes/no; required status checks: list).
7. Confirmation that no `NEXT_PUBLIC_SITE_URL` env var was added to Vercel production (Phase 4 will own DEP-03).
8. The first commit hash (`git log -1 --format="%H %s"`) so Phase 2 can reference Phase 1's start point.
</output>
