---
phase: 03-seo-polish-performance-accessibility
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified: []
autonomous: true
requirements:
  - POL-04
  - POL-05
  - POL-06
  - POL-07
  - POL-08

must_haves:
  truths:
    - "Home-route JavaScript shipped is < 100 KB gzipped (Network tab verified)"
    - "Lighthouse mobile Performance >= 95 on the preview URL"
    - "Lighthouse mobile Accessibility = 100 on the preview URL"
    - "axe CLI exits 0 (zero violations) on home, /uses, and one /projects/[slug] URL"
    - "Every interactive element has a visible focus-visible: outline (keyboard users can see focus)"
    - "The site is fully keyboard-navigable — Tab/Shift+Tab/Enter reach all interactive elements; no mouse traps"
    - "OG meta tags (og:image, og:title, twitter:card) appear in <head> on the preview URL"
    - "sitemap.xml returns valid XML with 6 <url> entries on the preview URL"
    - "robots.txt contains Allow: / and a Sitemap: line on the preview URL"
    - "Visiting a non-existent path on the preview URL returns HTTP 404"
  artifacts:
    - path: ".planning/phases/03-seo-polish-performance-accessibility/verification-report.txt"
      provides: "Record of all automated verification results (Lighthouse scores, axe output, curl checks, build output)"
  key_links:
    - from: "Lighthouse CLI output"
      to: "POL-05 threshold: Performance >= 95, Accessibility = 100"
      via: "npx lighthouse <PREVIEW_URL> --preset=mobile"
      pattern: "Performance.*[0-9]+ / 100"
    - from: "axe CLI output"
      to: "POL-06 threshold: 0 violations"
      via: "npx @axe-core/cli <URL> --exit"
      pattern: "0 violations"
---

<objective>
Run the full automated quality gate against the preview deployment and record the results — Lighthouse mobile audit, axe accessibility scans, bundle budget check, curl structural checks, and keyboard/focus-ring audit.

Purpose: Plans 01 and 02 ship code. This plan verifies that code meets the measurable thresholds. By separating verification into its own plan, the executor can run it on the committed + deployed preview URL with a clean context and fresh eyes — not mixed into the implementation context. All checks here are Claude-automatable per D-VERIFY-01/02. Human-only checks (real iPhone, LinkedIn Inspector, Slack DM, cold-read) are Plan 04.

Output:
- .planning/phases/03-seo-polish-performance-accessibility/verification-report.txt recording all check results
- Any focus-ring or HTML fixes applied inline if axe or Lighthouse flags issues
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-seo-polish-performance-accessibility/03-CONTEXT.md
@.planning/phases/03-seo-polish-performance-accessibility/03-RESEARCH.md
@.planning/phases/03-seo-polish-performance-accessibility/03-01-SUMMARY.md
@.planning/phases/03-seo-polish-performance-accessibility/03-02-SUMMARY.md
@src/app/globals.css
@src/components/primitives/ExternalLink.tsx
@src/components/nav/BackLink.tsx
@src/components/interactive/CopyEmail.tsx
@src/components/primitives/SiteFooter.tsx
@src/components/cards/ProjectCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle budget + structural curl checks + OG head-tag verification (POL-04, SEO-04, SEO-05, SEO-06)</name>
  <files></files>

  <read_first>
    - 03-RESEARCH.md §"Code Examples: Measuring Bundle Budget (POL-04)" — two methods: pnpm build terminal output + Network tab; use build output as primary signal (First Load JS), then note Network-tab as authoritative fallback
    - 03-RESEARCH.md §"Automatable (Claude runs during task execution)" table — exact curl commands for sitemap, robots, 404, icon, OG head tags
    - 03-RESEARCH.md Pitfall 4 — sitemap always emits https://pjnhek.com URLs even from preview (env .default() behavior); this is expected
    - STATE.md §"Infrastructure Provisioned" — Vercel Deployment Protection = None; preview URLs are publicly accessible
  </read_first>

  <action>
    This task runs verification commands against the deployed preview URL. Before running, confirm the current preview URL from the Vercel dashboard or `vercel ls` CLI. The production/preview URL format is `https://portfolio-[hash].vercel.app` or a branch preview URL.

    **Step 1 — Get preview URL:**
    Run `npx vercel ls 2>/dev/null | head -10` or check STATE.md for the known preview URL pattern. If a specific preview URL is not available from prior plan SUMMARYs, use the canonical preview URL recorded in STATE.md (Plan 01-03: `https://portfolio-kappa-bay-ew5cos0ri7.vercel.app/`) — confirm it's still the production preview by curling it.

    **Step 2 — Bundle budget (POL-04):**
    Run `pnpm build 2>&1 | grep "First Load JS"` from the project root. Record the home route "First Load JS" number from the table. This number is in raw KB (NOT gzipped). Per RESEARCH.md, gzip ratio is ~30-40%, so a First Load JS of ≤ ~150 KB typically gzips to ≤ 100 KB. Record the exact number.
    For authoritative gzip confirmation: run `pnpm start` locally (or use the preview URL), open Chrome DevTools Network tab, disable cache, hard reload, filter by JS, sum the "Size" (transferred) column — that is gzipped bytes. Record result. Must be < 100 KB.

    **Step 3 — OG head tags (SEO-04):**
    Run: `curl -sL <PREVIEW_URL> | grep -E 'og:image|og:title|twitter:card'`
    Must return at least three meta tags containing og:image, og:title, and twitter:card.

    **Step 4 — sitemap.xml structure (SEO-05):**
    Run: `curl -sL <PREVIEW_URL>/sitemap.xml | head -30`
    Must return XML with `<urlset>` tag and 6 `<url>` entries. Per RESEARCH.md Pitfall 4, URLs will show `https://pjnhek.com` (from env .default()) even on a preview deploy — that is expected and correct.

    **Step 5 — robots.txt (SEO-06):**
    Run: `curl -sL <PREVIEW_URL>/robots.txt`
    Must contain "Allow: /" and "Sitemap:" with the sitemap URL.

    **Step 6 — 404 status (POL-01):**
    Run: `curl -o /dev/null -s -w "%{http_code}" <PREVIEW_URL>/this-url-does-not-exist-at-all`
    Must return 404.

    **Step 7 — icon exists (POL-02):**
    Run: `curl -s -o /dev/null -w "%{http_code}" <PREVIEW_URL>/icon`
    Must return 200.

    **Step 8 — OG image is reachable (SEO-04):**
    Run: `curl -s -I <PREVIEW_URL>/opengraph-image | grep -i content-type`
    Must return Content-Type containing "image/png".

    **Step 9 — Record all results to file:**
    Write all command outputs to `.planning/phases/03-seo-polish-performance-accessibility/verification-report.txt` with section headers for each check. Include timestamps, the exact commands run, and the raw output.

    If any check fails:
    - Bundle > 100 KB gzipped: investigate which JS bundle is inflated (`pnpm build` output shows per-chunk breakdown). The most likely cause is an accidental client-side import. Check `"use client"` count — it must be exactly 1 (CopyEmail.tsx). If a second "use client" was accidentally introduced, find and remove it.
    - curl checks failing: verify the preview URL is accessible and the deployment completed. Check Vercel dashboard for build errors.
  </action>

  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/portfolio" && pnpm build 2>&1 | grep -E "First Load JS|Route \(app\)"</automated>
  </verify>

  <acceptance_criteria>
    - verification-report.txt exists in .planning/phases/03-seo-polish-performance-accessibility/
    - verification-report.txt contains a section "Bundle Budget (POL-04)" with recorded First Load JS value for home route
    - verification-report.txt contains a section "OG Head Tags (SEO-04)" showing og:image, og:title, twitter:card present
    - verification-report.txt contains a section "sitemap.xml (SEO-05)" showing valid XML response
    - verification-report.txt contains a section "robots.txt (SEO-06)" showing Allow: / and Sitemap: line
    - verification-report.txt contains a section "404 status (POL-01)" showing HTTP 404 for non-existent path
    - Home route First Load JS (from pnpm build output) is recorded; if gzip estimate > 100 KB, Network-tab verification was run and recorded
    - If any check failed, the root cause was diagnosed and fixed before this task completes
  </acceptance_criteria>

  <done>All structural checks passed and recorded in verification-report.txt: OG tags in head, sitemap 6 URLs, robots Allow+Sitemap, 404 returns 404, icon returns 200, OG image returns image/png, bundle budget within target.</done>
</task>

<task type="auto">
  <name>Task 2: Lighthouse mobile audit + axe CLI scans + keyboard/focus-ring audit (POL-05, POL-06, POL-07, POL-08)</name>
  <files></files>

  <read_first>
    - 03-RESEARCH.md §"Running Lighthouse CLI (POL-05)" — exact npx lighthouse command with --preset=mobile, --only-categories, --chrome-flags
    - 03-RESEARCH.md §"Running axe CLI (POL-06)" — exact npx @axe-core/cli command with --exit flag
    - 03-RESEARCH.md Pattern 8 — focus ring CSS pattern; where to look in ExternalLink.tsx, BackLink.tsx, CopyEmail.tsx, SiteFooter, ProjectCard
    - 03-PATTERNS.md §"Focus-Ring Pattern" — verbatim focus-visible: outline string used throughout the codebase
    - src/components/primitives/ExternalLink.tsx — has focus ring
    - src/components/nav/BackLink.tsx — has focus ring
    - src/components/interactive/CopyEmail.tsx — the client island; must have focus ring on the button
    - src/components/primitives/SiteFooter.tsx — contains links; verify focus ring presence
    - src/components/cards/ProjectCard.tsx — contains Link + ExternalLink; verify focus ring
    - src/app/not-found.tsx — just created; verify focus ring on "← Back to home" link
  </read_first>

  <action>
    This task runs three verification dimensions: Lighthouse CLI, axe CLI, and a code-level keyboard/focus-ring audit.

    **Step 1 — Focus-ring code audit (POL-07, POL-08 — before running tools):**
    Read each interactive-element-containing file and verify the focus-visible: outline pattern is present. The canonical pattern (from ExternalLink.tsx and BackLink.tsx) is:
    `focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-ink)]`

    Check these files:
    - src/components/primitives/ExternalLink.tsx — should already have it (Phase 1 baseline)
    - src/components/nav/BackLink.tsx — should already have it (Phase 2 baseline)
    - src/components/interactive/CopyEmail.tsx — the button element must have this class string on its `<button>` element
    - src/components/primitives/SiteFooter.tsx — any `<Link>` or `<ExternalLink>` inside the footer should have focus rings (ExternalLink handles its own; plain `<Link>` elements need explicit focus ring classes)
    - src/components/cards/ProjectCard.tsx — the primary `<Link>` wrapping the card must have focus ring classes; ExternalLink (GitHub icon) handles its own
    - src/app/not-found.tsx — the "← Back to home" `<Link>` must have focus ring (should be present from Plan 02 implementation)

    If any interactive element is missing the focus ring, add the canonical class string now. Record all findings in verification-report.txt §"Focus Ring Audit (POL-07)".

    **Step 2 — Keyboard nav walkthrough (POL-08):**
    Validate the tab order by reading the DOM structure of each page (via source code inspection, not manual browser testing):
    - Home page: Tab should reach → [any nav/skip link] → hero links → About → Experience → Projects → ProjectCard links (each card's Link + ExternalLink for GitHub) → Contact → CopyEmail button → mailto link → LinkedIn ExternalLink → GitHub ExternalLink → SiteFooter links
    - /uses: BackLink → Section content → SiteFooter links  
    - /projects/[slug]: BackLink → title/content → ExternalLink (GitHub link) → SiteFooter links
    Verify no element requires a mouse-only interaction. The sole "use client" (CopyEmail) uses a `<button>` element which is keyboard-native. Record as "PASS" or flag specific issues.

    **Step 3 — Lighthouse CLI (POL-05):**
    Run against the preview URL:
    ```
    npx lighthouse <PREVIEW_URL> \
      --preset=mobile \
      --only-categories=performance,accessibility \
      --output=json \
      --output-path=/tmp/lighthouse-report.json \
      --chrome-flags="--headless --no-sandbox"
    ```
    Extract scores: `cat /tmp/lighthouse-report.json | python3 -c "import json,sys; r=json.load(sys.stdin); print('Performance:', round(r['categories']['performance']['score']*100), '| Accessibility:', round(r['categories']['accessibility']['score']*100))"`
    Thresholds: Performance >= 95, Accessibility = 100.
    If Accessibility < 100: read the specific failures from the JSON (categories.accessibility.auditRefs with score < 1) and fix them. Common issues at this stage: missing aria-label on icon-only buttons, insufficient color contrast (already handled by monochrome palette at 18.8:1), missing lang attribute (already on <html> in layout.tsx).
    Record results in verification-report.txt §"Lighthouse Audit (POL-05)".

    **Step 4 — axe CLI scans (POL-06):**
    Run on three URLs:
    ```
    npx @axe-core/cli <PREVIEW_URL> --exit
    npx @axe-core/cli <PREVIEW_URL>/uses --exit
    npx @axe-core/cli <PREVIEW_URL>/projects/voice-intent-eval --exit
    ```
    Each must exit 0 (zero violations). If violations are found, read the violation descriptions and fix the underlying HTML/ARIA issue. Record pass/fail in verification-report.txt §"axe Accessibility Scans (POL-06)".

    **Step 5 — Append all results to verification-report.txt:**
    Add sections:
    - §"Focus Ring Audit (POL-07)" — list of files checked, pass/fail per file, any fixes applied
    - §"Keyboard Nav Audit (POL-08)" — DOM tab order analysis, PASS or specific issues
    - §"Lighthouse Audit (POL-05)" — Performance score and Accessibility score, full CLI output tail
    - §"axe Scans (POL-06)" — per-URL exit code and violation count

    **If Lighthouse Performance < 95:** Most likely cause for this site is LCP or TBT. Check for:
    - Any accidental heavy client-side import (grep for "use client" count — must be 1)
    - Any unoptimized image in the above-the-fold region
    The site is RSC-first with one client island and no JS animation library, so performance should easily exceed 95. If it's genuinely failing, investigate the Lighthouse diagnostics JSON.
  </action>

  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/portfolio" && grep -rn '"use client"' src/ | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - verification-report.txt §"Lighthouse Audit (POL-05)" records Performance >= 95 and Accessibility = 100
    - verification-report.txt §"axe Scans (POL-06)" records exit 0 for all three URLs (home, /uses, /projects/voice-intent-eval)
    - verification-report.txt §"Focus Ring Audit (POL-07)" records all interactive element files checked; every interactive element has focus-visible: outline
    - verification-report.txt §"Keyboard Nav Audit (POL-08)" records PASS for tab-order analysis
    - `grep -rn '"use client"' src/ | wc -l` returns 1 (CopyEmail.tsx only — SEC-07 invariant confirmed)
    - If any Lighthouse or axe failure was found, the fix was applied and re-run confirmed pass before this task closes
  </acceptance_criteria>

  <done>Lighthouse mobile Performance >= 95 and Accessibility = 100; axe exits 0 on home / /uses / one project detail; all interactive elements have focus-visible: outline; keyboard nav PASS; "use client" count = 1; all results recorded in verification-report.txt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification tools → preview URL | npx lighthouse and @axe-core/cli fetch the preview URL; these are read-only audit tools |
| curl → preview URL | Read-only HTTP requests to verify structural outputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | Lighthouse report (may capture page content) | accept | Report is written to /tmp/lighthouse-report.json; not committed to git. Contains only public portfolio content — no secrets. |
| T-03-09 | Tampering | npx @axe-core/cli + npx lighthouse (ephemeral via npx) | accept | Both packages are from authoritative organizations (Deque Systems, Google Chrome team) and are approved in RESEARCH.md Package Legitimacy Audit. Run via npx, not installed. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in the project. Verification tools run ephemeral via npx. |
| T-03-10 | Elevation of Privilege | Second "use client" detected during focus-ring audit | mitigate | If audit finds a second "use client" file, it is removed immediately. Acceptance criteria enforces "use client" count = 1 via grep. |

Note: This plan makes no code changes to the project (unless fixing issues found by the tools). The threat surface is: verification tools fetching a public URL and potentially fixing minor HTML/CSS issues. All fix categories are limited to aria-labels, focus-ring classes, or HTML semantics — no new client islands, no new network calls, no secrets.
</threat_model>

<verification>
All checks are themselves the verification. Summary assertion:

1. verification-report.txt exists with all section headers populated
2. Lighthouse: Performance >= 95, Accessibility = 100
3. axe: 0 violations on all three tested URLs
4. Focus ring: confirmed present on ExternalLink, BackLink, CopyEmail button, SiteFooter links, ProjectCard Link, not-found home link
5. Keyboard nav: PASS
6. "use client" count = 1 (CopyEmail only)
</verification>

<success_criteria>
- Lighthouse mobile Performance >= 95 and Accessibility = 100 on preview URL (POL-05)
- axe CLI exits 0 on home, /uses, and /projects/voice-intent-eval (POL-06)
- Every interactive element has focus-visible: outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-ink)] (POL-07)
- Keyboard-only navigation can reach all interactive elements on all three pages; no mouse traps (POL-08)
- Home route First Load JS translates to < 100 KB gzipped (POL-04)
- verification-report.txt records all results with pass/fail per check
</success_criteria>

<output>
Create `.planning/phases/03-seo-polish-performance-accessibility/03-03-SUMMARY.md` when done
</output>
