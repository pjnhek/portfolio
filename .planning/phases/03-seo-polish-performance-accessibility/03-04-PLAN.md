---
phase: 03-seo-polish-performance-accessibility
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified: []
autonomous: false
requirements:
  - SEO-07
  - SEO-08
  - SEO-09
  - POL-09
  - POL-10

must_haves:
  truths:
    - "LinkedIn Post Inspector renders the OG card (image + title + description) for the preview URL"
    - "opengraph.xyz preview matches the D-OG-03/D-OG-04 design (left-aligned type stack, off-white, name/role/domain)"
    - "A real Slack or iMessage DM of the preview URL shows the OG card inline (not just a plain link)"
    - "The site renders correctly on a real iPhone at 375px — no horizontal scroll, text legible, contact links tap correctly"
    - "After 60 seconds on the home page, a reader can answer: 'James is an AI Engineer at Asurion doing RAG/eval work' and 'Contact via email or LinkedIn'"
  artifacts:
    - path: ".planning/phases/03-seo-polish-performance-accessibility/verification-report.txt"
      provides: "Human checkpoint confirmation appended to the existing automated verification record"
  key_links:
    - from: "James (human)"
      to: "LinkedIn Post Inspector at https://www.linkedin.com/post-inspector/"
      via: "paste PREVIEW_URL, click Inspect, verify image renders"
      pattern: "SEO-07 confirmed"
    - from: "James (human)"
      to: "https://opengraph.xyz"
      via: "paste PREVIEW_URL, verify card matches design"
      pattern: "SEO-08 confirmed"
---

<objective>
Close Phase 3 with the five human-only verification checkpoints that Claude cannot automate — LinkedIn Post Inspector, opengraph.xyz, a real Slack/iMessage DM, a real iPhone device, and a 60-second cold-read. These are blocking checkpoints: the phase does not close until James confirms all five.

Purpose: Per D-VERIFY-01 and D-VERIFY-02, Claude handles all automatable verification (Plan 03) and hands James a concrete checklist for human-only items. This plan gives James the exact URLs to visit, the exact things to look for, and a clear resume-signal ("type 'confirmed' after each check"). Phase 4 will re-run the LinkedIn Inspector and real-device checks against the production pjnhek.com domain after DNS cutover — but these preview-URL verifications catch render bugs early.

Note per ROADMAP.md SC#5: final production verification of SEO-07/08/09 + POL-09 against pjnhek.com is Phase 4 scope, NOT this phase. This plan verifies against the Vercel PREVIEW URL only.

Output:
- James confirms all five checkpoints
- Any issues found are fixed and re-verified before phase close
- Human confirmation appended to verification-report.txt
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-seo-polish-performance-accessibility/03-CONTEXT.md
@.planning/phases/03-seo-polish-performance-accessibility/03-03-SUMMARY.md
@.planning/phases/03-seo-polish-performance-accessibility/verification-report.txt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 1: LinkedIn Post Inspector + opengraph.xyz OG card verification (SEO-07, SEO-08, D-VERIFY-01)</name>

  <read_first>
    - .planning/phases/03-seo-polish-performance-accessibility/verification-report.txt — read the preview URL confirmed in Plan 03 verification; use the same URL here
    - 03-CONTEXT.md D-OG-03 — expected OG card content: "James Nhek / AI Engineer @ Asurion / RAG · evaluations · agentic workflows / pjnhek.com"
    - 03-CONTEXT.md D-OG-04 — expected visual: left-aligned type stack, off-white background, monochrome
    - 03-RESEARCH.md Pitfall 6 — LinkedIn caches aggressively; if stale card shows, use the "Refresh" button in LinkedIn Inspector
  </read_first>

  <what-built>
    Plans 01 and 02 shipped:
    - app/opengraph-image.tsx — static 1200×630 PNG with left-aligned type stack (James Nhek / AI Engineer @ Asurion / RAG · evaluations · agentic workflows / pjnhek.com)
    - app/layout.tsx, uses/page.tsx, projects/[slug]/page.tsx — all route metadata now includes openGraph.images pointing to this card
    
    Plan 03 confirmed via curl that og:image, og:title, and twitter:card are present in <head> on the preview URL.
    
    Now James verifies that social platforms actually render the card correctly.
  </what-built>

  <how-to-verify>
    **Get the preview URL:** Check verification-report.txt from Plan 03 for the exact preview URL used. It should be in the format `https://portfolio-[something].vercel.app` or the main preview `https://portfolio-kappa-bay-ew5cos0ri7.vercel.app`. Confirm it loads in a browser first.

    **SEO-07 — LinkedIn Post Inspector:**
    1. Go to https://www.linkedin.com/post-inspector/ (requires LinkedIn login)
    2. Paste the PREVIEW_URL into the URL field
    3. Click "Inspect" (or "Check" — the button label varies)
    4. If LinkedIn shows a cached version from before Plan 01/02 were deployed, click the "Refresh" button or "Re-scrape" option
    5. Verify:
       - OG image renders (should show the "James Nhek / AI Engineer @ Asurion" poster)
       - Title shows "James Nhek — AI Engineer" (or similar — the buildMetadata title for the home route)
       - Description shows "AI Engineer at Asurion. RAG, evaluations, and agentic workflows. Based in San Francisco. Open to roles."
    6. Screenshot the result for the record

    **SEO-08 — opengraph.xyz:**
    1. Go to https://opengraph.xyz
    2. Paste the PREVIEW_URL into the input field
    3. Click the preview/check button
    4. Verify the card matches:
       - Image renders (the typography poster)
       - Title and description text correct
       - Image is 1200×630 (or close — the tool may show dimensions)
    5. opengraph.xyz does NOT require login and tends to show fresher results than LinkedIn

    **What a PASS looks like:**
    - LinkedIn: OG card image renders (left-aligned James Nhek poster), title and description show below
    - opengraph.xyz: Same card image + correct metadata

    **What a FAIL looks like (and what to do):**
    - "No image" / blank card: Check the preview URL directly in browser → open DevTools → Elements → search for `og:image` in head. If the tag is missing, Plan 01 Task 3 may not have deployed yet. Check Vercel dashboard for the latest deployment.
    - LinkedIn shows an old version: Click "Refresh" in the inspector
    - Image URL is wrong: The `og:image` content should be an absolute URL (not a relative `/opengraph-image` path — metadataBase in layout.tsx resolves it). If it's relative, it means metadataBase is missing or not being applied.
  </how-to-verify>

  <resume-signal>Type "SEO-07 PASS / SEO-08 PASS" or describe any issues found. If issues found, provide the exact error so Claude can diagnose and fix before you continue to the next checkpoint.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: Real Slack/iMessage DM OG card preview (SEO-09, D-VERIFY-01)</name>

  <read_first>
    - 03-RESEARCH.md Pitfall 6 — social crawlers cache; use a URL you haven't shared before for the cleanest result; Slack and iMessage are more reliable than LinkedIn for preview testing
    - 03-CONTEXT.md D-VERIFY-01 — preview URL used here; production re-verification is Phase 4
  </read_first>

  <what-built>
    The site-wide OG card is live on the preview URL. This checkpoint verifies the OG card renders in real messaging apps — which scrape the URL server-side when the link is pasted, independent of the browser experience.
  </what-built>

  <how-to-verify>
    **SEO-09 — Real messaging app DM:**

    Option A (Slack — most reliable for developers):
    1. Open Slack and message yourself (or a test channel you own)
    2. Paste the PREVIEW_URL as a plain message (no formatting, just the URL)
    3. Send the message
    4. Wait 5-10 seconds for Slack to unfurl the link
    5. Verify: the OG card appears with image + title + description (should match the James Nhek typography poster)

    Option B (iMessage):
    1. Open iMessage and message yourself (using a second Apple device or the macOS Messages app)
    2. Paste the PREVIEW_URL and send
    3. Wait for iMessage to unfurl the link preview
    4. Verify the preview card shows image + title

    **What a PASS looks like:**
    - An inline card appears below the pasted URL showing the OG image (typography poster), title, and description

    **What a FAIL looks like:**
    - Only the URL shows as plain text (no card) — this usually means the meta tags weren't picked up. Try again with a slightly modified URL (add ?v=1 as a cache-buster — this forces a re-scrape). If still failing, check whether og:image is an absolute URL in the page source.
    - Card shows but no image — og:image URL may not be publicly accessible. Verify the preview deployment is public (Deployment Protection = None per STATE.md).
  </how-to-verify>

  <resume-signal>Type "SEO-09 PASS" or describe what you saw (app used, whether card rendered, any issues). If issues found, provide the URL you tested and the exact behavior observed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Real iPhone at 375px viewport — mobile render verification (POL-09)</name>

  <read_first>
    - 03-CONTEXT.md D-VERIFY-02 — James runs real-device check; Chrome DevTools emulation is not sufficient for POL-09
    - ROADMAP.md Phase 3 SC#4 — "renders correctly on a real iPhone at 375px (not just DevTools emulation)"
  </read_first>

  <what-built>
    The site was built mobile-first with Tailwind v4 responsive tokens and tested by Phase 1/2 plans. Phase 3 changed only metadata, icons, and the LCP image hint — not layout. This checkpoint confirms nothing broke on real hardware.
  </what-built>

  <how-to-verify>
    **POL-09 — Real iPhone device check:**

    1. On your iPhone, open Safari (default iOS browser — not Chrome mobile)
    2. Navigate to the PREVIEW_URL (the same preview URL from verification-report.txt)
    3. Check each of the following on the HOME page:
       - No horizontal scroll at 375px width (scroll left/right — nothing should scroll past the viewport edge)
       - Hero text ("James Nhek", "AI Engineer @ Asurion") is readable without zooming
       - Project cards stack to 1 column (not overflowing)
       - Contact section: `mailto:` link and copy-email button are both tappable (adequate touch target size)
       - The JN monogram appears in the browser tab (or bookmark bar) — this confirms icon.tsx is working
    4. Swipe to /uses:
       - Categories render in a single column, no horizontal overflow
    5. Tap one project card to visit a detail page:
       - "← Back to home" link is visible and tappable at the top
       - Narrative text is readable, no clipping

    **What a PASS looks like:**
    - Everything readable and tappable; no horizontal scroll; JN monogram in tab

    **What a FAIL looks like (and likely causes):**
    - Horizontal scroll on mobile: Probably a wide element (code block, long URL, unwrapped text). Find the culprit with a quick Safari DevTools inspect (connect iPhone via USB → macOS Safari → Develop menu).
    - JN monogram not showing: icon.tsx may not have been picked up in the latest deployment. Check the Vercel dashboard to confirm the deployment includes the new icon files.

    Note: Chrome DevTools emulation at 375px is NOT a substitute — iOS Safari renders some things differently (viewport height, font rendering, tap target sizing). Real device is required for this checkpoint to close.
  </how-to-verify>

  <resume-signal>Type "POL-09 PASS" or describe any issues found (which page, what broke, iPhone model/iOS version if relevant).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 4: 60-second cold-read legibility test (POL-10, D-VERIFY-02)</name>

  <read_first>
    - 03-CONTEXT.md D-VERIFY-02 — "POL-10's cold-read may be James's own read for v1 (a non-technical reader is higher-signal but a real-world dependency — not required to close the phase)"
    - ROADMAP.md Phase 3 SC#4 — "a non-technical cold reader can answer 'What does James do?' and 'How would I contact him?' after 60 seconds on the home page"
    - 03-CONTEXT.md Deferred Ideas — "Non-technical cold reader for POL-10 — optional, not required to close Phase 3"
  </read_first>

  <what-built>
    The home page narrative was written and refined in Phase 2: Hero → About (pivot opener) → Experience (numbers-backed Asurion bullets) → Featured Projects (4 cards with MetricCallouts) → Contact (mailto + copy-email + LinkedIn + GitHub). Phase 3 changed nothing about content — only metadata and performance.
  </what-built>

  <how-to-verify>
    **POL-10 — 60-second cold-read test:**

    This is your own read (or optionally a friend/family member). The standard:
    - Visit the home page PREVIEW_URL
    - Read for exactly 60 seconds (set a timer)
    - Stop and answer: "What does this person do?" and "How would I contact this person?"

    **PASS criteria:**
    - "What does James do?" → Answer should surface: AI Engineer, Asurion, RAG / evaluations / agentic workflows, or equivalent signal
    - "How would I contact him?" → Answer should surface: email or LinkedIn (both visible in the Contact section)

    This is the recruiter test. If you (as the author) struggle to answer either question in 60 seconds, the hierarchy or copy needs adjustment before phase close.

    **Optional (higher signal, not blocking):** Ask a non-technical friend/family member to visit the URL on their phone and answer the same two questions after one minute. This removes author-familiarity bias. Record their answer if you do this.

    **Note per 03-CONTEXT.md:** A non-technical third-party reader is explicitly noted as "optional, not required to close Phase 3." If you want to do it, great — it's higher signal. But James's own 60-second read is sufficient to close this checkpoint.
  </how-to-verify>

  <resume-signal>Type "POL-10 PASS — [brief answer to what James does + how to contact]" or describe any legibility issues that need fixing before phase close. If a third-party reader was used, include their answer.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 5: Phase close confirmation + 404 copy sign-off (D-404-01, Phase 3 close)</name>

  <read_first>
    - .planning/phases/03-seo-polish-performance-accessibility/verification-report.txt — confirm all automated checks passed (Plans 03)
    - 03-CONTEXT.md D-404-01 — "Final copy is Claude-drafted, James-confirms" — the 404 copy "This page wandered off. Here's the way back." was Claude-drafted; James confirms it here
    - ROADMAP.md Phase 3 success criteria — all 5 must be TRUE for the phase to close
  </read_first>

  <what-built>
    All Phase 3 work is complete:
    - Plan 01: buildMetadata factory + OG card + route metadata wired
    - Plan 02: JN icons + 404 page + sitemap + robots + fetchPriority fix
    - Plan 03: Lighthouse ≥95 / Accessibility 100 / axe 0 violations / focus rings / keyboard nav confirmed
    - Plans 01-04 checkpoints: LinkedIn Inspector / opengraph.xyz / real Slack DM / real iPhone confirmed
    
    This final checkpoint is a phase-close gate: confirm 404 copy is acceptable and all 5 ROADMAP success criteria are TRUE.
  </what-built>

  <how-to-verify>
    **D-404-01 — 404 copy confirmation:**
    Visit: `<PREVIEW_URL>/this-is-definitely-not-a-page`
    Verify the 404 page shows:
    - "404" in large display type
    - "This page wandered off. Here's the way back." — or edit this line now if you want different wording
    - "← Back to home" link that works

    If you want to change the wording, provide the replacement line and Claude will update not-found.tsx, redeploy, and close.

    **Phase 3 success criteria checklist — confirm all 5 are TRUE:**
    1. [ ] Every route exports generateMetadata through lib/seo.ts; sitemap.ts derives from content/projects.ts; robots.ts references sitemap; opengraph-image.tsx returns a 1200×630 PNG
    2. [ ] Lighthouse mobile Performance ≥ 95 and Accessibility = 100; axe zero issues on home/project detail/uses; all interactive elements have focus-visible: outline; fully keyboard-navigable; home-route JS < 100 KB gzipped
    3. [ ] not-found.tsx renders a styled 404; icon.tsx + apple-icon.tsx render; ArchitectureDiagram.tsx raster branch uses fetchPriority="high" (not deprecated priority)
    4. [ ] Site renders correctly on real iPhone at 375px; non-technical cold-read can answer "What does James do?" and "How to contact?"
    5. [ ] LinkedIn Post Inspector, opengraph.xyz, and real Slack/iMessage DM all rendered the OG card with image + title + description on the PREVIEW URL

    Note: SC#5 production re-verification on pjnhek.com is Phase 4 scope — this checkpoint only closes Phase 3.
  </how-to-verify>

  <resume-signal>Type "PHASE 3 COMPLETE" to close the phase. Include any final copy change for 404 if desired, or "404 copy approved as-is". If any success criterion is NOT yet TRUE, describe what's missing and Claude will address it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| James (human) → third-party tools | James pastes the preview URL into LinkedIn Post Inspector, opengraph.xyz — these are read-only preview tools |
| Preview URL → social scrapers | LinkedIn, Slack, iMessage scrapers fetch the preview URL server-side — URL is already public (Deployment Protection = None) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Information Disclosure | Sharing preview URL with social platforms | accept | Preview URL is intentionally public (Deployment Protection = None per STATE.md Plan 01-03 decision). URL is a Vercel non-guessable hash. OG content (name, role, domain) is intentionally public portfolio content. |
| T-03-12 | Tampering | LinkedIn/Slack/iMessage scrapers fetching public URL | accept | These are read-only crawlers fetching public HTML. No user input is accepted by these scrapers, and the site serves no user-controlled content in <head>. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. This plan has no code changes — only human verification. |

Note: This plan contains only human-verify checkpoints with no code changes. Threat surface: zero new attack surface. The honest assessment is that sharing a public URL with read-only social preview tools is an intended operation with no meaningful threat.
</threat_model>

<verification>
Phase 3 closes when James types "PHASE 3 COMPLETE" in the Checkpoint 5 resume signal, confirming:
1. LinkedIn Post Inspector rendered the OG card (SEO-07)
2. opengraph.xyz rendered the OG card (SEO-08)
3. Real Slack or iMessage DM showed the OG card inline (SEO-09)
4. Real iPhone at 375px rendered correctly — no horizontal scroll, contact links tappable (POL-09)
5. 60-second cold-read: reader can answer "What does James do?" and "How to contact?" (POL-10)
6. 404 copy confirmed (D-404-01)
</verification>

<success_criteria>
- SEO-07: LinkedIn Post Inspector confirms OG card image + title + description on PREVIEW URL
- SEO-08: opengraph.xyz confirms OG card matches D-OG-03/D-OG-04 (left-aligned type stack, off-white, monochrome)
- SEO-09: Real Slack or iMessage DM unfurls the OG card inline (not just a plain link)
- POL-09: Real iPhone Safari at 375px renders the full site without horizontal scroll; all tap targets work
- POL-10: 60-second read yields correct answers to "What does James do?" and "How to contact him?"
- D-404-01: James confirms the 404 copy "This page wandered off. Here's the way back." or provides final wording
- All five ROADMAP Phase 3 success criteria confirmed TRUE by James
</success_criteria>

<output>
Create `.planning/phases/03-seo-polish-performance-accessibility/03-04-SUMMARY.md` when done, marking Phase 3 COMPLETE.
Update .planning/ROADMAP.md Phase 3 "Plans:" section to 4/4 plans executed.
Update .planning/STATE.md current phase to "Phase 4 — Domain Cutover & Production Verification — ready to plan."
</output>
