---
phase: 02-content-sections-with-confidentiality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/content.ts
  - src/content/site.ts
  - src/content/experience.ts
  - src/content/projects.ts
  - src/content/uses.ts
  - src/lib/content.ts
  - src/app/globals.css
autonomous: false
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CONT-07
  - CONT-08
  - SEC-08

tags:
  - content
  - types
  - data-modules
  - confidentiality-source

user_setup:
  - service: github-profile-james
    why: "GitHub repo URLs for the 4 featured projects are needed to populate content/projects.ts. James must confirm exact slugs (or indicate public repos do not yet exist)."
    dashboard_config:
      - task: "Confirm GitHub URLs for sf-date-night-concierge, gtm-research-pipeline, voice-intent-eval, daily-weather-pipeline. If any repo is not yet public, the URL may point to https://github.com/pjnhek (profile) as a fallback for v1."
        location: "https://github.com/pjnhek"
  - service: resume-source-of-truth
    why: "Confidentiality gate (D-Asurion-02, D-Review-03) requires .planning/refs/RESUME-2026-05.md to exist before any Asurion bullet can be authored. Every Asurion sentence must cite a number from this file."
    dashboard_config:
      - task: "Deposit the public May-2026 resume at .planning/refs/RESUME-2026-05.md (markdown form preferred; .pdf acceptable). Commit before continuing past Task 0."
        location: "Local filesystem — .planning/refs/"

must_haves:
  truths:
    - "src/types/content.ts exports Project, Role, EducationItem, UsesItem, UsesCategory, SiteConfig, Metric types under strict mode."
    - "src/content/site.ts exports a single SiteConfig with James's name, tagline, location, email, github, linkedin, baseUrl."
    - "src/content/experience.ts exports roles: Role[] (3 work roles, Asurion role has tags) and education: EducationItem[] (USF + UH)."
    - "src/content/projects.ts exports projects: Project[] with exactly 4 entries (sf-date-night-concierge, gtm-research-pipeline, voice-intent-eval, daily-weather-pipeline)."
    - "src/content/uses.ts exports uses: UsesItem[] with 5 categories and 3-6 items per category (~15 total)."
    - "src/lib/content.ts exposes getAllProjects() and getProject(slug) accessors."
    - "Every Asurion-touching bullet/sentence in content/experience.ts carries an inline `// Resume source: ...` comment citing the resume location."
    - "src/app/globals.css declares html { scroll-padding-top: 4rem } and the motion-gated scroll-behavior: smooth — outside any @theme block."
    - "pnpm tsc --noEmit exits 0 after these files are written."
  artifacts:
    - path: "src/types/content.ts"
      provides: "Type definitions for all content modules"
      contains: "export type Project"
    - path: "src/content/site.ts"
      provides: "SiteConfig instance — name, email, GitHub, LinkedIn"
      contains: "export const site"
    - path: "src/content/experience.ts"
      provides: "roles + education arrays"
      contains: "export const roles"
    - path: "src/content/projects.ts"
      provides: "4 Project entries"
      contains: "export const projects"
    - path: "src/content/uses.ts"
      provides: "UsesItem array seed (Claude proposes; James edits in Plan 02-04)"
      contains: "export const uses"
    - path: "src/lib/content.ts"
      provides: "getAllProjects + getProject accessors"
      contains: "export function getAllProjects"
    - path: "src/app/globals.css"
      provides: "anchor-scroll CSS — scroll-padding-top + motion-gated smooth scroll"
      contains: "scroll-padding-top"
  key_links:
    - from: "src/content/projects.ts"
      to: "src/types/content.ts"
      via: "import type { Project }"
      pattern: "import type \\{ Project"
    - from: "src/lib/content.ts"
      to: "src/content/projects.ts"
      via: "import { projects }"
      pattern: "import \\{ projects \\}"
    - from: "Every Asurion bullet"
      to: ".planning/refs/RESUME-2026-05.md"
      via: "inline `// Resume source:` comment"
      pattern: "// Resume source:"
---

<objective>
Establish the **data + types foundation** that every subsequent Phase 2 plan composes against. This plan ships zero new user-visible content on its own — but every downstream plan (02-02 home composition, 02-03 detail routes, 02-04 /uses page, 02-05 diagrams + confidentiality gate) imports from the modules created here.

Authored as a single coherent type-and-content layer so the executor produces internally consistent shapes in one pass (avoiding the "type drift" of writing types in one task and content in another).

Purpose:
- Lock the typed-TS content convention (per Phase 1 STATE.md "Key Decisions Locked: typed TS modules in content/").
- Source every Asurion-touching bullet against `.planning/refs/RESUME-2026-05.md` with inline `// Resume source:` provenance, so the confidentiality gate in Plan 02-05 can verify D-Review-03 item 1 mechanically.
- Add the anchor-scroll CSS that SEC-08 depends on (the URL hash update is native; only the scroll-padding-top + motion-gated smooth-scroll needs CSS).
- Front-load the two human-prerequisite checkpoints (resume present + GitHub URLs confirmed) before any content drafting begins.

Output: 7 files committed (`src/types/content.ts`, `src/content/{site,experience,projects,uses}.ts`, `src/lib/content.ts`, `src/app/globals.css` appended). Build still passes; home page still shows Phase 1 "Coming soon" placeholders (Plan 02-02 wires real content into the home).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-CONTEXT.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md
@CLAUDE.md
@src/lib/env.ts
@src/app/globals.css
@tsconfig.json

<interfaces>
<!-- Verbatim type shapes the executor MUST produce in src/types/content.ts.
     Source: 02-RESEARCH.md §"Type definitions" lines 614-673 + 02-PATTERNS.md
     §"src/types/content.ts" lines 65-73. Strict-mode-safe under noUncheckedIndexedAccess. -->

```ts
export type SiteConfig = {
  name: string;
  tagline: string;
  location: string;
  email: string;
  github: string;
  linkedin: string;
  baseUrl: string;
};

export type Metric = { value: string; label: string };

export type Project = {
  slug: string;
  title: string;
  subtitle: string;
  metric: Metric;
  tech: string[];
  github: string;
  demo?: string;
  description?: string;
  problem: string;
  approach: string;
  result: string;
  diagram?: { src: string; alt: string; caption: string };
};

export type Role = {
  title: string;
  company: string;
  dates: string;
  location: string;
  bullets: string[];
  tags?: string[];
};

export type EducationItem = {
  program: string;
  institution: string;
  dates: string;
};

export type UsesCategory = "Models" | "MCP Servers" | "Eval Stack" | "Agent Framework" | "Dev Workflow";

export type UsesItem = {
  category: UsesCategory;
  name: string;
  rationale: string;
};
```

<!-- Existing Phase 1 contracts the executor must respect -->

From src/lib/env.ts: `export const env` is a server-only zod-validated env. `env.NEXT_PUBLIC_SITE_URL` is available (defaults to `"https://pjnhek.com"`).
From tsconfig.json: `@/*` → `./src/*`. `strict: true`, `noUncheckedIndexedAccess: true`. Implication: `projects.find(...)` always returns `Project | undefined`, never `Project`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Confirm prerequisites — resume deposited + GitHub URLs known</name>
  <read_first>
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-CONTEXT.md (§ Asurion Content Sourcing — D-Asurion-02, D-Asurion-03)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md (Pitfall 6 — Resume file not deposited before planning starts)
  </read_first>
  <files>.planning/refs/RESUME-2026-05.md (verified to exist; not modified by Claude)</files>
  <action>Pause execution. Verify the three preconditions in <how-to-verify> below. Do NOT create any other file in this plan until James replies with the resume-signal contents.</action>
  <what-built>
    Nothing yet — this is the **blocking** pre-execution checkpoint. No file in this plan can be authored without it.
  </what-built>
  <how-to-verify>
    Two preconditions must BOTH be true before continuing past this task:

    1. **Resume present (per D-Asurion-02):** Run `ls -la .planning/refs/RESUME-2026-05.md` from the repo root. The file MUST exist and contain the public May-2026 resume (markdown form preferred; `.pdf` accepted — if `.pdf`, also re-extract bullets/numbers into a `RESUME-2026-05.md` companion so inline `// Resume source: line N` comments work without paging a PDF).
       - If absent: deposit the resume now. Confidentiality gate (D-Review-03 item 1) cannot pass without it; every Asurion bullet in Task 3 requires a citable number from this file.

    2. **GitHub URLs confirmed (per RESEARCH.md Open Question #5):** Provide the GitHub repo URL for each of:
       - `sf-date-night-concierge`
       - `gtm-research-pipeline`
       - `voice-intent-eval`
       - `daily-weather-pipeline`

       For each, either supply the full `https://github.com/pjnhek/<repo-name>` URL OR state that the repo is not yet public — in which case the URL falls back to `https://github.com/pjnhek` (profile) for v1 and Phase 4 / DEP-10 promotes individual repos to pinned status before launch.

    3. **Contact email confirmed (per RESEARCH.md Open Question #2):** Default is `nhekvirakyuth@gmail.com`. Confirm or override (e.g., a future `james@pjnhek.com` is NOT yet provisioned and Phase 4 owns that DNS work — for v1 stick with the Gmail unless explicitly stating otherwise).
  </how-to-verify>
  <resume-signal>
    Reply with the exact contents to write into the relevant fields:
    - `RESUME_OK` (or attach how to confirm — `ls .planning/refs/RESUME-2026-05.md` output)
    - 4 lines: `<slug> -> <URL or PROFILE_FALLBACK>` (one per project)
    - `EMAIL: <address>` (default `nhekvirakyuth@gmail.com`)
  </resume-signal>
  <verify>
    <automated>test -f .planning/refs/RESUME-2026-05.md || test -f .planning/refs/RESUME-2026-05.pdf</automated>
  </verify>
  <done>James has confirmed all three preconditions and the resume file is present in `.planning/refs/`. Task 1 can begin.</done>
</task>

<task type="auto">
  <name>Task 1: Author types + lib/content + site.ts + uses.ts (seed)</name>
  <files>src/types/content.ts, src/content/site.ts, src/content/uses.ts, src/lib/content.ts</files>
  <read_first>
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md (§"Type definitions" lines 614-673; §"Content accessor" lines 705-719; §"/uses default content seed" lines 763-791)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md (§"src/types/content.ts" — convention to establish, header-comment style; §"src/lib/content.ts" — JSDoc/SERVER-ONLY banner pattern matching env.ts)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§Components/CopyEmail prop signature implies SiteConfig.email; §SiteFooter implies SiteConfig.github/linkedin; §Components/MetricCallout implies Metric type)
    - src/lib/env.ts (verbose JSDoc banner — match the style for lib/content.ts)
    - src/components/primitives/Section.tsx (header-comment style for type module)
    - tsconfig.json (verify strict + noUncheckedIndexedAccess to design return types that satisfy `Project | undefined`)
  </read_first>
  <action>
    Create four files implementing the typed content foundation. Per D-Uses-04 the uses.ts seed in this task is Claude's proposal; James edits in Plan 02-04.

    1. **`src/types/content.ts`** — pure `export type` module, no runtime exports. Reproduce the type shapes verbatim from the `<interfaces>` block above. Use the verbose `//` banner header per 02-PATTERNS.md (reference CONT-01 and list downstream consumers). All field names exact: `slug`, `title`, `subtitle`, `metric` (of type `Metric`), `tech: string[]`, `github`, `demo?`, `description?`, `problem`, `approach`, `result`, `diagram?` for Project. Asurion-only `tags?: string[]` on Role. `UsesCategory` is a string-literal union of exactly 5 values.

    2. **`src/content/site.ts`** — single named export `site: SiteConfig`. Populate per D-Uses-04 / RESEARCH.md Open Question #2: `name: "James Nhek"`, `tagline: "AI Engineer @ Asurion"`, `location: "San Francisco, CA"`, `email` = value confirmed in Task 0, `github: "https://github.com/pjnhek"`, `linkedin: <confirm during exec — default "https://www.linkedin.com/in/jamesnhek" subject to user verification>`, `baseUrl: env.NEXT_PUBLIC_SITE_URL`. Import `env` from `@/lib/env`. **Server-only by design — do NOT import from a "use client" file.** Header comment must say so explicitly per 02-PATTERNS.md.

    3. **`src/content/uses.ts`** — `uses: UsesItem[]` seed per 02-RESEARCH.md lines 763-791 verbatim (15 entries across 5 categories: Models × 3, MCP Servers × 3, Eval Stack × 3, Agent Framework × 2, Dev Workflow × 3). Header comment notes this is a Claude-proposed seed; James edits in Plan 02-04 per D-Uses-04.

    4. **`src/lib/content.ts`** — `getAllProjects(): readonly Project[]` returning the imported `projects` array, and `getProject(slug: string): Project | undefined` using `Array.prototype.find` (which always returns `T | undefined` under noUncheckedIndexedAccess — type signature MUST be `Project | undefined`, not `Project`). Use the verbose JSDoc banner pattern from `src/lib/env.ts` lines 1-26 (the `/** ... */` block) — explicit "SERVER-ONLY content accessors. DO NOT import from Client Components."

    Every file must use the `@/*` path alias (never relative `../`). Every header comment must reference the requirement ID it satisfies (CONT-01 / CONT-02 / CONT-05 / CONT-06).

    DO NOT add zod runtime validation to `lib/content.ts` — TypeScript checks the shape at build (per 02-RESEARCH.md line 156, zod is "available but unused in Phase 2"). The content modules are not user input; they are typed source.
  </action>
  <acceptance_criteria>
    - `grep -l "export type SiteConfig" src/types/content.ts` returns the file.
    - `grep -c "export type" src/types/content.ts` returns ≥ 7 (SiteConfig, Metric, Project, Role, EducationItem, UsesCategory, UsesItem).
    - `grep -c "noUncheckedIndexedAccess" src/types/content.ts` may be 0 (it's a tsconfig flag, not a code keyword), but the comment block MUST mention it as a downstream constraint.
    - `grep "Project | undefined" src/lib/content.ts` matches (return type explicit; not narrowed to `Project`).
    - `grep "import { env }" src/content/site.ts` matches (Server-only env read; `baseUrl` derived).
    - `grep -c "category:" src/content/uses.ts` returns ≥ 15 (one per UsesItem entry).
    - `grep -E "^\s*\"use client\"" src/content/site.ts src/content/uses.ts src/lib/content.ts src/types/content.ts | wc -l` returns 0 (no client directive on any of these files).
    - `pnpm tsc --noEmit` exits 0 with these four files in place (use stub `roles = []` and `projects = []` placeholders in NEXT task; for THIS task, `lib/content.ts` may temporarily not import from `@/content/projects` if it doesn't exist yet — or order tasks so projects.ts exists first; cleanest is to author Task 2 before Task 1 closes, but they're listed sequentially for readability — executor decides write order, tsc check at end of Task 1 may need `// @ts-expect-error` until Task 2 completes, OR Task 1 writes a placeholder `src/content/projects.ts` with `export const projects: Project[] = []` to satisfy the import).
    - Files do not appear in `grep -rn "src/content" src/ | grep -v "src/content/"` from a Client Component (no client component exists yet; constraint enforced by file naming).
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit && grep -l "export type Project" src/types/content.ts && grep -l "export function getAllProjects" src/lib/content.ts && grep -l "export const site" src/content/site.ts && grep -l "export const uses" src/content/uses.ts</automated>
  </verify>
  <done>
    Four files exist with the documented exports. TypeScript compiles. `lib/content.ts` cannot be imported from a Client Component (enforced by convention — no `"use client"` ever appears in these files). uses.ts seed has ~15 entries across the 5 locked categories.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author content/experience.ts + content/projects.ts (with Asurion confidentiality discipline)</name>
  <files>src/content/experience.ts, src/content/projects.ts</files>
  <read_first>
    - .planning/refs/RESUME-2026-05.md (the canonical source-of-truth — every Asurion bullet must trace to a numbered line here)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-CONTEXT.md (§Experience Section Depth — D-Exp-01..04; §Project Detail Page Depth — D-Proj-01..04; §Project Card Metric Callout — D-Metric-01..03)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md (§"Content module" lines 677-700; Pitfall 1 — Asurion confidentiality; Pitfall 9 — same Asurion bullet on two pages; Pitfall 6 — drafting without resume; the locked metric strings table line 230-237)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md (§"src/content/*.ts" — Resume source inline-comment convention lines 84-104)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§Experience section locked order; §Featured Projects table line 230-237 — primary metric per project; §Copywriting Contract voice rules 7-12)
    - src/types/content.ts (the types this content satisfies — read AFTER Task 1)
  </read_first>
  <action>
    Author the two heaviest content modules. Confidentiality discipline applies to EVERY Asurion-touching string in `experience.ts`.

    **`src/content/experience.ts`** — exports `roles: Role[]` and `education: EducationItem[]`:

    - `roles` order (D-Exp-01, locked): [0] AI Engineer @ Asurion (Oct 2025 – Present, San Francisco, CA), [1] Tax Analyst @ A to Z Tax Services, [2] Data Analyst @ FWD Life Insurance.
    - Asurion role: 3-5 bullets per D-Exp-02. **Every bullet MUST contain a number from RESUME-2026-05.md**, per D-Asurion-03. **Every bullet MUST be preceded by an inline `// Resume source: RESUME-2026-05.md line N — "<verbatim resume fragment>"` comment** (02-PATTERNS.md lines 84-104). If a candidate bullet has no resume number, do not include it — D-Review-03 item 1 will fail.
    - Asurion role gets `tags: string[]` (D-Exp-03) — 5-8 public-tech names the resume explicitly discloses (e.g., LangGraph, MLflow, pgvector). Tags are sourced from the resume too; same inline `// Resume source:` discipline.
    - Tax Analyst + FWD Life Insurance roles: 3-5 bullets each. Number-led where the resume has one (use `// Resume source:` comment); action-verb-led otherwise. **No `tags` field** on these two roles (D-Exp-03 — Asurion only).
    - **No internal Asurion product names, codenames, queue names, team names anywhere** (D-Asurion-04). The only proper noun referencing Asurion is the company name "Asurion" itself.
    - Dates for Tax Analyst and FWD: pull from the resume verbatim. Locations: same — pull from resume; if not present, leave as `""` for FWD/Tax Analyst (skip the location segment in the rendered header).
    - `education: EducationItem[]` per D-Exp-01: USF MSDS (`"M.S. Data Science"`, `"University of San Francisco"`, `"July 2025 – June 2026"`) and University of Houston (`"B.S. Mathematics + Data Science"`, `"University of Houston"`, `"2021"`).

    **`src/content/projects.ts`** — exports `projects: Project[]` with exactly 4 entries in this order: `sf-date-night-concierge`, `gtm-research-pipeline`, `voice-intent-eval`, `daily-weather-pipeline`.

    For each project, fill ALL required fields:
    - `slug` (the locked value above).
    - `title`: "SF Date Night Concierge", "GTM Research Pipeline", "Voice Intent Eval", "Daily Weather Pipeline" (working titles — refine in execution; final voice is Claude's draft, James edits).
    - `subtitle`: plain-English one-liner per UI-SPEC §"ProjectCard plain-English subtitle".
    - `metric: Metric`: VERBATIM from UI-SPEC line 232-237 (and 02-RESEARCH.md line 230-237) — `sf-date-night-concierge` = `{ value: "5,800+", label: "place embeddings (pgvector + HNSW)" }`; `gtm-research-pipeline` = `{ value: "Cohen's κ", label: "LLM-as-judge eval, self-preference bias" }`; `voice-intent-eval` = `{ value: "100%", label: "intent accuracy / 80 scenarios" }`; `daily-weather-pipeline` = `{ value: "<retraining-cadence metric pulled from RESUME-2026-05.md>", label: "Airflow → BigQuery ML" }`. **Final exact strings confirmed at execution against the resume** for the daily-weather one; the other three values are locked.
    - `tech: string[]`: 4-8 chips per card. Public-tech names the resume discloses for the relevant project.
    - `github`: full URL from Task 0 confirmation (`https://github.com/pjnhek/<repo>` or `https://github.com/pjnhek` fallback if repo not yet public).
    - `demo?`: optional — include if a live demo URL exists (likely only for `sf-date-night-concierge`); omit otherwise.
    - `description?`: optional 1-2 line card description per UI-SPEC line 224 (may elide if subtitle carries the load).
    - `problem`, `approach`, `result`: 100-180 words each (sum 300-500 per page per D-Proj-01). These are Claude-drafted initial narratives. They will be reviewed and refined for voice in Plan 02-03 alongside the route work. Do NOT use marketing voice — per voice rule 7 (no "passionate", "leveraged", "cutting-edge", etc.).
    - `diagram?`: leave undefined for all four. The voice-intent-eval diagram is added in Plan 02-05 (which writes the SVG + edits this file to add the `diagram` property).

    **No Asurion content in projects.ts** (Pitfall 9 — bullets live in exactly one place). These projects are personal; they should not reference Asurion at all.

    Header comments on both files reference the requirement ID(s) and the relevant D-* decision IDs (per D-Asurion-01..04 for experience.ts; per D-Proj-01, D-Metric-03 for projects.ts).
  </action>
  <acceptance_criteria>
    - `grep -c "// Resume source:" src/content/experience.ts` returns a number equal to the count of Asurion bullets (>= 3, <= 5) plus the Asurion `tags` count provenance (1 group comment OK).
    - `grep -E "^\s*export const roles" src/content/experience.ts` matches.
    - `grep -E "^\s*export const education" src/content/experience.ts` matches.
    - `grep -E "company:\s*\"Asurion\"" src/content/experience.ts` matches exactly once (only the AI Engineer role).
    - `grep -E "tags:\s*\[" src/content/experience.ts` matches exactly once (Asurion role only — D-Exp-03).
    - `grep -c "slug:" src/content/projects.ts` returns 4.
    - `grep -E "slug:\s*\"sf-date-night-concierge\"" src/content/projects.ts` matches; same for `gtm-research-pipeline`, `voice-intent-eval`, `daily-weather-pipeline`.
    - `grep -E "value:\s*\"5,800\+\"" src/content/projects.ts` matches.
    - `grep -E "value:\s*\"100%\"" src/content/projects.ts` matches.
    - `grep -E "value:\s*\"Cohen.s κ\"" src/content/projects.ts` matches (note the special characters — apostrophe + Greek kappa).
    - `grep -c "problem:" src/content/projects.ts` returns 4.
    - `grep -c "approach:" src/content/projects.ts` returns 4.
    - `grep -c "result:" src/content/projects.ts` returns 4.
    - **No Asurion content leaks into projects.ts:** `grep -i "asurion" src/content/projects.ts | grep -v "^//"` returns 0 lines.
    - **No forbidden voice words:** `grep -iE "(passionate|leveraged|cutting-edge|synergy|rockstar|ninja|amazing|incredible)" src/content/experience.ts src/content/projects.ts | grep -v "^[[:space:]]*//"` returns 0 lines.
    - `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit && [ $(grep -c "slug:" src/content/projects.ts) -eq 4 ] && grep -q "company:.*\"Asurion\"" src/content/experience.ts && [ $(grep -cE "company:\s*\"Asurion\"" src/content/experience.ts) -eq 1 ]</automated>
  </verify>
  <done>
    Both content files exist with the locked shapes. Every Asurion bullet carries a `// Resume source:` inline comment. No Asurion content leaks into projects.ts. No forbidden marketing words appear. TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append anchor-scroll CSS to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (read the existing file to know what's there — do NOT touch the @theme blocks or the @import or @source line)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md (§Anchor Navigation lines 814-845; §"globals.css additions" lines 747-757)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§Tailwind v4 @theme token usage lines 880-913 — "One addition to globals.css (NOT to @theme)")
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md (§"src/app/globals.css" — additive append rules lines 530-553)
  </read_first>
  <action>
    Append (do NOT modify existing rules) two CSS additions to `src/app/globals.css`, placed AFTER the existing `html { color: ...; background: ...; }` block (currently lines 46-49) and BEFORE the `body { ... }` block — or at the very end of the file, whichever the executor finds cleaner. The `html` selector gets a second declaration block; CSS cascade handles it.

    Add a section comment marker first (`/* Phase 2: anchor scroll behavior. ... */`) then the two rules:

    - `html { scroll-padding-top: 4rem; }` (always-on — gives 64px breathing room above target heading; SEC-08 anchor support).
    - `@media (prefers-reduced-motion: no-preference) { html { scroll-behavior: smooth; } }` (motion-gated — users with reduced-motion preference get instant jumps).

    **Do NOT add `.sr-only`** — Tailwind v4 ships it in the base layer (02-RESEARCH.md line 759). **Do NOT add new `@theme` tokens** (UI-SPEC line 882). **Do NOT touch** the `@import`, `@source not`, `@theme inline`, `@theme`, existing `html { color: ...; background: ...; }`, `body { ... }`, or `::selection { ... }` blocks.
  </action>
  <acceptance_criteria>
    - `grep -c "scroll-padding-top: 4rem" src/app/globals.css` returns 1.
    - `grep -c "scroll-behavior: smooth" src/app/globals.css` returns 1.
    - `grep -c "prefers-reduced-motion: no-preference" src/app/globals.css` returns 1.
    - `grep -c "@theme" src/app/globals.css` returns exactly 2 (one `@theme inline` for fonts + one `@theme` for tokens — the Phase 1 count; not 3).
    - `grep -c "\.sr-only" src/app/globals.css` returns 0 (Tailwind v4 base layer ships it).
    - The new `html` declaration is OUTSIDE any `@theme` block (visually verifiable; structurally enforced by appending after the closing `}` of the existing `@theme {...}` block).
    - `pnpm build` produces CSS without errors (Tailwind v4 + PostCSS picks up the new rules cleanly).
  </acceptance_criteria>
  <verify>
    <automated>[ $(grep -c "scroll-padding-top: 4rem" src/app/globals.css) -eq 1 ] && [ $(grep -c "prefers-reduced-motion: no-preference" src/app/globals.css) -eq 1 ] && [ $(grep -c "scroll-behavior: smooth" src/app/globals.css) -eq 1 ] && [ $(grep -c "@theme" src/app/globals.css) -eq 2 ] && pnpm build 2>&1 | grep -E "(error|Error)" | grep -v "type Error" || true</automated>
  </verify>
  <done>
    globals.css contains scroll-padding-top (always) and scroll-behavior: smooth (motion-gated). Phase 1 @theme blocks untouched. `pnpm build` produces zero CSS parser errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RESUME → content/experience.ts | Build-time authoring boundary: every Asurion sentence and number is sourced from `.planning/refs/RESUME-2026-05.md` by manual citation. The threat is the author (Claude/James) introducing a sentence that has no resume backing. |
| content/*.ts → rendered HTML | Server-Component render path. No user input. No XSS surface. |
| globals.css → browser | Stylesheet shipped as static asset. No user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-LEAK | Information Disclosure | `src/content/experience.ts` Asurion role bullets and tags | mitigate | D-Asurion-03 + `// Resume source:` inline comments per bullet. Plan 02-05 confidentiality gate verifies every Asurion sentence cites a number from RESUME-2026-05.md before phase verifier. |
| T-02-01-CODE | Information Disclosure | `src/content/projects.ts` — accidental Asurion content cross-contamination | mitigate | Pitfall 9 (RESEARCH.md). Acceptance criterion: `grep -i "asurion" src/content/projects.ts` returns 0 non-comment lines. Personal projects have no Asurion content. |
| T-02-01-VOICE | Information Disclosure (qualitative — reputation risk) | Marketing buzzwords leak into prose | mitigate | Voice rules 7-12 (UI-SPEC §Copywriting Contract). Acceptance criterion: forbidden-words grep returns 0. |
| T-02-01-ENV | Tampering | `lib/env.ts` accidentally imported from a Client Component | accept | No client components exist in this plan. lib/env import remains server-only. Re-evaluate when CopyEmail ships in Plan 02-05. |
| T-02-01-DEPS | Tampering | npm install of new packages | not-applicable | Zero new packages installed in Phase 2 (RESEARCH.md §Package Legitimacy Audit). No supply-chain surface added. |
</threat_model>

<verification>
After all three tasks complete:

1. `pnpm tsc --noEmit` exits 0.
2. `pnpm lint` exits 0 (no ESLint errors on new files).
3. `pnpm build` exits 0 — the home page still renders Phase 1 "Coming soon" placeholders; content modules are unused by `src/app/page.tsx` until Plan 02-02. This is expected.
4. `ls -la src/types/content.ts src/content/site.ts src/content/experience.ts src/content/projects.ts src/content/uses.ts src/lib/content.ts` — all 6 files exist.
5. `grep -c "// Resume source:" src/content/experience.ts` returns ≥ 3 (one per Asurion bullet at minimum).
6. `grep -c "slug:" src/content/projects.ts` returns exactly 4.
7. `grep "scroll-padding-top: 4rem" src/app/globals.css` matches.
8. **Confidentiality pre-check (informal — formal review is Plan 02-05):** read every Asurion bullet aloud. For each, verify: (a) the bullet contains a number; (b) the number traces to RESUME-2026-05.md via the inline comment; (c) no internal Asurion proper noun other than "Asurion" itself appears.
</verification>

<success_criteria>
- Task 0 checkpoint passed (resume + GitHub URLs + email confirmed).
- 6 new files created (types/content + 4 content modules + lib/content) + globals.css appended.
- Every Asurion bullet in experience.ts carries an inline `// Resume source:` provenance comment.
- No Asurion content appears in projects.ts (personal projects only).
- TypeScript strict compile passes.
- globals.css declares anchor-scroll CSS outside `@theme` block; `@theme` block count remains exactly 2.
- Build passes (home still shows Phase 1 placeholders — wired in 02-02).
- Plan committed via `gsd-sdk query commit`.
</success_criteria>

<output>
Create `.planning/phases/02-content-sections-with-confidentiality-gate/02-01-SUMMARY.md` with:
- List of files created and key fields populated.
- Asurion bullet count + resume-source line citations (for Plan 02-05 gate reviewer).
- GitHub URL resolution for each of the 4 projects (full URL vs profile fallback).
- Final confirmed contact email.
- Confirmation that uses.ts is a seed (Plan 02-04 finalizes per D-Uses-04).
- Any voice or factual ambiguity that James should adjudicate before Plan 02-05's confidentiality gate (e.g., "Asurion bullet 3 cites two resume numbers — is the second one essential?").
</output>
