---
phase: 02-content-sections-with-confidentiality-gate
plan: 05
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - public/diagrams/asurion-rag-pipeline.svg
  - public/diagrams/voice-intent-eval-flow.svg
  - src/components/interactive/CopyEmail.tsx
  - src/components/experience/ExperienceBlock.tsx
  - src/content/projects.ts
  - src/app/page.tsx
  - .planning/phases/02-content-sections-with-confidentiality-gate/CONFIDENTIALITY-REVIEW.md
autonomous: false
requirements:
  - SEC-02
  - SEC-06
  - SEC-07
  - PROJ-04
  - DIAG-01
  - DIAG-02
  - DIAG-03
  - DIAG-04
  - CONT-07
  - CONT-08

tags:
  - diagrams
  - copy-email
  - about-copy
  - contact-section
  - confidentiality-gate
  - phase-2-closeout

must_haves:
  truths:
    - "public/diagrams/asurion-rag-pipeline.svg exists, renders in the Asurion ExperienceBlock at full mobile width with crisp retina rendering, and its labels are ONLY from {Ingestion, Embed, Vector Store, Retriever, Reranker, LLM, Eval}."
    - "public/diagrams/voice-intent-eval-flow.svg exists, renders inside /projects/voice-intent-eval's Approach section, and depicts TTS → ASR → two-stage Claude classifier → dual-judge benchmark → CI."
    - "src/components/interactive/CopyEmail.tsx is THE ONLY \"use client\" file in the entire codebase (SEC-07). It receives `email` as a prop, never imports `@/lib/env`."
    - "src/app/page.tsx renders the real About copy (120-180 words, 2-3 paragraphs, first sentence opens with the tax-analyst → AI-engineer pivot, closes on Asurion work with a number from RESUME-2026-05.md) and the real Contact section (mailto: link + CopyEmail button + LinkedIn ExternalLink + GitHub ExternalLink)."
    - "ExperienceBlock conditionally renders the DIAG-01 ArchitectureDiagram between the Asurion bullets and the Asurion tag-chip row (only when role.company === 'Asurion')."
    - "content/projects.ts has voice-intent-eval's `diagram` field populated with `{ src: '/diagrams/voice-intent-eval-flow.svg', alt: <descriptive flow text>, caption: <short caption> }`."
    - "CONFIDENTIALITY-REVIEW.md is committed with all 3 checklist items marked passed (D-Review-03). The review covers every Asurion-touching paragraph and DIAG-01. This is a HARD MERGE GATE before /gsd:verify-phase 2."
    - "DIAG-01 caption verbatim per UI-SPEC §Asurion diagram placement: 'Generic RAG pipeline shape. Generic role labels only — no internal product names.'"
    - "Both SVGs use only `--color-ink` (resolved to #0a0a0a) and `--color-ink-muted` (resolved to #737373) — no accent hex colors."
    - "pnpm tsc --noEmit && pnpm lint && pnpm build all pass; CopyEmail success state announces via aria-live='polite' on the home Contact section."
  artifacts:
    - path: "public/diagrams/asurion-rag-pipeline.svg"
      provides: "DIAG-01 — 6-7 node generic RAG pipeline, no internal product names"
      contains: "<svg"
    - path: "public/diagrams/voice-intent-eval-flow.svg"
      provides: "DIAG-02 — 5-6 node Voice Intent Eval flow, public-tech names allowed"
      contains: "<svg"
    - path: "src/components/interactive/CopyEmail.tsx"
      provides: "ONLY \"use client\" island in Phase 2 — clipboard write with aria-live announcement"
      contains: "\"use client\""
    - path: "src/components/experience/ExperienceBlock.tsx"
      provides: "Conditional DIAG-01 render when role.company === 'Asurion'"
      contains: "ArchitectureDiagram"
    - path: "src/content/projects.ts"
      provides: "voice-intent-eval.diagram populated with DIAG-02 src/alt/caption"
      contains: "voice-intent-eval-flow.svg"
    - path: "src/app/page.tsx"
      provides: "Real About copy + real Contact section (mailto + CopyEmail + LinkedIn + GitHub)"
      contains: "<CopyEmail"
    - path: ".planning/phases/02-content-sections-with-confidentiality-gate/CONFIDENTIALITY-REVIEW.md"
      provides: "Signed 3-item confidentiality checklist — HARD MERGE GATE"
      contains: "Signed: James Nhek"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/interactive/CopyEmail.tsx"
      via: "import { CopyEmail } and <CopyEmail email={site.email} />"
      pattern: "<CopyEmail"
    - from: "src/components/experience/ExperienceBlock.tsx"
      to: "src/components/primitives/ArchitectureDiagram.tsx"
      via: "conditional render when role.company === 'Asurion'"
      pattern: "ArchitectureDiagram"
    - from: "src/content/projects.ts"
      to: "public/diagrams/voice-intent-eval-flow.svg"
      via: "diagram.src field on voice-intent-eval entry"
      pattern: "voice-intent-eval-flow\\.svg"
    - from: ".planning/phases/02-content-sections-with-confidentiality-gate/CONFIDENTIALITY-REVIEW.md"
      to: ".planning/refs/RESUME-2026-05.md"
      via: "checklist item 1 cites resume line numbers per Asurion sentence"
      pattern: "RESUME-2026-05"
---

<objective>
The phase closer. Five distinct concerns coordinated under one plan because they SHARE the confidentiality gate boundary and because Asurion-touching prose (About-closer + Experience bullets) + Asurion diagram (DIAG-01) must be reviewed together by D-Review-02 ("review runs once, at end of phase, against everything").

This plan ships:
1. **Two sanitized SVG diagrams** authored in Excalidraw, post-edited to hard-code the resolved mono palette (`#0a0a0a` for ink, `#737373` for ink-muted) because `ArchitectureDiagram` uses passthrough `<img src>` which does not propagate `currentColor`.
2. **The only `"use client"` file in the codebase** — `CopyEmail` — receiving `email` as a prop from the Contact section (never imports `@/lib/env`).
3. **The real About paragraphs** (120-180 words, 2-3 paragraphs, conversational, pivot-first opening, Asurion-closing with a resume number).
4. **The real Contact section** on home — mailto + CopyEmail + LinkedIn + GitHub.
5. **DIAG-01 wired into the Asurion ExperienceBlock** + **DIAG-02 wired into /projects/voice-intent-eval's Approach section** (which the Plan 02-03 route already renders conditionally on `project.diagram`).
6. **`CONFIDENTIALITY-REVIEW.md`** — the 3-item objective checklist (D-Review-03), committed before `/gsd:verify-phase 2` runs. **HARD MERGE GATE.**

MVP slice outcome: after this plan, the Phase 2 success criteria are all true. A recruiter sees the complete portfolio narrative — Hero → About (pivot) → Experience (with Asurion architecture diagram) → 4 Projects (one with its own diagram on its detail page) → Contact (working mailto + copy button + outbound social links). Every Asurion-touching string has been signed-off by James against the public May-2026 resume.

Output: 2 SVGs, 1 new "use client" component, modifications to 4 existing files (ExperienceBlock, projects.ts, page.tsx, and the review artifact). Phase 2 closes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/refs/RESUME-2026-05.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-CONTEXT.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-01-SUMMARY.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-02-SUMMARY.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-03-SUMMARY.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-04-SUMMARY.md

<interfaces>
<!-- CopyEmail — verbatim from 02-UI-SPEC.md §Components/CopyEmail lines 651-719.
     The ONLY "use client" file added in Phase 2 (SEC-07 hard rule). -->

```tsx
"use client";
import { Clipboard, Check } from "lucide-react";
import { useState } from "react";

type CopyEmailProps = { email: string };

export function CopyEmail({ email }: CopyEmailProps) {
  const [state, setState] = useState<"idle" | "success" | "error">("idle");

  async function handleCopy() {
    try {
      await navigator.clipboard.writeText(email);
      setState("success");
      setTimeout(() => setState("idle"), 2000);
    } catch {
      setState("error");
      setTimeout(() => setState("idle"), 5000);
    }
  }

  const label = state === "success" ? "Copied" :
                state === "error" ? "Copy failed — select email manually" :
                "Copy email";
  const Icon = state === "success" ? Check : Clipboard;

  return (
    <>
      <button
        type="button"
        onClick={handleCopy}
        className="inline-flex min-h-[44px] items-center gap-2 border border-[color:var(--color-rule)] px-4 py-2 font-medium text-[length:var(--text-body)] text-[color:var(--color-ink)] hover:border-[color:var(--color-ink)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-ink)]"
      >
        <Icon size={16} aria-hidden="true" />
        <span>{label}</span>
      </button>
      <span role="status" aria-live="polite" className="sr-only">
        {state === "success" ? "Copied to clipboard" :
         state === "error" ? "Copy failed. Select the email address to copy it manually." :
         ""}
      </span>
    </>
  );
}
```

<!-- DIAG-01 (Asurion) — caption + alt strings LOCKED -->

DIAG-01 caption: `Generic RAG pipeline shape. Generic role labels only — no internal product names.`
DIAG-01 alt: `Box-and-arrow architecture diagram showing a retrieval-augmented generation pipeline: documents flow into Ingestion, then Embed, then a Vector Store. User queries pass through Retriever and Reranker before being sent to an LLM. An Eval loop feeds back into the pipeline.`
DIAG-01 label whitelist (D-Asurion-04): `{Ingestion, Embed, Vector Store, Retriever, Reranker, LLM, Eval}` — ONLY these. 6-7 nodes max.

<!-- DIAG-02 (Voice Intent Eval) — caption + alt DRAFTED at execution -->

DIAG-02 labels allowed: `TTS`, `ASR`, `Two-stage Claude classifier`, `Dual-judge benchmark`, `CI` (5-6 nodes). Public-tech names allowed (this is a personal project — D-Asurion-04's stricter rule does NOT apply here).

<!-- CONFIDENTIALITY-REVIEW.md template — verbatim from 02-RESEARCH.md §Artifact shape lines 501-539.
     This is the signed-off artifact, not the unfilled template. -->

```markdown
# Phase 2 Confidentiality Review

**Reviewed by:** James Nhek
**Reviewed on:** YYYY-MM-DD
**Resume source-of-truth:** `.planning/refs/RESUME-2026-05.md` (sha256: <hash>)

## 3-Item Checklist (D-Review-03)

### Item 1: Every Asurion sentence cites a number from RESUME-2026-05.md
- [x] About section closing sentence (file: src/app/page.tsx line X — number: "X" — resume location: line Y)
- [x] Asurion bullet 1 (file: src/content/experience.ts — number: "X" — resume location: line Y)
- [x] Asurion bullet 2 …
- [x] Asurion bullet 3 …

### Item 2: No internal Asurion product / codename / queue / team names anywhere
- [x] src/app/page.tsx (About + Contact)
- [x] src/content/experience.ts (Asurion role)
- [x] src/content/projects.ts (vacuously — no Asurion content)
- [x] src/content/site.ts (vacuously)
- [x] src/content/uses.ts (vacuously)
- [x] public/diagrams/asurion-rag-pipeline.svg

### Item 3: No screenshots of any Asurion internal system
- [x] public/ directory contents reviewed; no screenshots
- [x] src/content/*.ts reviewed for image paths; no Asurion screenshots

## DIAG-01 specific check (D-Asurion-04)
- [x] All box labels on public/diagrams/asurion-rag-pipeline.svg are from the whitelist
- [x] No public-tech names on DIAG-01

## Sign-off
Signed: James Nhek
```

<!-- Phase 1 + Plan 02-02 dependencies (all already shipped) -->

From `@/components/primitives/ArchitectureDiagram` — `{ src, alt, caption? }`, `alt` REQUIRED.
From `@/components/primitives/ExternalLink` — mailto:/https:/http: allowlist.
From `@/components/primitives/Tag`, `@/components/primitives/MetricCallout`, `@/components/primitives/Section`.
From `@/content/site` — `site.email`, `site.github`, `site.linkedin`.
From `@/content/experience` — `roles`, `education`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author both SVG diagrams + populate voice-intent-eval.diagram</name>
  <files>public/diagrams/asurion-rag-pipeline.svg, public/diagrams/voice-intent-eval-flow.svg, src/content/projects.ts</files>
  <read_first>
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-CONTEXT.md (§Architecture Diagram Tool + Style — D-Diag-01..04; §Asurion Content Sourcing — D-Asurion-04 label whitelist)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§Asurion diagram placement lines 309-319; §Voice Intent Eval diagram lines 321-327; §Confidentiality Visual Constraints lines 861-873)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md (§Diagram Workflow lines 562-606; specifically "Option B (works with passthrough <img>)" lines 587-589 — hard-code resolved palette values)
    - src/components/primitives/ArchitectureDiagram.tsx (the consumer — `alt` REQUIRED at TS level; passthrough <img> for SVGs; aspect-[16/9] reservation; do NOT inline as <svg> — would break Phase 1 freeze)
    - src/app/globals.css (token color values — `--color-ink: oklch(0.18 0 0)` ≈ #0a0a0a; `--color-ink-muted: oklch(0.55 0 0)` ≈ #737373)
    - src/types/content.ts (the `diagram?: { src; alt; caption }` field shape on Project)
    - src/content/projects.ts (current state — populate the voice-intent-eval `diagram` property)
  </read_first>
  <action>
    Author two SVGs in Excalidraw (https://excalidraw.com), export to `public/diagrams/`, post-edit to hard-code the resolved mono palette per RESEARCH.md Option B (passthrough `<img>` does not propagate CSS color, so inline color values are required). Strict monochrome is locked per D-Diag-02 — no accent hex values anywhere in either SVG.

    **`public/diagrams/asurion-rag-pipeline.svg`** (DIAG-01 — D-Asurion-04 strict labels):
    - Authoring (in Excalidraw): box-and-arrow flow with 6-7 nodes max. Allowed labels ONLY:
      - `Ingestion`, `Embed`, `Vector Store`, `Retriever`, `Reranker`, `LLM`, `Eval`
    - Flow shape (D-Diag-03): `Ingestion → Embed → Vector Store` (left side, the indexing path) and on the query side: `Retriever → Reranker → LLM`, with `Eval` looping back to the pipeline.
    - **Forbidden anywhere on DIAG-01:** any public-tech name (`pgvector`, `LangSmith`, `Vertex`, `Claude`, `Anthropic`, `OpenAI`, etc.) and any internal Asurion product/codename/queue/team name. Acceptance criterion grep verifies the SVG source.
    - Stroke + fill style: only black (or `currentColor`) at draw time. Then in the exported SVG file, find-and-replace every inline `stroke="#000000"` (or whatever Excalidraw output) with `stroke="#0a0a0a"` and every secondary/de-emphasized stroke with `stroke="#737373"`. Do the same for `fill` attributes if any. No accent colors. **The SVG must NOT contain any non-mono hex values.**
    - Export from Excalidraw with "Embed scene data" enabled so the file remains re-editable.

    **`public/diagrams/voice-intent-eval-flow.svg`** (DIAG-02 — public-tech names allowed):
    - Authoring: 5-6 nodes max. Labels: `TTS`, `ASR`, `Two-stage Claude classifier`, `Dual-judge benchmark`, `CI`.
    - Flow: `TTS → ASR → Two-stage Claude classifier → Dual-judge benchmark → CI` (with arrows or a loop back from CI to the classifier if depicting the feedback loop) — flow shape locked per D-Diag-04.
    - Same color discipline: hard-code `#0a0a0a` and `#737373` only.

    **`src/content/projects.ts`** modification: populate the `diagram` field on the `voice-intent-eval` entry:
    ```ts
    diagram: {
      src: "/diagrams/voice-intent-eval-flow.svg",
      alt: "<descriptive flow: TTS, then ASR, then two-stage Claude classifier, then dual-judge benchmark, with a CI feedback loop>",
      caption: "<short caption describing what the diagram depicts>",
    },
    ```
    The exact alt + caption strings are Claude-drafted, James-confirms. The other 3 projects' `diagram` fields stay undefined (Plan 02-03 ships them without diagrams).

    **Important:** do NOT inline the SVGs as React `<svg>` JSX. They must remain referenced via `<img src>` per `ArchitectureDiagram.tsx`'s passthrough branch (which is locked in Phase 1).

    **No DIAG-03 placement work in this task** — Task 3 wires DIAG-01 into ExperienceBlock; Plan 02-03's `/projects/[slug]/page.tsx` already wires DIAG-02 conditionally on `project.diagram` (no change needed there beyond populating the data).
  </action>
  <acceptance_criteria>
    - `ls public/diagrams/asurion-rag-pipeline.svg public/diagrams/voice-intent-eval-flow.svg` finds both files.
    - **DIAG-01 label whitelist enforcement** (D-Asurion-04): every text element in `public/diagrams/asurion-rag-pipeline.svg` is from `{Ingestion, Embed, Vector Store, Retriever, Reranker, LLM, Eval}`. Verifiable via: `grep -oE ">[A-Z][a-z][a-zA-Z ]*<" public/diagrams/asurion-rag-pipeline.svg | sort -u | grep -vE "^>(Ingestion|Embed|Vector Store|Retriever|Reranker|LLM|Eval)<$" | wc -l` returns 0. (Adjust regex if SVG text rendering uses `<tspan>`; alternative check: `grep -iE "(pgvector|langsmith|langchain|vertex|anthropic|openai|claude|gpt|gemini)" public/diagrams/asurion-rag-pipeline.svg | wc -l` returns 0.)
    - **Mono-palette enforcement:** `grep -oE "(stroke|fill)=\"#[0-9a-fA-F]{3,6}\"" public/diagrams/asurion-rag-pipeline.svg | sort -u | grep -ivE "(#0a0a0a|#737373|none|currentColor)" | wc -l` returns 0 (only the two locked mono hex values, optionally `none` for transparent fills, optionally `currentColor`).
    - Same mono check for `public/diagrams/voice-intent-eval-flow.svg`.
    - **DIAG-02 labels include the expected node set:** `grep -iE "(TTS|ASR|Claude|Dual-judge|CI)" public/diagrams/voice-intent-eval-flow.svg | wc -l` returns ≥ 5 (one match per node).
    - **No Asurion product/team names on DIAG-01:** `grep -iE "(asurion|chathub|inferno|<actual internal codenames if James has any>)" public/diagrams/asurion-rag-pipeline.svg | wc -l` returns 0. (Even the literal word "Asurion" should not appear on the diagram — only generic role labels.)
    - **content/projects.ts updated:** `grep -E "voice-intent-eval-flow\\.svg" src/content/projects.ts` matches. `grep -E "diagram:\s*\{" src/content/projects.ts | wc -l` returns 1 (only voice-intent-eval has it).
    - **Other 3 projects do not have `diagram` populated:** `grep -B 2 "diagram:" src/content/projects.ts | grep -iE "(sf-date-night|gtm-research|daily-weather)" | wc -l` returns 0.
    - `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit && test -f public/diagrams/asurion-rag-pipeline.svg && test -f public/diagrams/voice-intent-eval-flow.svg && grep -q "voice-intent-eval-flow\\.svg" src/content/projects.ts && ! grep -iE "(pgvector|langsmith|vertex|anthropic|claude|gpt|gemini|openai)" public/diagrams/asurion-rag-pipeline.svg</automated>
  </verify>
  <done>Both SVGs exist, use only the locked mono palette, DIAG-01 labels are strictly from the whitelist, content/projects.ts has voice-intent-eval.diagram populated. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build CopyEmail client island + wire DIAG-01 into ExperienceBlock</name>
  <files>src/components/interactive/CopyEmail.tsx, src/components/experience/ExperienceBlock.tsx</files>
  <read_first>
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§Components/CopyEmail lines 651-719 — the canonical implementation; §Asurion diagram placement lines 309-319)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md (§CopyEmail lines 353-374; §src/app/page.tsx line 436 — "Implementation choice: pass an optional `diagram` prop to ExperienceBlock, or render the <ArchitectureDiagram> inline... when role.company === 'Asurion'")
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md (§Pattern 3 lines 416-424; Pitfall 3 — env import from client; Pitfall 8 — icon set expansion)
    - src/components/experience/ExperienceBlock.tsx (Plan 02-02 output — current state)
    - src/components/primitives/ArchitectureDiagram.tsx (props shape — required alt)
  </read_first>
  <action>
    Two coordinated builds.

    **`src/components/interactive/CopyEmail.tsx`** (SEC-07 — THE ONLY `"use client"` FILE IN PHASE 2):

    Reproduce the canonical implementation from the `<interfaces>` block above (which is verbatim from UI-SPEC.md §Components/CopyEmail lines 651-719). Key invariants:

    - `"use client"` directive is line 1 of the file.
    - Imports order: directive → `lucide-react` icons (`Clipboard`, `Check` — and ONLY these two — per Pitfall 8 budget) → React (`useState`).
    - **DO NOT import `@/lib/env`** (Pitfall 3) — `email` arrives as a prop from a Server Component (page.tsx Contact section).
    - State machine: `idle → success (2s) → idle` ; `idle → error (5s) → idle`.
    - `role="status" aria-live="polite"` `sr-only` span for non-urgent SR announcement (NOT `aria-live="assertive"` — UI-SPEC line 715).
    - `min-h-[44px]` touch target (UI-SPEC Spacing-Scale Exceptions).
    - Icon size 16px + `aria-hidden="true"`.
    - Border 1px `--color-rule` resting → `--color-ink` on hover (no fill, mono palette).
    - `focus-visible:` 4-class outline string verbatim from ExternalLink.tsx line 57.
    - **No animation, no toast lib, no `useCallback`** (React Compiler handles memoization per CLAUDE.md).

    Server-Component file's header comment is REPLACED with a `// CopyEmail client island (SEC-07, UI-SPEC §Components/CopyEmail).` block noting this is the SOLE `"use client"` file added in Phase 2.

    **`src/components/experience/ExperienceBlock.tsx`** modification:

    Wire DIAG-01 inline when `role.company === "Asurion"`. Two valid implementation choices per PATTERNS.md line 436:

    Choice A (simpler — recommended): render `<ArchitectureDiagram>` inline inside ExperienceBlock between the `<ul>` of bullets and the conditional tag-chip row, gated on `role.company === "Asurion"`:

    ```tsx
    {role.company === "Asurion" && (
      <ArchitectureDiagram
        src="/diagrams/asurion-rag-pipeline.svg"
        alt="Box-and-arrow architecture diagram showing a retrieval-augmented generation pipeline: documents flow into Ingestion, then Embed, then a Vector Store. User queries pass through Retriever and Reranker before being sent to an LLM. An Eval loop feeds back into the pipeline."
        caption="Generic RAG pipeline shape. Generic role labels only — no internal product names."
      />
    )}
    ```

    The alt and caption strings MUST be exactly verbatim from UI-SPEC §Asurion diagram placement (lines 315-317). These strings are themselves part of the confidentiality discipline — they signal to readers (and to the confidentiality reviewer at Task 5) that labels are deliberately abstracted.

    Choice B (more general): add an optional `diagram?: { src; alt; caption }` prop to `ExperienceBlock`, populated from a new `Role.diagram?` field, with the data flowing through `content/experience.ts`. **Skip Choice B** — adds complexity (a content-field change) for no benefit (DIAG-01 is the only diagram in Experience for Phase 2).

    Pick Choice A. Add `import { ArchitectureDiagram } from "@/components/primitives/ArchitectureDiagram";` at the top of ExperienceBlock.tsx. The component remains a Server Component (no `"use client"`).
  </action>
  <acceptance_criteria>
    - **CopyEmail file:**
      - `head -1 src/components/interactive/CopyEmail.tsx` exactly equals `"use client";`.
      - `grep -E "import \\{ Clipboard, Check \\} from \"lucide-react\"" src/components/interactive/CopyEmail.tsx` matches.
      - `grep -c "import.*lucide-react" src/components/interactive/CopyEmail.tsx` returns 1 (no other icon imports).
      - `grep -c '@/lib/env' src/components/interactive/CopyEmail.tsx` returns 0 (Pitfall 3 — env never imported here).
      - `grep -E "navigator\\.clipboard\\.writeText" src/components/interactive/CopyEmail.tsx` matches.
      - `grep -E "role=\"status\" aria-live=\"polite\"" src/components/interactive/CopyEmail.tsx` matches.
      - `grep -E "min-h-\[44px\]" src/components/interactive/CopyEmail.tsx` matches.
      - `grep -E "focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-\[color:var\(--color-ink\)\]" src/components/interactive/CopyEmail.tsx` matches.
    - **THE ONLY `"use client"` file in the codebase (SEC-07 hard rule):**
      - `grep -rlE "^\"use client\"" src | wc -l` returns exactly 1.
      - That file is `src/components/interactive/CopyEmail.tsx`.
    - **ExperienceBlock:**
      - `grep -c "ArchitectureDiagram" src/components/experience/ExperienceBlock.tsx` returns ≥ 2 (import + render).
      - `grep -E "role\\.company === \"Asurion\"" src/components/experience/ExperienceBlock.tsx` matches.
      - `grep -E "src=\"/diagrams/asurion-rag-pipeline\\.svg\"" src/components/experience/ExperienceBlock.tsx` matches.
      - `grep -E "Generic RAG pipeline shape\\. Generic role labels only — no internal product names\\." src/components/experience/ExperienceBlock.tsx` matches (verbatim caption).
      - `grep -E "^\"use client\"" src/components/experience/ExperienceBlock.tsx` returns nothing (still a Server Component).
    - **No new lucide icons leak anywhere else:** `grep -rE "import.*lucide-react" src/ | wc -l` returns exactly 1 (only CopyEmail uses lucide).
    - `pnpm tsc --noEmit && pnpm lint && pnpm build` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint && pnpm build && [ "$(head -1 src/components/interactive/CopyEmail.tsx)" = '"use client";' ] && [ $(grep -rlE "^\"use client\"" src | wc -l) -eq 1 ] && grep -q "role.company === \"Asurion\"" src/components/experience/ExperienceBlock.tsx && grep -q "asurion-rag-pipeline\\.svg" src/components/experience/ExperienceBlock.tsx</automated>
  </verify>
  <done>CopyEmail is the only `"use client"` file in src/. State machine (idle/success/error) implemented with aria-live polite SR announcement. ExperienceBlock conditionally renders DIAG-01 when role.company === "Asurion" with the locked caption + alt strings. TypeScript + ESLint + build all pass.</done>
</task>

<task type="auto">
  <name>Task 3: Author About copy + Contact section in page.tsx</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - .planning/refs/RESUME-2026-05.md (canonical source for the Asurion-closer number in About + the About narrative facts)
    - src/app/page.tsx (current state — Plan 02-02 output with About + Contact placeholders)
    - src/content/site.ts (Plan 02-01 — site.email, site.github, site.linkedin)
    - src/components/primitives/ExternalLink.tsx (allowlist includes mailto:)
    - src/components/interactive/CopyEmail.tsx (Task 2 output — props: { email })
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-CONTEXT.md (§About Section Voice + Length — D-About-01..04)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§About section lines 181-188; §Contact section lines 283-292; §Copywriting Contract voice rules 7-12)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md (Pitfall 2 — vague "passionate about AI" copy; Pitfall 7 — wrong target email)
  </read_first>
  <action>
    Two coordinated edits inside `src/app/page.tsx` — replacing the Plan 02-02 placeholders for About and Contact.

    **About section (`01. About`)** — replace the "Coming soon — drafted in Plan 02-05..." placeholder with Claude-drafted prose per D-About-01..04:

    - 120-180 words total (count words; aim for the lower end for the 60-second mobile test).
    - 2-3 short paragraphs.
    - **First sentence MUST open with the tax-analyst → AI-engineer pivot** (D-About-03; UI-SPEC line 184). Example shape (NOT the final copy): "I started out as a tax analyst and ended up an AI engineer — which is a stranger sentence than it looks." Use first person, conversational register, contractions OK, parenthetical asides OK (per D-About-02 — conversational voice over corporate).
    - Middle paragraph: narrate the path through data work (FWD Life Insurance) + grad school (USF MSDS) + the pivot into AI engineering. No buzzwords.
    - **Closing sentence MUST contain a number sourced from RESUME-2026-05.md** (D-Asurion-03 — every Asurion sentence on the site cites a number). Inline a `{/* Resume source: line N — "<verbatim resume fragment>" */}` comment AT the JSX level so the Task 5 confidentiality review can verify item 1.
    - **No `<ArchitectureDiagram>` in About** — UI-SPEC line 187: "About becomes pure prose." DIAG-01 already lives in ExperienceBlock (Task 2).
    - **Forbidden words anywhere in About:** "passionate", "leveraged", "cutting-edge", "synergy", "rockstar", "ninja", "I love", "let's build", "amazing", "incredible", any superlative, the word "journey" — UI-SPEC voice rules 7-12.

    Render structure for About:
    ```tsx
    <Section id="about" number="01" title="About">
      <div className="space-y-6 text-[length:var(--text-body)] leading-[var(--leading-body)] text-[color:var(--color-ink)]">
        <p>{paragraph 1 — first sentence is the pivot}</p>
        <p>{paragraph 2 — narrative}</p>
        <p>{paragraph 3 — closing on Asurion work, with a number; preceded by a {/* Resume source: ... */} comment}</p>
      </div>
    </Section>
    ```

    **Contact section (`04. Contact`)** — replace the "Coming soon — wired in Plan 02-05" placeholder with the real section per UI-SPEC lines 283-292:

    ```tsx
    <Section id="contact" number="04" title="Contact">
      <div className="space-y-4 text-[length:var(--text-body)] leading-[var(--leading-body)] text-[color:var(--color-ink)]">
        <p className="text-[color:var(--color-ink-muted)]">Email is the fastest way to reach me.</p>
        <div className="flex flex-col gap-3 md:flex-row md:items-center md:gap-4">
          <ExternalLink href={`mailto:${site.email}`}>{site.email}</ExternalLink>
          <CopyEmail email={site.email} />
        </div>
        <div className="flex flex-col gap-3 md:flex-row md:gap-4">
          <ExternalLink href={site.linkedin}>LinkedIn</ExternalLink>
          <ExternalLink href={site.github}>GitHub</ExternalLink>
        </div>
      </div>
    </Section>
    ```

    Imports update in page.tsx — add: `import { CopyEmail } from "@/components/interactive/CopyEmail";`. Confirm `site` is already imported from Plan 02-02 (it was, for `<ExternalLink href={site.github}>See more on GitHub</ExternalLink>` in the Projects section). Confirm `ExternalLink` is already imported.

    Update the page.tsx header comment to reflect Phase 2 closure: page now composes Hero (frozen) + 4 numbered Sections with real content; CopyEmail is the only client island.

    **Critical:** the home page MUST NOT carry `"use client"` anywhere. CopyEmail is the lone client island; the page itself remains a Server Component that just renders `<CopyEmail email={site.email} />` (props pass server→client cleanly).
  </action>
  <acceptance_criteria>
    - **About section:**
      - `grep -E "<Section id=\"about\" number=\"01\" title=\"About\">" src/app/page.tsx` matches.
      - About no longer contains "Coming soon" — `grep -E "Coming soon" src/app/page.tsx` returns 0 (Contact placeholder also gone — see below).
      - About paragraph count: between 2 and 3 — `awk '/id="about"/,/<\/Section>/' src/app/page.tsx | grep -c "<p" ` returns 2 or 3.
      - **First sentence pivot check:** the first paragraph mentions both "tax analyst" (or "tax") AND "AI engineer" (or "AI"). Verifiable by inspection — Claude verifies during execution.
      - **Closing sentence number check:** the About section contains AT LEAST ONE number near the end. Verifiable: `awk '/id="about"/,/<\/Section>/' src/app/page.tsx | grep -cE "[0-9]" >= 1`. (Stricter check happens at Task 5's confidentiality gate.)
      - **Inline resume-source comment on the closing sentence:** `awk '/id="about"/,/<\/Section>/' src/app/page.tsx | grep -c "Resume source:" ` returns ≥ 1.
      - **No forbidden marketing words in About:** `awk '/id="about"/,/<\/Section>/' src/app/page.tsx | grep -iE "(passionate|leveraged|cutting-edge|synergy|rockstar|ninja|I love|let.s build|amazing|incredible|journey)" | wc -l` returns 0.
      - **No <ArchitectureDiagram> in About:** `awk '/id="about"/,/<\/Section>/' src/app/page.tsx | grep -c "ArchitectureDiagram"` returns 0.

    - **Contact section:**
      - `grep -E "<Section id=\"contact\" number=\"04\" title=\"Contact\">" src/app/page.tsx` matches.
      - `grep -E "<CopyEmail email=\{site\\.email\}" src/app/page.tsx` matches.
      - `grep -E "href=\`mailto:\\\$\\{site\\.email\\}\`" src/app/page.tsx` matches OR `grep -E "href=\{`mailto:\\\${site\\.email}`\}" src/app/page.tsx` matches (template-literal mailto).
      - `grep -E "<ExternalLink href=\{site\\.linkedin\}" src/app/page.tsx` matches.
      - `grep -E "<ExternalLink href=\{site\\.github\}" src/app/page.tsx` matches at least TWICE (once for Projects "See more on GitHub", once for Contact GitHub).

    - **Imports + client-island discipline:**
      - `grep -c 'import \{ CopyEmail \} from "@/components/interactive/CopyEmail"' src/app/page.tsx` returns 1.
      - `head -3 src/app/page.tsx | grep -c '"use client"'` returns 0 (page.tsx remains a Server Component).
      - **Sitewide `"use client"` count is still exactly 1:** `grep -rlE "^\"use client\"" src | wc -l` returns 1 (still only CopyEmail).

    - **Hero block frozen (re-asserted from Plan 02-02):** `grep -A 16 "Hero — outside any <section>" src/app/page.tsx | grep -E "(James Nhek|AI Engineer @ Asurion|RAG · evaluations · agentic workflows|San Francisco — open to AI Engineer roles\\.)" | wc -l` returns 4 — all four hero copy lines still present verbatim after the About + Contact edits.

    - **Build + lint:**
      - `pnpm tsc --noEmit && pnpm lint && pnpm build` all exit 0.
      - `pnpm dev` + visit `http://localhost:3000` — About renders ≥ 2 paragraphs, Contact renders mailto + CopyEmail + LinkedIn + GitHub.
      - Click the CopyEmail button — the label flips to "Copied" for 2 seconds, then back to "Copy email". Open DevTools → Application → no JS errors.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint && pnpm build && ! grep -E "Coming soon" src/app/page.tsx && grep -q "<CopyEmail email=" src/app/page.tsx && grep -q "Resume source:" src/app/page.tsx && [ $(grep -rlE "^\"use client\"" src | wc -l) -eq 1 ] && ! awk '/id="about"/,/<\/Section>/' src/app/page.tsx | grep -iE "(passionate|leveraged|cutting-edge|synergy|rockstar|ninja)"</automated>
  </verify>
  <done>About section is 2-3 paragraphs of conversational prose opening with the pivot and closing with a number citing a Resume source comment. Contact section renders mailto + CopyEmail + LinkedIn + GitHub with site config values. No "Coming soon" placeholders remain. Sitewide `"use client"` count is exactly 1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual sanity pass — Asurion diagram + Voice Intent Eval diagram + Contact CopyEmail</name>
  <read_first>
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§Confidentiality Visual Constraints lines 861-873; §Asurion diagram placement; §Voice Intent Eval diagram)
  </read_first>
  <files>src/app/page.tsx, src/app/projects/[slug]/page.tsx, src/app/uses/page.tsx, public/diagrams/asurion-rag-pipeline.svg, public/diagrams/voice-intent-eval-flow.svg (all read-only during this checkpoint — visual review only)</files>
  <action>Pause execution. Visually inspect the deployed site (Vercel preview or local pnpm dev) per the steps in <how-to-verify> below. If a visual fix is needed, Claude applies it and the checkpoint re-runs.</action>
  <what-built>
    All 5 tasks worth of code has executed. The site renders:
    - Home: Hero (frozen) + About (real, with pivot + Asurion closer + number) + Experience (3 roles, Asurion role has DIAG-01 between bullets and tag chips) + Projects (4 cards) + Contact (mailto + CopyEmail + LinkedIn + GitHub) + SiteFooter.
    - /uses: 5 numbered Sections (Plan 02-04).
    - /projects/sf-date-night-concierge, /gtm-research-pipeline, /voice-intent-eval (with DIAG-02 in Approach), /daily-weather-pipeline.

    Visual review needed before the confidentiality gate (Task 5) — confidentiality is a text-and-label check; this checkpoint catches anything visual that needs adjustment first (e.g., diagram blurry at retina; CopyEmail button cramped at 375px; Asurion diagram bleeds out of the content column).
  </what-built>
  <how-to-verify>
    On the Vercel preview URL (or local `pnpm dev` if preview not yet deployed). Per D-About-04 (Claude drafts, James edits), James inspects the About prose as part of this visual pass and flags any voice/content edits via the `fix:` resume-signal below — the visual checkpoint doubles as the About-copy editorial pass:

    1. **Home @ 375px (iPhone DevTools preset or real iPhone):**
       - Hero, About (≥ 2 paragraphs), Experience (3 roles), Asurion role shows DIAG-01 between bullets and tag chips, Asurion tag chips wrap (no horizontal scroll), 4 project cards stack to 1 column, Contact mailto+CopyEmail stack vertically, SiteFooter stacks to 2 lines.
       - DIAG-01 renders crisply (no blur). At 375px the SVG occupies the full content column width. No text labels overflow.
       - CopyEmail button is ≥ 44×44 px touch target. Tapping it copies the email and the label flips to "Copied" for 2s.

    2. **Home @ 1280px desktop:**
       - 4 project cards in 2-column grid. Contact mailto + CopyEmail align horizontally. SiteFooter is a single line.
       - DIAG-01 occupies the same content-column width (max-w-2xl = 672px).

    3. **/projects/voice-intent-eval @ 375px AND 1280px:**
       - BackLink visible at top. H1, subtitle, tech chips, GitHub link, MetricCallout (`100%`).
       - Approach section: prose paragraph(s), then DIAG-02 renders inline, then more prose.
       - DIAG-02 renders crisply, all 5-6 node labels visible (TTS, ASR, etc.), no public-tech name SHADE OF GRAY drifts (per Confidentiality Visual Constraints — strict mono only).

    4. **DevTools console clean** on home + /uses + /projects/voice-intent-eval — no warnings, no errors.

    5. **Tab through home with keyboard only** — every interactive element shows the focus-visible 2px ink outline: hero has no focusable (correct — it's text), then each `<Section>` is skipped, ProjectCard `<Link>`s are focusable, GitHub `<ExternalLink>` is focusable, CopyEmail button is focusable, footer `/uses` Link + GitHub + LinkedIn are focusable.

    6. **`prefers-reduced-motion`** — toggle macOS "Reduce motion" in System Settings → Accessibility → Display, then click `#projects` anchor: should be an instant jump, not a smooth scroll. ProjectCard hover transition should not animate.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` — visual pass; proceed to Task 5 confidentiality gate.
    - `fix: <description>` — Claude attempts the fix; re-checkpoint after.

    Common fixes that may surface:
    - "DIAG-01 too small at 375px" → adjust the SVG's viewBox or width attribute.
    - "Asurion tag chip row wraps to 4 lines, looks heavy" → trim chip count to 5-6.
    - "Voice Intent Eval diagram has a label cut off at 375px" → re-export from Excalidraw with smaller text scale.
    - "CopyEmail button + mailto link feel cramped on mobile" → bump gap from `gap-3` to `gap-4`.
  </resume-signal>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>James has visually inspected all three diagrams and the new interactive Contact button. Any layout fixes needed are applied. Task 5 confidentiality gate can run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Confidentiality review — author CONFIDENTIALITY-REVIEW.md and sign off</name>
  <read_first>
    - .planning/refs/RESUME-2026-05.md (the source-of-truth for item 1 — every Asurion sentence MUST cite a line from here)
    - src/app/page.tsx (the About section closing sentence — Item 1 candidate)
    - src/content/experience.ts (the Asurion bullets and tags — Items 1 + 2)
    - public/diagrams/asurion-rag-pipeline.svg (the DIAG-01 labels — Items 2 + DIAG-01 whitelist check)
    - public/diagrams/voice-intent-eval-flow.svg (vacuously checked — personal project)
    - src/content/site.ts (Item 2 — confirm no internal product names appear; should be vacuously OK)
    - src/content/projects.ts (Item 2 — should be vacuously OK; no Asurion content)
    - src/content/uses.ts (Item 2 — should be vacuously OK)
    - public/ directory contents (Item 3 — confirm no Asurion screenshots)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-CONTEXT.md (§Confidentiality Review Process — D-Review-01..04)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md (§Confidentiality Gate Workflow lines 488-560 — specifically §"Artifact shape" lines 492-539 for the template)
  </read_first>
  <files>.planning/phases/02-content-sections-with-confidentiality-gate/CONFIDENTIALITY-REVIEW.md</files>
  <action>Author CONFIDENTIALITY-REVIEW.md using the template from 02-RESEARCH.md §"Artifact shape" lines 501-539. Walk the 3-item D-Review-03 checklist + the DIAG-01 specific check against every file listed in <read_first>. Mark each `[x]` only if it passes. If ANY item fails, fix the violation in code, then RE-RUN the full 3-item checklist (D-Review-04 — no partial re-reviews). Once all items pass, sign with `Signed: James Nhek` and commit.</action>
  <what-built>
    The portfolio is complete. CONFIDENTIALITY-REVIEW.md does not yet exist. This task creates and signs it.
  </what-built>
  <how-to-verify>
    **STEP 1 — Author `.planning/phases/02-content-sections-with-confidentiality-gate/CONFIDENTIALITY-REVIEW.md`** using the template from 02-RESEARCH.md §"Artifact shape" lines 501-539 (verbatim from the `<interfaces>` block in this plan). Fill in:

    - `Reviewed by: James Nhek`
    - `Reviewed on: <today's date>`
    - `Resume source-of-truth:` path + sha256 hash. Compute via `shasum -a 256 .planning/refs/RESUME-2026-05.md` (or `.pdf` extension if PDF form was deposited) and paste the hash inline. The hash binds the review to a specific resume version — if the resume file changes later, the gate should be re-run.

    **STEP 2 — Run the 3-item checklist** (D-Review-03). MARK EACH BOX `[x]` ONLY IF THE ITEM PASSES. If any item fails, do NOT proceed — fix the violation, then RE-RUN ALL THREE items per D-Review-04 (fixing one bullet can introduce a new violation elsewhere).

    ### Item 1: Every Asurion sentence cites a number from RESUME-2026-05.md
    For each Asurion-touching sentence on the site, record:
    - File path
    - Approximate line number
    - The exact number cited
    - The matching line in RESUME-2026-05.md

    Sentences to check (NOT exhaustive — find every one):
    - About section closing sentence in `src/app/page.tsx`.
    - Every bullet in the Asurion role inside `src/content/experience.ts` (each has a `// Resume source:` comment per Plan 02-01 Task 2 — verify each one).
    - The `tags` array on the Asurion role (each tag should be sourced from a public-tech name the resume discloses).

    If a sentence has no traceable number → cut it or rewrite it → restart the checklist.

    ### Item 2: No internal Asurion product / codename / queue / team names anywhere in copy or diagrams
    Files scanned:
    - `src/app/page.tsx` (About + Contact)
    - `src/content/experience.ts` (Asurion role + bullets + tags)
    - `src/content/projects.ts` (vacuously — personal projects)
    - `src/content/site.ts`, `src/content/uses.ts` (vacuously)
    - `public/diagrams/asurion-rag-pipeline.svg` (DIAG-01 — label whitelist verified by Task 1 grep)

    Cross-check: grep each file for any proper noun OTHER than "Asurion" (the company name). Each proper noun MUST be either (a) a public-tech name the resume discloses (allowed in copy per D-Asurion-04, FORBIDDEN on DIAG-01), or (b) a personal-project name, or (c) a public institution name (USF, University of Houston, etc.). Any internal-Asurion proper noun = FAIL.

    ### Item 3: No screenshots of any Asurion internal system
    - `ls -la public/` — confirm no PNG/JPG/screenshot files referencing Asurion systems.
    - `grep -rE "\\.(png|jpg|jpeg|webp|avif)" src/content/ src/app/` — every image path inspected for non-Asurion-leakage.

    ### DIAG-01 specific check (D-Asurion-04)
    - Every text element in `public/diagrams/asurion-rag-pipeline.svg` is from the whitelist `{Ingestion, Embed, Vector Store, Retriever, Reranker, LLM, Eval}`.
    - No public-tech names on DIAG-01 (no `pgvector`, `LangSmith`, `Vertex`, `Anthropic`, `Claude`, `OpenAI`, etc.).

    **STEP 3 — Sign off.** Append `Signed: James Nhek` at the bottom of the file. Commit.

    **If any item fails:** revise the offending content; re-run the FULL THREE-ITEM CHECKLIST (D-Review-04 — no partial re-reviews); re-sign.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `signed` — the file is authored, all 3 items pass, James has signed. /gsd:verify-phase 2 can run.
    - `fail: <which item> — <details>` — Claude attempts the fix, the checklist re-runs (full 3 items per D-Review-04), then this checkpoint repeats.
  </resume-signal>
  <verify>
    <automated>test -f .planning/phases/02-content-sections-with-confidentiality-gate/CONFIDENTIALITY-REVIEW.md && grep -q "Signed: James Nhek" .planning/phases/02-content-sections-with-confidentiality-gate/CONFIDENTIALITY-REVIEW.md && grep -E "\\[x\\] All box labels" .planning/phases/02-content-sections-with-confidentiality-gate/CONFIDENTIALITY-REVIEW.md && [ $(grep -c "\\[x\\]" .planning/phases/02-content-sections-with-confidentiality-gate/CONFIDENTIALITY-REVIEW.md) -ge 5 ]</automated>
  </verify>
  <done>
    CONFIDENTIALITY-REVIEW.md exists in the phase directory, all 3 D-Review-03 checklist items + the DIAG-01 specific check are marked `[x]`, James has signed. Phase 2 is ready for `/gsd:verify-phase 2`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `site.email` prop → `CopyEmail` (Server → Client) | Build-time inlined string passed via React prop. No user input. No env import on the client side. |
| `navigator.clipboard.writeText(email)` | Browser-owned API. Writes only the page-provided string; no read access required; no information disclosure. |
| RESUME → Asurion content on the site | The Phase 2 dominant threat — every Asurion sentence/diagram label is reviewed against the resume + D-Asurion-04 whitelist via the manual D-Review-03 gate. |
| `public/diagrams/*.svg` → `<img src>` | Static asset. SVG content rendered as image; no inline `<svg>` (would create XSS surface). Phase 1 `ArchitectureDiagram` routes `.svg` through passthrough `<img>`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-LEAK | Information Disclosure | Asurion confidentiality leak via About closer / Experience bullets / DIAG-01 labels | mitigate | D-Review-03 3-item checklist as hard merge gate (Task 5). Failure → full re-run (D-Review-04). DIAG-01 label whitelist enforced via grep in Task 1 acceptance criteria. |
| T-02-05-ENV | Information Disclosure | CopyEmail imports `@/lib/env` and the env throw runs in the browser | mitigate | Pitfall 3 explicit. Acceptance criterion: `grep '@/lib/env' src/components/interactive/CopyEmail.tsx` returns 0. `email` arrives as a prop. |
| T-02-05-CLIPB | Tampering | Clipboard hijack — page writes attacker payload to clipboard | not-applicable | Page writes only `site.email` (build-time constant). No user-controllable input is ever written to clipboard. |
| T-02-05-SVG | Tampering | Malicious SVG payload (script tags inside SVG) | accept | SVGs are authored manually in Excalidraw + post-edited by James. Trusted source. No external SVG imports. Risk is low for a personal portfolio. |
| T-02-05-CLIENT | Tampering | A second `"use client"` file leaks into the codebase | mitigate | SEC-07 hard rule. Acceptance criterion at Task 2 + Task 3: `grep -rlE "^\"use client\"" src \| wc -l` returns exactly 1. |
| T-02-05-VOICE | Information Disclosure (reputation) | Forbidden marketing words in About closer | mitigate | Forbidden-words grep on About text in Task 3 acceptance. |
| T-02-05-RESUME | Tampering | Resume file changes after the review is signed; gate becomes stale | mitigate | CONFIDENTIALITY-REVIEW.md captures the resume sha256 hash. Phase 3+ work that touches Asurion content should re-run the gate if the hash differs. |
</threat_model>

<verification>
End-of-phase verification (this plan IS the Phase 2 closer):

1. `pnpm tsc --noEmit && pnpm lint && pnpm build` all exit 0.
2. `pnpm dev` + smoke-test the full site:
   - Home renders: Hero (frozen) + About (real prose with pivot + number) + Experience (3 roles + DIAG-01 inside Asurion block + 2 education entries) + Projects (4 cards) + Contact (mailto + CopyEmail + LinkedIn + GitHub) + SiteFooter.
   - /uses renders (Plan 02-04 — re-smoke).
   - /projects/voice-intent-eval renders DIAG-02 inside Approach.
   - The other 3 detail pages render without diagrams.
3. **Click CopyEmail button** — label flips to "Copied" for 2s, copied content matches `site.email`. (No `aria-live="assertive"` regression — should be polite.)
4. **Sitewide client island count:** `grep -rlE "^\"use client\"" src | wc -l` returns exactly 1.
5. **CONFIDENTIALITY-REVIEW.md exists and is signed** — all 3 D-Review-03 items `[x]` + DIAG-01 whitelist check `[x]` + `Signed: James Nhek`.
6. **No DevTools console errors** on /, /uses, /projects/voice-intent-eval.
7. **Keyboard nav** through the home — all interactive elements (project card Links, Contact mailto/CopyEmail/LinkedIn/GitHub, footer /uses/GitHub/LinkedIn) reachable with Tab; visible focus-visible outline on each; no mouse traps.
8. **Mobile @ 375px**: no horizontal scroll on any route; DIAG-01 and DIAG-02 render at full content-column width; Asurion tag chips wrap.
9. **Plan committed.** `/gsd:verify-phase 2` can run.

Note: Lighthouse / axe / 60-second cold-read are Phase 3. They are NOT part of this plan's verification.
</verification>

<success_criteria>
- Both SVG diagrams exist with the locked mono palette and label rules.
- voice-intent-eval has `diagram` populated.
- CopyEmail is THE ONLY `"use client"` file in the codebase.
- ExperienceBlock conditionally renders DIAG-01 for the Asurion role with verbatim caption + alt.
- About copy is 2-3 paragraphs, opens with the pivot, closes with a number citing a Resume source comment.
- Contact section renders mailto + CopyEmail + LinkedIn + GitHub.
- All forbidden marketing words absent.
- CONFIDENTIALITY-REVIEW.md is signed with all 3 D-Review-03 items + the DIAG-01 whitelist check passed.
- All builds/lints pass.
- Phase 2 SC #1-5 (per ROADMAP.md) all satisfied.
- Plan + review artifact committed.
</success_criteria>

<output>
Create `.planning/phases/02-content-sections-with-confidentiality-gate/02-05-SUMMARY.md` with:
- Confirmation both diagrams render crisply with mono palette only.
- Confirmation CopyEmail is the only client island.
- About paragraph count + word count + the resume line(s) cited by the closing sentence.
- Final email used in the Contact mailto (should match the Plan 02-01 Task 0 confirmed value).
- Path to the signed CONFIDENTIALITY-REVIEW.md and a one-line summary ("All 3 items + DIAG-01 whitelist check passed").
- Vercel preview URL where the full portfolio is now live.
- Any deferred-to-Phase-3 visual concerns surfaced during Task 4 (e.g., "Hero portrait would land well here in Phase 3 — but defer until LCP budget is re-checked").

Then mark the phase ready for `/gsd:verify-phase 2`.
</output>
