---
phase: 02-content-sections-with-confidentiality-gate
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/app/projects/[slug]/page.tsx
  - src/components/nav/BackLink.tsx
  - src/content/projects.ts
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-05

tags:
  - dynamic-route
  - project-detail-pages
  - back-navigation

must_haves:
  truths:
    - "Visiting /projects/sf-date-night-concierge, /projects/gtm-research-pipeline, /projects/voice-intent-eval, /projects/daily-weather-pipeline each renders a Problem → Approach → Result narrative."
    - "`generateStaticParams` returns all 4 slugs at build; `pnpm build` produces 4 static HTML files under `.next/server/app/projects/` (one per slug)."
    - "Each detail page calls `generateMetadata` returning title = `${project.title} — James Nhek` and description = subtitle (truncated to 160 chars). No openGraph or twitter keys (Phase 3 owns SEO factory)."
    - "Each detail page renders: BackLink (← Back to home) → H1 (project title at --text-heading, NOT --text-display) → subtitle → tech chips → GitHub + optional Demo ExternalLinks → MetricCallout (scale='detail') → three numbered Sections (Problem 01, Approach 02, Result 03)."
    - "BackLink uses Next.js <Link> (internal), NOT ExternalLink. Carries focus-visible outline + ← glyph U+2190 wrapped in aria-hidden."
    - "Each project's problem/approach/result strings in content/projects.ts are refined for voice (conversational, no forbidden marketing words, weekend-project test applied per UI-SPEC §Copywriting Contract voice rule 7)."
    - "Visiting /projects/<invalid-slug> returns 404 (Next.js notFound())."
    - "pnpm tsc --noEmit && pnpm lint && pnpm build all pass."
  artifacts:
    - path: "src/app/projects/[slug]/page.tsx"
      provides: "Dynamic route — pre-renders 4 project detail pages at build"
      contains: "export async function generateStaticParams"
    - path: "src/components/nav/BackLink.tsx"
      provides: "Internal back-arrow link primitive"
      contains: "export function BackLink"
    - path: "src/content/projects.ts"
      provides: "Refined problem/approach/result narratives for all 4 projects"
      contains: "approach:"
  key_links:
    - from: "src/app/projects/[slug]/page.tsx"
      to: "src/lib/content.ts"
      via: "import { getAllProjects, getProject }"
      pattern: "from \"@/lib/content\""
    - from: "src/app/projects/[slug]/page.tsx"
      to: "next/navigation"
      via: "import { notFound }"
      pattern: "notFound"
---

<objective>
Ship the **`/projects/[slug]` dynamic route** that pre-renders all 4 project detail pages at build via `generateStaticParams` + per-page `generateMetadata`. This is the Phase 2 establishment of the App Router dynamic-route convention (no precedent in the codebase — Phase 1 has no dynamic routes).

The MVP slice outcome: after this plan, a recruiter who clicks any of the 4 project cards on the home page lands on a tight Problem → Approach → Result writeup of that project. The Voice Intent Eval card's detail page is wired ready for DIAG-02 (the `<ArchitectureDiagram>` render conditional on `project.diagram`) — the actual SVG asset and the `diagram` field population land in Plan 02-05.

Purpose:
- Establish the Next.js 16 async-params dynamic-route pattern (`params: Promise<{ slug: string }>` — first use site in the codebase).
- Establish the `generateMetadata` per-route pattern (Phase 3 will refactor into `lib/seo.ts` factory; Phase 2 inlines title + description only — NO openGraph/twitter keys per RESEARCH.md Pitfall 10).
- Refine `content/projects.ts` problem/approach/result narratives from Plan 02-01's drafted placeholders into recruiter-ready prose (300–500 words per page across the three sections).
- Ship `BackLink` — the internal navigation counterpart to `ExternalLink` — for use on both `/projects/[slug]` AND `/uses` (Plan 02-04 imports it).

Output: 1 new component file, 1 new route file, 1 modified content file. All 4 project detail pages live on Vercel preview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-CONTEXT.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-01-SUMMARY.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-02-SUMMARY.md

<interfaces>
<!-- App Router dynamic-route contract — Next.js 16 async params. Verbatim from
     02-RESEARCH.md §"Pattern 1" lines 322-362. -->

```tsx
// src/app/projects/[slug]/page.tsx (NEW)
import { notFound } from "next/navigation";
import type { Metadata } from "next";
import { getAllProjects, getProject } from "@/lib/content";

export async function generateStaticParams() {
  const projects = getAllProjects();
  return projects.map((p) => ({ slug: p.slug }));
}

export async function generateMetadata(
  { params }: { params: Promise<{ slug: string }> }
): Promise<Metadata> {
  const { slug } = await params;
  const project = getProject(slug);
  if (!project) return {};
  return {
    title: `${project.title} — James Nhek`,
    description: project.subtitle.slice(0, 160),
  };
}

export default async function ProjectDetailPage(
  { params }: { params: Promise<{ slug: string }> }
) {
  const { slug } = await params;
  const project = getProject(slug);
  if (!project) notFound();
  // …compose BackLink → header → MetricCallout → 3 Sections
}
```

<!-- BackLink shape — 02-UI-SPEC.md §Components/BackLink lines 564-591 -->

```tsx
type BackLinkProps = { href: string; children: ReactNode };
export function BackLink(props: BackLinkProps): JSX.Element;
```

<!-- Phase 1 primitives + Plan 02-02 components used here -->

From `@/components/primitives/Section` (Phase 1) — wraps each of Problem/Approach/Result.
From `@/components/primitives/Tag` (Phase 1) — tech-chip row.
From `@/components/primitives/ExternalLink` (Phase 1) — GitHub + optional Demo links.
From `@/components/primitives/ArchitectureDiagram` (Phase 1) — Voice Intent Eval diagram (wired ready; asset filed in Plan 02-05).
From `@/components/primitives/MetricCallout` (Plan 02-02) — `scale="detail"` per D-Proj-04.
From `@/types/content` (Plan 02-01) — `Project` type.

<!-- Content shape evolution -->

Plan 02-01 created `content/projects.ts` with placeholder problem/approach/result strings. This plan REFINES those strings; the file is modified, not recreated.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BackLink primitive</name>
  <files>src/components/nav/BackLink.tsx</files>
  <read_first>
    - src/components/primitives/ExternalLink.tsx (focus-visible outline string verbatim; ↗ glyph wrapped in aria-hidden — the pattern BackLink mirrors)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§Components/BackLink lines 564-591)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md (§BackLink lines 324-349 — "Why no ExternalLink reuse?")
  </read_first>
  <action>
    Create `src/components/nav/BackLink.tsx` — internal back-navigation link. Composes Next.js `<Link>`, NOT `<ExternalLink>` (ExternalLink hardcodes `target="_blank"` which would break Next.js client-side route caching on internal nav).

    - Props: `{ href: string; children: ReactNode }`.
    - Imports: `import Link from "next/link";` and `import type { ReactNode } from "react";`.
    - Render: `<Link href={href} className="inline-flex items-center font-mono text-[length:var(--text-caption)] text-[color:var(--color-ink-muted)] hover:text-[color:var(--color-ink)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-ink)]"><span aria-hidden="true" className="mr-1">←</span>{children}</Link>`. The `←` is U+2190 LEFTWARDS ARROW (not `<`); aria-hidden so screen-readers don't announce "leftwards arrow."
    - Server Component — header-comment block per Phase 1 primitive style; reference 02-UI-SPEC §Components/BackLink.
    - Caption-sized Geist Mono, ink-muted resting → ink on hover/focus — same accent treatment as RoleHeader caption line and SiteFooter text.
  </action>
  <acceptance_criteria>
    - `grep -l "export function BackLink" src/components/nav/BackLink.tsx` matches.
    - `grep -c "from \"next/link\"" src/components/nav/BackLink.tsx` returns 1.
    - `grep -c "ExternalLink" src/components/nav/BackLink.tsx` returns 0 (does not import ExternalLink — uses Next.js Link directly).
    - `grep -E "aria-hidden=\"true\"" src/components/nav/BackLink.tsx` matches (← glyph wrapped).
    - `grep -E "←" src/components/nav/BackLink.tsx` matches (the actual U+2190 character — verify it's not ASCII `<-`).
    - `grep -E "focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-\[color:var\(--color-ink\)\]" src/components/nav/BackLink.tsx` matches.
    - Server-Component discipline: `grep -E "^\"use client\"" src/components/nav/BackLink.tsx` returns nothing.
    - `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit && grep -q "export function BackLink" src/components/nav/BackLink.tsx && ! grep -E "^\"use client\"" src/components/nav/BackLink.tsx && grep -q "next/link" src/components/nav/BackLink.tsx</automated>
  </verify>
  <done>BackLink exists as Server Component. Internal Next.js Link, focus-visible outline, ← glyph aria-hidden. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Refine problem/approach/result narratives in content/projects.ts</name>
  <files>src/content/projects.ts</files>
  <read_first>
    - src/content/projects.ts (current — read the existing problem/approach/result drafts from Plan 02-01)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-CONTEXT.md (§Project Detail Page Depth — D-Proj-01..04)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§"Project detail pages" lines 243-258; §Copywriting Contract voice rules 7-12)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md (§"State of the Art" — recruiter-facing tone guidance; Pitfall 2 — vague "passionate about AI" copy)
    - .planning/refs/RESUME-2026-05.md (only to cross-check personal-project facts — none of these projects are Asurion-related, so D-Asurion-03 confidentiality discipline does NOT apply here; what DOES apply is voice rule 7's "weekend project test")
  </read_first>
  <action>
    Refine the 4 projects' `problem` / `approach` / `result` strings. Targets per D-Proj-01: 300-500 total words per project across the three fields.

    Voice contract for these strings (UI-SPEC §Copywriting Contract):
    - Plain English; first-person OK ("I built…", "I noticed…"); imperative-action OK ("Encoded 5,800 places into pgvector").
    - **Forbidden words anywhere:** "passionate", "leveraged", "cutting-edge", "synergy", "rockstar", "ninja", "I love", "let's build", "amazing", "incredible", any superlative, the word "journey".
    - The "weekend project test" (PITFALL #1): every sentence must carry signal a recruiter couldn't get from the resume bullet alone. If a sentence summarizes "I built a vector search agent for SF dates" — that's the subtitle, not the body. The body explains *the interesting decision* and *the measurable outcome*.
    - Numbers where available. Concrete tech names (pgvector, HNSW, Airflow, BigQuery ML, LangGraph, Claude Sonnet, etc.). These are personal projects — public-tech names are encouraged here.

    Per-project structure suggestion (NOT prescriptive — the recruiter cares about depth, not template adherence):

    - **`sf-date-night-concierge`** —
      - `problem`: ~100 words on what "vector search agent that plans a date in SF" actually means — the user pain (date suggestions that don't account for venue type, distance, or vibe), why a vector-embedding approach over keyword search.
      - `approach`: ~150 words on the architecture — embed 5,800+ SF places via pgvector + HNSW index, the LangGraph orchestration, retrieval strategy (which fields, dimensionality, distance metric).
      - `result`: ~80 words on what shipped — live demo (if URL exists), retrieval accuracy or qualitative outcome, what was learned that's worth a recruiter knowing.

    - **`gtm-research-pipeline`** —
      - `problem`: ~100 words on the GTM (go-to-market) eval problem — LLM-as-judge unreliability, self-preference bias as the specific failure mode being measured.
      - `approach`: ~150 words on the pipeline architecture — paired prompt sampling, judge model rotation, Cohen's κ for inter-rater agreement, bootstrapped CIs for the score deltas.
      - `result`: ~80 words on what the eval surfaced — the κ value range, the self-preference bias magnitude, the methodological takeaway.

    - **`voice-intent-eval`** —
      - `problem`: ~100 words on the voice intent classification problem — TTS + ASR introduces noise that breaks naive LLM classifiers, recruiters care about robustness numbers.
      - `approach`: ~150 words on the eval architecture — two-stage Claude classifier (rephrase → classify), dual-judge benchmarking, CI integration. **This page renders DIAG-02 in Plan 02-05** — the approach prose should land BEFORE the diagram (since `02-UI-SPEC.md` line 765 specifies "after the first body paragraph" — i.e., a paragraph break, then the diagram, then more prose).
      - `result`: ~80 words — 100% intent accuracy across 80 scenarios (verbatim from the metric callout), the methodological move that got it there.

    - **`daily-weather-pipeline`** —
      - `problem`: ~100 words on the daily-retraining problem — feature drift in weather forecasting, why Airflow + BigQuery ML is the right call for a data-engineer-flavored ML pipeline.
      - `approach`: ~150 words on the architecture — Airflow DAG schedule, BigQuery ML training calls, evaluation gate before promotion to production.
      - `result`: ~80 words — retraining cadence achieved (the metric callout value), the operational outcome (e.g., reduced manual intervention, freshness target met).

    **Type preservation:** the type signature of `Project.problem | approach | result` from `src/types/content.ts` (Plan 02-01) is `string` (required). The strings can contain `\n\n` for paragraph breaks if the executor wants paragraph structure — the route renderer (Task 3) splits on `\n\n` and renders each chunk as a `<p>`.

    Do NOT add `diagram` to any project in this task — Plan 02-05 adds the Voice Intent Eval diagram property when the SVG asset ships.
  </action>
  <acceptance_criteria>
    - `grep -c "slug:" src/content/projects.ts` returns 4 (no projects added or removed).
    - For each of the 4 slugs (`sf-date-night-concierge`, `gtm-research-pipeline`, `voice-intent-eval`, `daily-weather-pipeline`): the `problem`/`approach`/`result` strings are each ≥ 50 words. Verify by inspection (each field's content has ≥ 50 space-separated tokens).
    - **No forbidden marketing words:** `grep -iE "(passionate|leveraged|cutting-edge|synergy|rockstar|ninja|I love|let.s build|amazing|incredible)" src/content/projects.ts | grep -v "^[[:space:]]*//" | grep -v "^[[:space:]]*\*" | wc -l` returns 0.
    - **No accidental Asurion content** (D-Asurion-04 — projects are personal, no Asurion references): `grep -iE "asurion" src/content/projects.ts | grep -v "^[[:space:]]*//" | wc -l` returns 0.
    - The `metric.value` strings from Plan 02-01 are preserved (regression check): `grep -E "value:\s*\"5,800\+\"" src/content/projects.ts` matches; `grep -E "value:\s*\"100%\"" src/content/projects.ts` matches; `grep -E "value:\s*\"Cohen.s κ\"" src/content/projects.ts` matches.
    - `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit && [ $(grep -c "slug:" src/content/projects.ts) -eq 4 ] && ! grep -iE "(passionate|leveraged|cutting-edge|synergy|rockstar|ninja)" src/content/projects.ts && ! grep -iE "asurion" src/content/projects.ts</automated>
  </verify>
  <done>4 projects each have refined problem/approach/result strings (≥ 50 words each). No forbidden marketing words. No accidental Asurion references. Card-facing metric values preserved. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Build /projects/[slug] dynamic route with generateStaticParams + generateMetadata</name>
  <files>src/app/projects/[slug]/page.tsx</files>
  <read_first>
    - src/lib/content.ts (the accessors this route calls — verify return types)
    - src/types/content.ts (Project type — verify field names like `diagram?`, `demo?`, `description?`)
    - src/components/primitives/Section.tsx (Section wraps Problem/Approach/Result — id, number, title, children)
    - src/components/primitives/Tag.tsx (tech-chip row)
    - src/components/primitives/ExternalLink.tsx (GitHub + optional Demo links)
    - src/components/primitives/ArchitectureDiagram.tsx (conditional render for voice-intent-eval — props { src, alt, caption? } with `alt` REQUIRED)
    - src/components/primitives/MetricCallout.tsx (scale="detail" per D-Proj-04 — Plan 02-02 output)
    - src/components/nav/BackLink.tsx (just-built in this plan's Task 1)
    - src/app/layout.tsx (Phase 1 — to see the `Metadata` import pattern; confirms `import type { Metadata } from "next"`)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md (§"Pattern 1: generateStaticParams + generateMetadata" lines 316-367)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§"Per-route page composition: /projects/[slug]" lines 745-772; §"Project detail pages" copywriting + layout)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md (§src/app/projects/[slug]/page.tsx lines 440-468)
  </read_first>
  <action>
    Create `src/app/projects/[slug]/page.tsx` — App Router dynamic route, async params, Server Component.

    **Top-level exports (mandatory):**

    1. `export async function generateStaticParams()` — returns `getAllProjects().map((p) => ({ slug: p.slug }))`. Pre-renders all 4 slugs at build, no SSR fallback.

    2. `export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata>` — awaits params, calls `getProject(slug)`. If undefined, return `{}`. Otherwise return `{ title: `${project.title} — James Nhek`, description: project.subtitle.slice(0, 160) }`. **Do NOT add `openGraph` or `twitter` keys** — Phase 3 / SEO-01..04 own those (RESEARCH.md Pitfall 10 line 958).

    3. `export default async function ProjectDetailPage({ params }: { params: Promise<{ slug: string }> })` — awaits params, calls `getProject(slug)`, calls `notFound()` from `next/navigation` if undefined.

    **Page body composition** (UI-SPEC §"Per-route page composition: /projects/[slug]" lines 745-772):

    ```
    <main>
      <div className="mx-auto max-w-2xl px-6 md:px-12 pt-8 md:pt-12 pb-4">
        <BackLink href="/">Back to home</BackLink>
      </div>
      <header className="mx-auto max-w-2xl px-6 md:px-12 pb-12 md:pb-16">
        <h1 className="text-[length:var(--text-heading)] font-medium tracking-[-0.01em] text-[color:var(--color-ink)]">{project.title}</h1>
        <p className="mt-3 text-[length:var(--text-body)] leading-[var(--leading-body)] text-[color:var(--color-ink-muted)]">{project.subtitle}</p>
        <div className="mt-6 flex flex-wrap gap-2">{project.tech.map((t) => <Tag key={t}>{t}</Tag>)}</div>
        <div className="mt-6 flex flex-wrap gap-4">
          <ExternalLink href={project.github}>GitHub</ExternalLink>
          {project.demo && <ExternalLink href={project.demo}>Demo</ExternalLink>}
        </div>
        <MetricCallout value={project.metric.value} label={project.metric.label} scale="detail" />
      </header>
      <Section number="01" title="Problem" id="problem">
        {project.problem.split("\n\n").map((para, i) => (<p key={i} className="text-[length:var(--text-body)] leading-[var(--leading-body)] text-[color:var(--color-ink)] mb-4">{para}</p>))}
      </Section>
      <Section number="02" title="Approach" id="approach">
        {project.approach.split("\n\n").map((para, i) => (<p key={i} className="text-[length:var(--text-body)] leading-[var(--leading-body)] text-[color:var(--color-ink)] mb-4">{para}</p>))}
        {project.diagram && (
          <ArchitectureDiagram src={project.diagram.src} alt={project.diagram.alt} caption={project.diagram.caption} />
        )}
      </Section>
      <Section number="03" title="Result" id="result">
        {project.result.split("\n\n").map((para, i) => (<p key={i} className="text-[length:var(--text-body)] leading-[var(--leading-body)] text-[color:var(--color-ink)] mb-4">{para}</p>))}
      </Section>
    </main>
    ```

    **Critical notes:**
    - **H1 uses `--text-heading`, NOT `--text-display`** — UI-SPEC line 96. The `--text-display` size is reserved for the home hero `<h1>` only ("James Nhek").
    - The `project.diagram &&` conditional renders the diagram inside the Approach section. In Plan 02-03 NO project has `diagram` populated, so this branch is dead code today. Plan 02-05 populates `voice-intent-eval`'s `diagram` field and ships the SVG.
    - `SiteFooter` is composed via `app/layout.tsx` (Plan 02-02 wired it) — this route inherits it; do NOT render `<SiteFooter />` from this page.
    - Header comment block — standard `//` block referencing PROJ-01..05, the `generateStaticParams` pattern source (Next.js 16 docs), and the deliberate omission of `openGraph`/`twitter` (Phase 3 owns).
    - Server Component — no `"use client"`.
  </action>
  <acceptance_criteria>
    - `ls src/app/projects/[slug]/page.tsx` finds the file (note: `[slug]` is a directory in App Router; the directory name includes the brackets).
    - `grep -c "export async function generateStaticParams" src/app/projects/[slug]/page.tsx` returns 1.
    - `grep -c "export async function generateMetadata" src/app/projects/[slug]/page.tsx` returns 1.
    - `grep -c "export default async function" src/app/projects/[slug]/page.tsx` returns 1.
    - `grep -c "import { notFound } from \"next/navigation\"" src/app/projects/[slug]/page.tsx` returns 1.
    - `grep -c "import type { Metadata } from \"next\"" src/app/projects/[slug]/page.tsx` returns 1.
    - `grep -c "getAllProjects\|getProject" src/app/projects/[slug]/page.tsx` returns ≥ 2 (both accessors used).
    - **Async params contract:** `grep -E "params: Promise<\{ slug: string \}>" src/app/projects/[slug]/page.tsx | wc -l` returns ≥ 2 (used in both `generateMetadata` signature and `ProjectDetailPage` signature).
    - **Awaited params:** `grep -c "await params" src/app/projects/[slug]/page.tsx` returns ≥ 2.
    - **Conditional diagram render:** `grep -E "project\.diagram && \(\s*<ArchitectureDiagram" src/app/projects/[slug]/page.tsx` matches OR a similar `{project.diagram && (...)}` guard.
    - **No openGraph / twitter keys:** `grep -E "openGraph:" src/app/projects/[slug]/page.tsx | wc -l` returns 0. `grep -E "twitter:" src/app/projects/[slug]/page.tsx | wc -l` returns 0.
    - **Title format:** `grep -E "\`\\\$\\{project\\.title\\} — James Nhek\`" src/app/projects/[slug]/page.tsx` matches (or equivalent template-literal form).
    - **Description truncation:** `grep -E "project\\.subtitle\\.slice\\(0, 160\\)" src/app/projects/[slug]/page.tsx` matches.
    - **H1 uses text-heading, not text-display:** `grep -E "<h1 className=\"text-\[length:var\(--text-heading\)\]" src/app/projects/[slug]/page.tsx` matches.
    - **Three Sections (01 Problem / 02 Approach / 03 Result):** `grep -c "<Section number=" src/app/projects/[slug]/page.tsx` returns 3.
    - **BackLink at top:** `grep -c "<BackLink href=\"/\"" src/app/projects/[slug]/page.tsx` returns 1.
    - **MetricCallout scale='detail':** `grep -E "scale=\"detail\"" src/app/projects/[slug]/page.tsx` matches.
    - Server-Component discipline: `grep -E "^\"use client\"" src/app/projects/[slug]/page.tsx` returns nothing.
    - **Build emits 4 static pages:** `pnpm build` exits 0 AND `find .next/server/app/projects -name "page.html" -o -name "*.html" | wc -l` returns ≥ 4 (one per slug) — exact path may vary by Next.js version; the JSON/HTML artifacts under `.next/server/app/projects/<slug>/` should appear for all 4 slugs. As a softer check: `pnpm build 2>&1 | grep -E "(/projects/sf-date-night-concierge|/projects/gtm-research-pipeline|/projects/voice-intent-eval|/projects/daily-weather-pipeline)"` shows the routes being statically generated.
    - **404 on invalid slug:** `pnpm dev` then `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/projects/nonexistent-slug` returns 404.
    - `pnpm tsc --noEmit` and `pnpm lint` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint && pnpm build && grep -q "export async function generateStaticParams" "src/app/projects/[slug]/page.tsx" && grep -q "export async function generateMetadata" "src/app/projects/[slug]/page.tsx" && [ $(grep -c "<Section number=" "src/app/projects/[slug]/page.tsx") -eq 3 ] && ! grep -E "openGraph:" "src/app/projects/[slug]/page.tsx"</automated>
  </verify>
  <done>The dynamic route exists at `src/app/projects/[slug]/page.tsx`. generateStaticParams pre-renders all 4 slugs. generateMetadata returns title + description only (no OG, no twitter). Page body renders BackLink → header → MetricCallout (detail scale) → Problem/Approach/Result Sections. Invalid slugs 404. Build produces 4 static pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `params.slug` → `getProject(slug)` | Untrusted-ish (recruiter can paste any URL). `getProject` returns `Project \| undefined`; `notFound()` narrows. No SQL, no injection vector — the slug indexes a typed-array via `.find`. |
| `project.demo` / `project.github` href → `<ExternalLink>` | Allowlisted at the primitive level (https/http/mailto). |
| `project.problem`/`approach`/`result` → `<p>` text content | Server-rendered as plain text. React's default text-escaping handles XSS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-SLUG | Information Disclosure | Invalid slug exposing 500 stack trace | mitigate | `notFound()` from `next/navigation` returns a 404 (Next.js renders the framework default or a custom `app/not-found.tsx` — the latter is Phase 3 POL-01). Acceptance criterion verifies 404 status. |
| T-02-03-DEMO | Tampering | Malicious `project.demo` URL | mitigate | Phase 1 `ExternalLink` allowlist (https/http/mailto only). A malformed demo URL degrades to plain text + dev warning. |
| T-02-03-SEO | Information Disclosure (reputation, scope creep) | Phase 2 adds `openGraph:` keys to generateMetadata; Phase 3's lib/seo.ts factory has to undo or merge | mitigate | RESEARCH.md Pitfall 10 — Phase 2 metadata is intentionally minimal: title + description only. Acceptance criterion: `grep -E "openGraph:" {file}` returns 0. |
| T-02-03-DRIFT | Information Disclosure (reputation) | Marketing voice drifts into refined problem/approach/result | mitigate | Voice rules 7-12. Acceptance criterion: forbidden-words grep returns 0. |
</threat_model>

<verification>
1. `pnpm tsc --noEmit` exits 0.
2. `pnpm lint` exits 0.
3. `pnpm build` exits 0 and the build output references all 4 slugs as statically generated.
4. `pnpm dev` then visit each in turn:
   - http://localhost:3000/projects/sf-date-night-concierge — renders BackLink + title + subtitle + tech chips + GitHub link + MetricCallout `5,800+` + 3 numbered Sections + SiteFooter.
   - http://localhost:3000/projects/gtm-research-pipeline — same shape; MetricCallout `Cohen's κ`.
   - http://localhost:3000/projects/voice-intent-eval — same shape; MetricCallout `100%`. **No diagram yet** — `project.diagram` is undefined; the conditional render produces nothing. Plan 02-05 populates the diagram.
   - http://localhost:3000/projects/daily-weather-pipeline — same shape; MetricCallout shows the retraining-cadence metric.
   - http://localhost:3000/projects/nonexistent — 404 page (Next.js framework default for now; Phase 3 POL-01 adds a styled `app/not-found.tsx`).
5. Click "Back to home" on each detail page — returns to `/`. URL hash does NOT need to be `#projects` (UI-SPEC §"BackLink targets" — `/` is locked; one-line change later if user prefers `#projects`).
6. **No DevTools console warnings** on any detail page (especially `validateDOMNesting` since detail pages do NOT use the whole-card-link pattern from ProjectCard).
7. **Mobile sanity** at 375px (Chrome DevTools iPhone 14 Pro): tech chips wrap; GitHub + Demo links stack vertically if both present; MetricCallout big number is legible.
</verification>

<success_criteria>
- BackLink primitive ships as a Server Component (no client directive).
- `/projects/[slug]` dynamic route file exists with `generateStaticParams`, `generateMetadata`, and an async default export — all three correctly typed for Next.js 16 async params (Promise<{ slug: string }>).
- All 4 project detail pages render at build time (visible in `pnpm build` output).
- Invalid slugs return 404 via `notFound()`.
- `project.problem`/`approach`/`result` strings are refined for voice in content/projects.ts (≥ 50 words each, no forbidden marketing words, no accidental Asurion references).
- Phase 2 metadata is minimal: `title` + `description` only. Zero `openGraph`/`twitter` keys.
- `pnpm tsc --noEmit && pnpm lint && pnpm build` all exit 0.
- Plan committed.
</success_criteria>

<output>
Create `.planning/phases/02-content-sections-with-confidentiality-gate/02-03-SUMMARY.md` with:
- Confirmation all 4 detail pages render and statically generate.
- Approximate word count per project (problem/approach/result).
- A note that `voice-intent-eval`'s `diagram` field is still `undefined`; Plan 02-05 populates it.
- Any voice ambiguity worth James reviewing before phase verification (e.g., "I used contractions throughout — confirm the conversational register feels right for a recruiter-facing detail page").
- Vercel preview URL.
</output>
