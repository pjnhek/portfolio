---
phase: 02-content-sections-with-confidentiality-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/primitives/MetricCallout.tsx
  - src/components/primitives/SiteFooter.tsx
  - src/components/cards/ProjectCard.tsx
  - src/components/experience/RoleHeader.tsx
  - src/components/experience/ExperienceBlock.tsx
  - src/components/experience/EducationItem.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - SEC-01
  - SEC-03
  - SEC-04
  - SEC-05
  - SEC-08
  - USES-03

tags:
  - home-composition
  - experience-section
  - projects-section
  - footer

must_haves:
  truths:
    - "Recruiter visiting the Vercel preview sees Hero → 01. About (placeholder retained for Plan 02-05) → 02. Experience (3 roles + 2 education entries, Asurion-only tags) → 03. Featured Projects (4 cards in 1-col mobile / 2-col desktop grid) → 04. Contact (placeholder retained for Plan 02-05)."
    - "SiteFooter renders on the home route — single line at ≥md, stacked at 375px — with /uses internal Link + GitHub + LinkedIn ExternalLinks + © year."
    - "Section numbering on / is exactly 4 (About=01, Experience=02, Projects=03, Contact=04); home 'Uses' section is REMOVED — /uses is reachable only from the footer."
    - "Hero block in app/page.tsx lines 19-35 is byte-identical to Phase 1 (frozen — SEC-01)."
    - "ProjectCard's whole-card <Link> wraps title/subtitle/metric/description/tech; GitHub ExternalLink is a SIBLING outside the <Link> but inside the <article> border (no nested <a>, no stopPropagation)."
    - "Each interactive element (ProjectCard Link, BackLink, SiteFooter /uses link) ships focus-visible: outline (2px ink, 2px offset) matching Phase 1 ExternalLink."
    - "All new components are Server Components (no `\"use client\"` directive anywhere in this plan's diff)."
    - "pnpm build emits a static home page; pnpm tsc --noEmit and pnpm lint exit 0."
  artifacts:
    - path: "src/components/primitives/MetricCallout.tsx"
      provides: "Big-number poster — value + label, scale: card | detail"
      contains: "export function MetricCallout"
    - path: "src/components/primitives/SiteFooter.tsx"
      provides: "Sitewide footer composed in layout.tsx"
      contains: "export function SiteFooter"
    - path: "src/components/cards/ProjectCard.tsx"
      provides: "Project card surface used in home grid"
      contains: "export function ProjectCard"
    - path: "src/components/experience/RoleHeader.tsx"
      provides: "Title–Company line + Dates–Location caption line"
      contains: "export function RoleHeader"
    - path: "src/components/experience/ExperienceBlock.tsx"
      provides: "RoleHeader + bullets + optional tag-chip row"
      contains: "export function ExperienceBlock"
    - path: "src/components/experience/EducationItem.tsx"
      provides: "Program · Institution + Dates row"
      contains: "export function EducationItem"
    - path: "src/app/layout.tsx"
      provides: "RootLayout composes SiteFooter after {children}"
      contains: "<SiteFooter"
    - path: "src/app/page.tsx"
      provides: "Home — Hero + 4 numbered Sections (About placeholder, Experience real, Projects real, Contact placeholder)"
      contains: "<ProjectCard"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/content.ts"
      via: "import { getAllProjects } from \"@/lib/content\""
      pattern: "from \"@/lib/content\""
    - from: "src/app/page.tsx"
      to: "src/content/experience.ts"
      via: "import { roles, education } from \"@/content/experience\""
      pattern: "from \"@/content/experience\""
    - from: "src/components/cards/ProjectCard.tsx"
      to: "src/components/primitives/MetricCallout.tsx"
      via: "import { MetricCallout }"
      pattern: "MetricCallout"
    - from: "src/app/layout.tsx"
      to: "src/components/primitives/SiteFooter.tsx"
      via: "import { SiteFooter }"
      pattern: "SiteFooter"
---

<objective>
Compose the **home-page recruiter narrative** by:

1. Building the six new components (`MetricCallout`, `SiteFooter`, `ProjectCard`, `RoleHeader`, `ExperienceBlock`, `EducationItem`) — all Server Components, all using the Phase 1 arbitrary-value Tailwind token idiom.
2. Wiring `<SiteFooter />` into `src/app/layout.tsx` after `{children}` (D-Uses-03 — sitewide footer; USES-03 — footer is the only `/uses` entry point).
3. Refactoring `src/app/page.tsx` to compose: frozen Hero (unchanged from Phase 1) → `01. About` (placeholder retained — real copy lands in Plan 02-05) → `02. Experience` (full content: 3 roles + 2 education entries via the new components, Asurion `tags`-only) → `03. Featured Projects` (4 `ProjectCard`s in a `grid-cols-1 md:grid-cols-2 gap-8` grid + `See more on GitHub ↗` link) → `04. Contact` (placeholder retained — full Contact ships in Plan 02-05). Drop the home `04. Uses` section per UI-SPEC §"Section numbering scheme on /" (locked: 4 numbered sections, not 5).

**MVP slice outcome:** After this plan, a recruiter on the Vercel preview sees the real Experience section (with the Asurion role + tags + bullets from Plan 02-01) and the real Featured Projects grid with 4 cards. About + Contact remain placeholders — they're filled in Plan 02-05 alongside the diagrams and CopyEmail, because that's where the confidentiality gate executes against the Asurion-touching About-closing sentence.

Purpose:
- Deliver the **biggest single chunk of recruiter-visible value in Phase 2** (Experience + Projects = the heart of the portfolio narrative).
- Establish the six new component conventions (cards/, experience/, primitives/SiteFooter, primitives/MetricCallout) ahead of the route work in 02-03 / 02-04.
- Wire SiteFooter at the layout level so 02-03 (/projects/[slug]) and 02-04 (/uses) inherit it without further layout changes.

Output: 6 new component files + layout.tsx + page.tsx modifications. Build passes. Home page renders the full Experience + Projects narrative on Vercel preview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-CONTEXT.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md
@.planning/phases/02-content-sections-with-confidentiality-gate/02-01-SUMMARY.md

<interfaces>
<!-- Phase 1 primitives Plan 02-02 composes. Verbatim signatures from source. -->

From src/components/primitives/Section.tsx:
```tsx
type SectionProps = { id: string; number: string; title: string; children: ReactNode };
export function Section(props: SectionProps): JSX.Element;
```

From src/components/primitives/NumberedHeading.tsx (composed inside Section): defaults to `<h2>`; this is why the hero stays inline (only `<h1>` on /).

From src/components/primitives/Tag.tsx:
```tsx
type TagProps = { children: ReactNode };
export function Tag(props: TagProps): JSX.Element;
```

From src/components/primitives/ExternalLink.tsx:
```tsx
type ExternalLinkProps = { href: string; children: ReactNode; showGlyph?: boolean };
export function ExternalLink(props: ExternalLinkProps): JSX.Element;
```
Safe protocols: `https://`, `http://`, `mailto:`. ↗ glyph rendered by default; pass `showGlyph={false}` to suppress.

From src/components/primitives/ArchitectureDiagram.tsx: `{ src, alt, caption? }` — `alt` REQUIRED at type level. Not used directly in this plan (Plan 02-05 places DIAG-01 inside the Asurion ExperienceBlock).

From src/types/content.ts (Plan 02-01):
```ts
export type Project = { slug, title, subtitle, metric: Metric, tech: string[], github, demo?, description?, problem, approach, result, diagram? };
export type Role = { title, company, dates, location, bullets: string[], tags?: string[] };
export type EducationItem = { program, institution, dates };
export type SiteConfig = { name, tagline, location, email, github, linkedin, baseUrl };
```

From src/lib/content.ts (Plan 02-01):
```ts
export function getAllProjects(): readonly Project[];
export function getProject(slug: string): Project | undefined;
```

From src/content/experience.ts (Plan 02-01):
```ts
export const roles: Role[];      // [Asurion, Tax Analyst, FWD] order
export const education: EducationItem[];  // [USF, UH]
```

From src/content/site.ts (Plan 02-01):
```ts
export const site: SiteConfig;
```
</interfaces>

<existing_files>
- `src/app/page.tsx` (Phase 1 — hero block lines 19-35 stays VERBATIM; all `<Section>` calls are replaced).
- `src/app/layout.tsx` (Phase 1 — only addition is one import + one JSX line after `{children}`).
</existing_files>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build MetricCallout + SiteFooter primitives</name>
  <files>src/components/primitives/MetricCallout.tsx, src/components/primitives/SiteFooter.tsx</files>
  <read_first>
    - src/components/primitives/Tag.tsx (header-comment style, minimal Server Component shape)
    - src/components/primitives/NumberedHeading.tsx (two-stacked-spans pattern with Mono + Sans, tabular-nums, ink + ink-muted)
    - src/components/primitives/Section.tsx (mx-auto max-w-2xl px-6 md:px-12 content column reused by SiteFooter, swap <section>→<footer>)
    - src/components/primitives/ExternalLink.tsx (focus-visible: outline pattern — verbatim 4-class string)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§Components/MetricCallout lines 378-411; §Components/SiteFooter lines 616-649; §Spacing-Scale "SiteFooter block padding")
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md (§MetricCallout lines 160-187; §SiteFooter lines 190-225)
  </read_first>
  <action>
    Build the two new primitives.

    **`src/components/primitives/MetricCallout.tsx`** (D-Metric-01..03, UI-SPEC §Components/MetricCallout):
    - `MetricCalloutProps = { value: string; label: string; scale?: "card" | "detail" }`. Default `scale = "card"`.
    - Render a block-level wrapper `<div className="my-6">` containing two stacked spans:
      - Value span: `font-mono font-medium tabular-nums tracking-[-0.01em] text-[length:clamp(28px,1.25rem+2vw,40px)] text-[color:var(--color-ink)]` (when `scale === "card"`); swap the clamp to `text-[length:clamp(36px,1.5rem+3vw,48px)]` when `scale === "detail"`.
      - Label span: `mt-1 font-mono text-[length:var(--text-caption)] text-[color:var(--color-ink-muted)]`.
    - Server Component — header comment per 02-PATTERNS.md lines 167-174 (must include "Server Component (no client-island directive). No event handlers.").
    - No event handlers, no hover state, no `<button>`. Pure render.
    - Important: the value clamp expressions go inline as `text-[length:...]` arbitrary values per UI-SPEC line 108 ("No new font-size token introduced. The contract budget holds.").

    **`src/components/primitives/SiteFooter.tsx`** (D-Uses-03, USES-03, UI-SPEC §Components/SiteFooter):
    - No props. Imports `{ site }` from `@/content/site` for the GitHub and LinkedIn URLs.
    - Renders `<footer className="mx-auto max-w-2xl px-6 md:px-12 py-12 md:py-16">` (matches Section.tsx content-column width + UI-SPEC §Spacing-Scale "SiteFooter block padding" row).
    - Inside, a `<div className="border-t border-[color:var(--color-rule)] pt-8 font-mono text-[length:var(--text-caption)] leading-[var(--leading-caption)] text-[color:var(--color-ink-muted)]">` hairline+text container.
    - Inside that, a flex row that stacks vertically at 375px and goes horizontal at ≥md: `flex flex-col gap-2 md:flex-row md:items-center md:justify-between`.
    - Left group (links): `flex flex-wrap items-center gap-x-3 gap-y-1` containing `<Link href="/uses">` (internal Next.js Link, with the same `focus-visible:` outline string from ExternalLink.tsx line 57 verbatim and a `hover:text-[color:var(--color-ink)]` on rest state), then `<span aria-hidden="true">·</span>` (U+00B7 middle dot), then `<ExternalLink href={site.github}>GitHub</ExternalLink>`, then another `·` span, then `<ExternalLink href={site.linkedin}>LinkedIn</ExternalLink>`.
    - Right group: `<div>© 2026 James Nhek</div>` — year `2026` hardcoded for v1 (UI-SPEC line 305).
    - Server Component — same header comment discipline.
    - Use `<Link>` from `next/link` for the internal `/uses` link (NOT `ExternalLink`, which would set `target="_blank"`).
  </action>
  <acceptance_criteria>
    - `grep -l "export function MetricCallout" src/components/primitives/MetricCallout.tsx` matches.
    - `grep -l "export function SiteFooter" src/components/primitives/SiteFooter.tsx` matches.
    - **Server-Component discipline:** `grep -lE "^\"use client\"" src/components/primitives/MetricCallout.tsx src/components/primitives/SiteFooter.tsx` returns nothing.
    - `grep -E "Server Component \(no client-island directive\)" src/components/primitives/MetricCallout.tsx src/components/primitives/SiteFooter.tsx` matches both files.
    - `grep -E "clamp\(28px,1\.25rem\+2vw,40px\)" src/components/primitives/MetricCallout.tsx` matches (card scale).
    - `grep -E "clamp\(36px,1\.5rem\+3vw,48px\)" src/components/primitives/MetricCallout.tsx` matches (detail scale).
    - `grep -E "scale.*\?.*\"card\".*\"detail\"" src/components/primitives/MetricCallout.tsx | wc -l` >= 1 OR a `type MetricCalloutProps` declaration that includes `scale?: "card" | "detail"`.
    - `grep -E "import \{ site \} from \"@/content/site\"" src/components/primitives/SiteFooter.tsx` matches.
    - `grep -E "<Link href=\"/uses\"" src/components/primitives/SiteFooter.tsx` matches.
    - `grep -E "ExternalLink href=\{site\.github\}" src/components/primitives/SiteFooter.tsx` matches.
    - `grep -E "ExternalLink href=\{site\.linkedin\}" src/components/primitives/SiteFooter.tsx` matches.
    - `grep -E "© 2026 James Nhek" src/components/primitives/SiteFooter.tsx` matches.
    - `grep -E "focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-\[color:var\(--color-ink\)\]" src/components/primitives/SiteFooter.tsx` matches.
    - `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit && grep -q "export function MetricCallout" src/components/primitives/MetricCallout.tsx && grep -q "export function SiteFooter" src/components/primitives/SiteFooter.tsx && ! grep -lE "^\"use client\"" src/components/primitives/MetricCallout.tsx src/components/primitives/SiteFooter.tsx</automated>
  </verify>
  <done>
    Two primitives exist as Server Components. MetricCallout takes `value`/`label`/`scale` props. SiteFooter renders the locked footer markup with `/uses` internal link + GitHub + LinkedIn externals + © line. TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build ProjectCard + Experience sub-components (RoleHeader, ExperienceBlock, EducationItem)</name>
  <files>src/components/cards/ProjectCard.tsx, src/components/experience/RoleHeader.tsx, src/components/experience/ExperienceBlock.tsx, src/components/experience/EducationItem.tsx</files>
  <read_first>
    - src/components/primitives/Tag.tsx (chip pattern + 1px-rule border)
    - src/components/primitives/ExternalLink.tsx (focus-visible outline string verbatim; ↗ glyph handling — must NOT wrap project-card root or the GitHub link sits outside the Link wrapper)
    - src/components/primitives/NumberedHeading.tsx (two-span Sans+Mono baseline pattern — analog for RoleHeader)
    - src/components/primitives/Section.tsx (sets the parent column width — these components render inside Section.children)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§Components/ProjectCard lines 413-462; §Components/RoleHeader lines 463-495; §Components/ExperienceBlock lines 496-533; §Components/EducationItem lines 534-563)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md (§ProjectCard lines 228-251; §RoleHeader lines 254-277; §ExperienceBlock lines 280-295; §EducationItem lines 298-303)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-RESEARCH.md (§Pattern 2 "Whole-card link with internal nav + external sibling link" lines 369-414; Pitfall 4 — nested <a> tags)
  </read_first>
  <action>
    Build four composed components, all Server Components.

    **`src/components/cards/ProjectCard.tsx`** (D-Proj-03, D-Metric-02, SEC-04, SEC-05, UI-SPEC §Components/ProjectCard):
    - Props: `{ project: Project }` (typed from `@/types/content`).
    - Outer element: `<article className="border border-[color:var(--color-rule)] p-6">` — 1px `--color-rule` hairline border, 24px inner padding, no background fill, no shadow, no rounded corners.
    - **Inside the article**, render TWO siblings, in this order:
      1. A `<Link href={`/projects/${project.slug}`}>` (Next.js `Link`) wrapping: `<h3>` (title, subhead, font-medium, ink), `<p>` (subtitle, body, ink-muted with `motion-safe:transition-colors group-hover:text-[color:var(--color-ink)]` for the hover transition gated by `prefers-reduced-motion`), `<MetricCallout value={project.metric.value} label={project.metric.label} />`, optionally `<p>` description if `project.description` exists (guarded with `{project.description && (...)}`), and a `<div className="mt-4 flex flex-wrap gap-2">` containing `{project.tech.map((t) => <Tag key={t}>{t}</Tag>)}`.
      2. A `<div className="mt-6"><ExternalLink href={project.github}>GitHub</ExternalLink></div>` SIBLING to the `<Link>` — i.e., NOT nested inside it. This avoids the nested-`<a>` HTML invalid (RESEARCH.md Pattern 2 / Pitfall 4 / D-Proj-03).
    - The `<Link>` itself carries the focus-visible outline string verbatim from ExternalLink.tsx line 57 (`focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-ink)]`) plus `group block`.
    - **Server Component.** No `"use client"`.

    **`src/components/experience/RoleHeader.tsx`** (D-Exp-04, UI-SPEC §Components/RoleHeader):
    - Props: `{ title: string; company: string; dates: string; location: string }`.
    - Render `<header className="mb-4">` containing TWO stacked elements (not flex-baseline — always two lines regardless of viewport):
      - `<h3>` (semantic level 3 — keeps outline coherent since `<h2>` is `02. Experience`): `text-[length:var(--text-body)] font-medium leading-[var(--leading-body)] text-[color:var(--color-ink)]`. Contents: `{title} <span aria-hidden="true">—</span> {company}`.
      - `<p>`: `mt-1 font-mono text-[length:var(--text-caption)] leading-[var(--leading-caption)] tabular-nums text-[color:var(--color-ink-muted)]`. Contents: `{dates} <span aria-hidden="true">—</span> {location}`. **If `location` is the empty string** (FWD / Tax Analyst case), render just `{dates}` without the em-dash + location segment.
    - Em-dash is U+2014 (`—`) — verify by file inspection; do NOT use ASCII `--`.
    - Server Component.

    **`src/components/experience/ExperienceBlock.tsx`** (D-Exp-02, D-Exp-03, UI-SPEC §Components/ExperienceBlock):
    - Props: `{ role: Role }`.
    - Render `<article className="space-y-4">` containing:
      - `<RoleHeader {...role} />` (pass title, company, dates, location).
      - `<ul className="space-y-3">` with `{role.bullets.map((b) => (<li key={b} className="text-[length:var(--text-body)] leading-[var(--leading-body)] text-[color:var(--color-ink)] pl-4 relative"><span aria-hidden="true" className="absolute left-0">–</span>{b}</li>))}` — note: en-dash U+2013 (`–`) as bullet marker, NOT em-dash; `aria-hidden` so SR doesn't read "en-dash" each row.
      - Conditional tag-chip row: `{role.tags && role.tags.length > 0 && (<div className="flex flex-wrap gap-2 pt-2">{role.tags.map((t) => <Tag key={t}>{t}</Tag>)}</div>)}`. Per D-Exp-03 this only renders for Asurion (the only role with `tags` populated in content/experience.ts).
    - Server Component.
    - **NOTE for Plan 02-05:** The DIAG-01 Asurion architecture diagram will be wired into ExperienceBlock in Plan 02-05 by either (a) adding an optional `diagram?: { src; alt; caption }` prop OR (b) rendering `<ArchitectureDiagram>` inline between bullets and tags when `role.company === "Asurion"`. For Plan 02-02, ExperienceBlock has NO diagram concern — it's a clean role-renderer. Plan 02-05 decides the wiring.

    **`src/components/experience/EducationItem.tsx`** (D-Exp-01, UI-SPEC §Components/EducationItem):
    - Props: `{ program: string; institution: string; dates: string }`.
    - Render `<div className="flex flex-col gap-1 md:flex-row md:items-baseline md:justify-between md:gap-4">`:
      - Left `<div className="text-[length:var(--text-body)] leading-[var(--leading-body)]">`: `<span className="font-medium text-[color:var(--color-ink)]">{program}</span><span className="text-[color:var(--color-ink)]"> · {institution}</span>`. Middle dot is U+00B7 (`·`).
      - Right `<div className="font-mono text-[length:var(--text-caption)] tabular-nums text-[color:var(--color-ink-muted)]">{dates}</div>`.
    - Server Component.

    All four files must have the verbose `//`-block header comment style from Phase 1 primitives — reference the requirement ID (SEC-04 / SEC-05 / SEC-03) and UI-SPEC section.
  </action>
  <acceptance_criteria>
    - `grep -l "export function ProjectCard" src/components/cards/ProjectCard.tsx` matches.
    - `grep -l "export function RoleHeader" src/components/experience/RoleHeader.tsx` matches.
    - `grep -l "export function ExperienceBlock" src/components/experience/ExperienceBlock.tsx` matches.
    - `grep -l "export function EducationItem" src/components/experience/EducationItem.tsx` matches.
    - **Server-Component discipline:** `grep -lE "^\"use client\"" src/components/cards/ProjectCard.tsx src/components/experience/RoleHeader.tsx src/components/experience/ExperienceBlock.tsx src/components/experience/EducationItem.tsx` returns nothing.
    - **No nested `<a>`:** the `<Link>` in ProjectCard wraps the metric/title/subtitle/description/tech ONLY — the `<ExternalLink href={project.github}>` is rendered as a SIBLING (not a descendant) of the `<Link>`. Verifiable: `awk '/<Link href=\`\\/projects/,/<\\/Link>/' src/components/cards/ProjectCard.tsx | grep -c "ExternalLink"` returns 0 (no ExternalLink inside the Link).
    - `grep -c "MetricCallout" src/components/cards/ProjectCard.tsx` returns ≥ 2 (import + render).
    - `grep -c "<Tag" src/components/cards/ProjectCard.tsx` returns ≥ 1 (render — chips loop).
    - `grep -E "aria-hidden=\"true\"" src/components/experience/RoleHeader.tsx` matches (em-dash glyph).
    - `grep -E "aria-hidden=\"true\"" src/components/experience/ExperienceBlock.tsx` matches (en-dash bullet glyph).
    - `grep -E "U\+2014|—" src/components/experience/RoleHeader.tsx` matches the em-dash literally OR the comment notes the unicode codepoint.
    - `grep -E "U\+2013|–" src/components/experience/ExperienceBlock.tsx` matches the en-dash.
    - `grep -E "role\.tags && role\.tags\.length > 0" src/components/experience/ExperienceBlock.tsx` matches (D-Exp-03 conditional).
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint && ! grep -lE "^\"use client\"" src/components/cards/ProjectCard.tsx src/components/experience/RoleHeader.tsx src/components/experience/ExperienceBlock.tsx src/components/experience/EducationItem.tsx && grep -q "export function ProjectCard" src/components/cards/ProjectCard.tsx && grep -q "role\.tags && role\.tags\.length > 0" src/components/experience/ExperienceBlock.tsx</automated>
  </verify>
  <done>
    Four composed components exist. ProjectCard's `<Link>` and `<ExternalLink>` are siblings inside `<article>` — never nested. RoleHeader is two-line, always. ExperienceBlock conditionally renders the tag-chip row only when `role.tags` is non-empty. EducationItem stacks at 375px and goes horizontal at ≥md. All Server Components.
  </done>
</task>

<task type="auto">
  <name>Task 3: Compose SiteFooter into layout.tsx and refactor page.tsx to render real Experience + Projects (keep About + Contact placeholders)</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    - src/app/layout.tsx (current Phase 1 file — only addition is one import + one JSX line after {children}; DO NOT touch metadata, fonts, env import)
    - src/app/page.tsx (current Phase 1 file — Hero block lines 19-35 stays VERBATIM)
    - src/components/primitives/Section.tsx (props shape — id, number, title, children)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-UI-SPEC.md (§Section numbering scheme on / lines 168-177; §"Per-route page composition: /" lines 723-743)
    - .planning/phases/02-content-sections-with-confidentiality-gate/02-PATTERNS.md (§src/app/page.tsx lines 377-437; §src/app/layout.tsx lines 508-525)
    - src/content/experience.ts (Plan 02-01 output — roles + education arrays)
    - src/content/projects.ts (Plan 02-01 output — projects array)
    - src/lib/content.ts (Plan 02-01 output — getAllProjects accessor)
    - src/content/site.ts (Plan 02-01 output — site.github for the "See more on GitHub" link)
  </read_first>
  <action>
    Two coordinated file edits.

    **`src/app/layout.tsx` (one additive change):**
    - Add `import { SiteFooter } from "@/components/primitives/SiteFooter";` near the other primitive imports (or below the existing imports — placement does not matter, only the import).
    - In the `RootLayout` JSX `<body>{children}</body>`, change to `<body>{children}<SiteFooter /></body>`.
    - **DO NOT MODIFY**: the `metadata` export (lines 26-31), the Geist/Geist_Mono font config, the `env` import, the `<html lang=...>` element. Phase 3 owns SEO; Phase 2 must not retouch metadata.

    **`src/app/page.tsx` (substantial refactor — hero is the ONLY block that stays byte-identical):**
    - Imports update — add: `import { ProjectCard } from "@/components/cards/ProjectCard";`, `import { ExperienceBlock } from "@/components/experience/ExperienceBlock";`, `import { EducationItem } from "@/components/experience/EducationItem";`, `import { roles, education } from "@/content/experience";`, `import { getAllProjects } from "@/lib/content";`, `import { site } from "@/content/site";`. **Remove** the existing `ArchitectureDiagram` import (it's no longer used here — DIAG-01 placement moves to ExperienceBlock when Plan 02-05 ships, but in this plan the About diagram is removed entirely and ExperienceBlock has no diagram yet).
    - **Hero block (current lines 19-35 inside `<header className="px-6 pt-24 pb-12 md:px-12 md:pt-32 md:pb-16">`):** byte-identical. Do not change the heading text, the `<p>` sequence, or any className. Hero is FROZEN per Phase 1 D-07 / SEC-01.
    - Replace the existing 5 `<Section>` calls with EXACTLY 4 (UI-SPEC §"Section numbering scheme on /" — locked: 4 numbered sections; home `04. Uses` is REMOVED):

      Section 01 — About (placeholder retained, plan 02-05 fills real copy):
      ```
      <Section id="about" number="01" title="About">
        <p className="text-[length:var(--text-body)] leading-[var(--leading-body)] text-[color:var(--color-ink-muted)]">
          Coming soon — drafted in Plan 02-05 under the confidentiality gate.
        </p>
      </Section>
      ```
      **CRITICAL:** Remove the existing ArchitectureDiagram from About (UI-SPEC §"Asurion diagram placement" — About becomes pure prose; DIAG-01 moves to the Asurion ExperienceBlock when Plan 02-05 ships).

      Section 02 — Experience (real content):
      ```
      <Section id="experience" number="02" title="Experience">
        <div className="space-y-12 md:space-y-16">
          {roles.map((role) => (<ExperienceBlock key={role.company} role={role} />))}
        </div>
        <hr className="my-12 border-t border-[color:var(--color-rule)] md:my-16" />
        <div className="space-y-6">
          {education.map((ed) => (<EducationItem key={ed.program} program={ed.program} institution={ed.institution} dates={ed.dates} />))}
        </div>
      </Section>
      ```

      Section 03 — Featured Projects (real content):
      ```
      <Section id="projects" number="03" title="Featured Projects">
        <div className="grid grid-cols-1 gap-8 md:grid-cols-2">
          {getAllProjects().map((p) => (<ProjectCard key={p.slug} project={p} />))}
        </div>
        <p className="mt-12">
          <ExternalLink href={site.github}>See more on GitHub</ExternalLink>
        </p>
      </Section>
      ```

      Section 04 — Contact (placeholder retained, Plan 02-05 fills CopyEmail + mailto + LinkedIn + GitHub):
      ```
      <Section id="contact" number="04" title="Contact">
        <p className="text-[length:var(--text-body)] leading-[var(--leading-body)] text-[color:var(--color-ink-muted)]">
          Coming soon — wired in Plan 02-05.
        </p>
      </Section>
      ```

    - The numbered-section count on `/` is now 4, NOT 5. UI-SPEC §"Section numbering scheme on /" is locked: drop the home `04. Uses` entirely (USES-03 routes `/uses` via the footer only — which Plan 02-02 has now wired via SiteFooter in layout.tsx).
    - Header comment block at the top of page.tsx should be updated to reflect Phase 2 wiring (remove "Phase 1 / Plan 01-02" language, note that the hero is frozen verbatim and Experience + Projects compose real content from `src/content/`).
  </action>
  <acceptance_criteria>
    - **layout.tsx:**
      - `grep -c "import { SiteFooter }" src/app/layout.tsx` returns 1.
      - `grep -c "<SiteFooter" src/app/layout.tsx` returns 1.
      - `grep -c "metadataBase" src/app/layout.tsx` returns 1 (Phase 1 metadata untouched).
      - `grep -c "James Nhek — AI Engineer" src/app/layout.tsx` returns 1 (default title untouched).
    - **page.tsx:**
      - `grep -c "import { ProjectCard }" src/app/page.tsx` returns 1.
      - `grep -c "import { ExperienceBlock }" src/app/page.tsx` returns 1.
      - `grep -c "import { EducationItem }" src/app/page.tsx` returns 1.
      - `grep -c "import { roles, education }" src/app/page.tsx` returns 1.
      - `grep -c "import { getAllProjects }" src/app/page.tsx` returns 1.
      - `grep -c "<Section" src/app/page.tsx` returns 4 (NOT 5 — home Uses removed).
      - `grep -c "number=\"05\"" src/app/page.tsx` returns 0 (UI-SPEC locked: 4 numbered sections, no 05).
      - `grep -E "id=\"about\".*number=\"01\".*title=\"About\"" src/app/page.tsx` matches (order may vary; presence required).
      - `grep -E "id=\"contact\".*number=\"04\".*title=\"Contact\"" src/app/page.tsx` matches (Contact renumbered to 04).
      - `grep -c "<ArchitectureDiagram" src/app/page.tsx` returns 0 (About diagram removed; Plan 02-05 places DIAG-01 inside ExperienceBlock).
      - `grep -c "grid-cols-1 gap-8 md:grid-cols-2\|grid-cols-1 md:grid-cols-2 gap-8" src/app/page.tsx` returns 1 (Projects grid layout).
      - **Hero block frozen:** `grep -A 16 "Hero — outside any <section>" src/app/page.tsx | grep -E "(James Nhek|AI Engineer @ Asurion|RAG · evaluations · agentic workflows|San Francisco — open to AI Engineer roles\.)" | wc -l` returns 4 — all four hero copy lines present verbatim.
      - **No new `"use client"`:** `grep -c '"use client"' src/app/page.tsx src/app/layout.tsx` returns 0.
    - **Build + lint:**
      - `pnpm tsc --noEmit` exits 0.
      - `pnpm lint` exits 0.
      - `pnpm build` exits 0 and emits `.next/server/app/page.html` or equivalent (build artifact for the home route).
    - **Visual smoke (manual, optional in a follow-up):** `pnpm dev` at http://localhost:3000 renders Hero + 4 numbered sections; Experience shows 3 roles + 2 education entries; Projects shows 4 cards; SiteFooter is visible at the bottom of the home route.
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint && pnpm build && [ $(grep -c "<Section" src/app/page.tsx) -eq 4 ] && grep -q "SiteFooter" src/app/layout.tsx && grep -q 'id="contact".*number="04".*title="Contact"' src/app/page.tsx && [ $(grep -c "<ArchitectureDiagram" src/app/page.tsx) -eq 0 ]</automated>
  </verify>
  <done>
    layout.tsx renders SiteFooter after children. page.tsx renders the frozen Hero + 4 numbered Sections (About placeholder, Experience real, Projects real with 4 ProjectCards, Contact placeholder). Home `04. Uses` is gone; Contact is renumbered to `04.`. About diagram is removed. Build, typecheck, and lint all pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/*.ts → JSX render | Server-Component render path. No user input. No XSS surface. |
| site.github / site.linkedin → ExternalLink href | URL passed to ExternalLink primitive which enforces the safe-protocol allowlist (`https://`, `http://`, `mailto:`). Defends T-01-VECTOR-INJ trivially. |
| ProjectCard `<Link>` and sibling `<ExternalLink>` | Internal vs external link separation prevents nested `<a>` (HTML invalid) and the click-on-GitHub-navigates-to-detail bug. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-NEST | Tampering | ProjectCard nested `<a>` tags | mitigate | D-Proj-03 sibling pattern — GitHub `<ExternalLink>` lives OUTSIDE the `<Link>` but INSIDE the `<article>`. Acceptance criterion verifies via grep that the awk-extracted Link region contains no `<ExternalLink>`. |
| T-02-02-PROTO | Elevation of Privilege | Malicious `javascript:` href via content/site.ts or content/projects.ts | mitigate | Phase 1 `ExternalLink` allowlist (https/http/mailto only). If a content author drops in a `javascript:` URL, the primitive degrades to plain text + dev warning. |
| T-02-02-CLI | Information Disclosure | Accidental `"use client"` directive on a Server Component leaking server-only imports to the browser | mitigate | Acceptance criterion: `grep -lE "^\"use client\"" {6 new files} {layout.tsx} {page.tsx}` returns nothing. Server-Component discipline enforced by review. |
| T-02-02-FOCUS | Tampering (accessibility) | Missing focus-visible outline on new interactive elements | mitigate | Verbatim focus-ring string copied from ExternalLink.tsx line 57 onto ProjectCard's `<Link>` and SiteFooter's `<Link href="/uses">`. Acceptance criterion grep-verifies presence. |
| T-02-02-FROZEN | Information Disclosure (reputation) | Accidental edit to the frozen Phase 1 Hero copy | mitigate | Acceptance criterion: all 4 hero copy lines (`James Nhek`, `AI Engineer @ Asurion`, `RAG · evaluations · agentic workflows`, `San Francisco — open to AI Engineer roles.`) present verbatim in page.tsx. |
</threat_model>

<verification>
1. `pnpm tsc --noEmit` exits 0.
2. `pnpm lint` exits 0.
3. `pnpm build` exits 0 and produces a static home page.
4. `pnpm dev` + visit `http://localhost:3000` renders:
   - Hero (unchanged from Phase 1)
   - `01. About` placeholder
   - `02. Experience` — 3 roles + hairline + 2 education entries
   - `03. Featured Projects` — 4 cards in 1-col mobile / 2-col desktop grid + "See more on GitHub ↗" link
   - `04. Contact` placeholder
   - SiteFooter at bottom (single-line ≥md, stacked at 375px) — `/uses` internal Link + GitHub + LinkedIn ExternalLinks + © 2026 James Nhek
5. **Visual sanity at 375px** (Chrome DevTools iPhone preset): no horizontal scroll; project cards stack to 1 column; ExperienceBlock RoleHeader wraps to 2 stacked lines (Title—Company on line 1, Dates—Location in Mono caption on line 2); EducationItem stacks (program+institution above dates).
6. **Anchor navigation smoke test:** click `pjnhek.com.test/#projects` (or paste `/#projects` after `http://localhost:3000`) — page scrolls to Featured Projects, target heading is not flush against viewport top (4rem `scroll-padding-top` from Plan 02-01 takes effect), URL hash updates.
7. **No DevTools console warnings** on home page (especially `validateDOMNesting` — confirms ProjectCard sibling pattern).
</verification>

<success_criteria>
- 6 new component files exist and compile cleanly as Server Components.
- layout.tsx renders SiteFooter after `{children}` without touching Phase 1 metadata.
- page.tsx renders Hero (frozen) + 4 numbered Sections (About placeholder, Experience+Projects with real content, Contact placeholder).
- Home `04. Uses` section is GONE; Contact is now `04.`; `/uses` is reachable only via the footer link.
- About diagram placeholder is REMOVED from page.tsx.
- All file acceptance criteria pass.
- `pnpm tsc --noEmit && pnpm lint && pnpm build` all exit 0.
- DevTools console clean on home (no nested-anchor warnings).
- Plan committed.
</success_criteria>

<output>
Create `.planning/phases/02-content-sections-with-confidentiality-gate/02-02-SUMMARY.md` with:
- List of 6 new component files + 2 modified files.
- Confirmation the Hero block in page.tsx is byte-identical to Phase 1 lines 19-35.
- Confirmation the section count on / is exactly 4 (no home Uses).
- Confirmation no `"use client"` was introduced (still 0 client components in the codebase — that arrives in Plan 02-05).
- Preview URL of the Vercel deploy showing the rendered Experience + Projects.
- Any visual ambiguity worth flagging for Plan 02-05 (e.g., "Asurion role has 7 tag chips that wrap to 3 lines at 375px — confirm vertical rhythm with James before adding the diagram below").
</output>
